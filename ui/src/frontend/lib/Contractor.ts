// Automatically generated by cinp-codegen from http://127.0.0.1:8888/api/v1/ at 2026-06-02T13:22:12.797201+00:00

import CInP, { List } from 'cinp'

export interface ListFilter {};
export interface QueryFilter { toFilter(): object | undefined };
export class ListFilterAll implements ListFilter {};
type ModelUpdate<T> = T | Record<string, any>;
type ModelConstructorSource<T> = T | Record<string, any> | string | number | undefined; // and any other types used by pks

export class ListQueryFilter implements ListFilter
{
  filter?: QueryFilter;
  sort?: string[];

  constructor( { filter, sort }: { filter?: QueryFilter, sort?: string[] } )
  {
    this.filter = filter;
    this.sort = sort;
  }
}

export interface ListParameters
{
  filter?: ListFilter;
  position?: number;
  count?: number;
}

function filterConvert( filter_items: Record<string, any> ): object
{
  const item_list = [];

  for( const [ field, value ] of Object.entries( filter_items ) )
  {
    if( value === undefined )
      continue;

    item_list.push( { 'field': field, 'value': value, 'operation': '=' } );
  }

  while( item_list.length > 1 )
    item_list.push( { 'operation': 'and', 'left': item_list.pop(), 'right': item_list.pop() } );

  return item_list[0];
}

export class Contractor
{
  private cinp: CInP;

  constructor( host: string, using_cookies: boolean=false )
  {
    this.cinp = new CInP( host );
    this.cinp.using_cookies = using_cookies;
  }

  async check(): Promise<boolean>
  {
    return this.cinp.describe( '/api/v1/' ).then( res => res.version === '1.0' );
  }

  setHeader( name: string, value: string ): void
  {
    return this.cinp.setHeader( name, value );
  }

  clearHeader( name: string ): void
  {
    return this.cinp.clearHeader( name );
  }

  setServerErrorHandler( handler: ( message: string, trace: string ) => void ): void
  {
    this.cinp.server_error_handler = handler;
  }

  raw( verb: string, uri: string, data: object, header_map: Record<string, string> ): Promise<unknown>
  {
    return this.cinp.raw( verb, uri, data, header_map );
  }
  // Namespace  at /api/v1/ version 1.0
/*

*/

  // Namespace Auth at /api/v1/Auth/ version 2.0
/*

*/

  // Model User at /api/v1/Auth/User

  async Auth_User_call_login( username: string, password: string ): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Auth/User(login)", { "username": username, "password": password } );
    return ( _cinp_result_ as string );
  }


  async Auth_User_call_logout(): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Auth/User(logout)", {} );
    return;
  }


  async Auth_User_call_whoami(): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Auth/User(whoami)", {} );
    return ( _cinp_result_ as string );
  }


  async Auth_User_call_changePassword( password: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Auth/User(changePassword)", { "password": password } );
    return;
  }


  // Model User (end)
  // Namespace Auth (end)
  // Namespace BluePrint at /api/v1/BluePrint/ version 0.1
/*

*/

  // Model BluePrint at /api/v1/BluePrint/BluePrint

  async BluePrint_BluePrint_call_getConfig( id: string ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/BluePrint/BluePrint:" + id + ":(getConfig)", {} );
    return ( _cinp_result_ as {} );
  }


  // Model BluePrint (end)
  // Model FoundationBluePrint at /api/v1/BluePrint/FoundationBluePrint

  async BluePrint_FoundationBluePrint_get( id: string ): Promise<BluePrint_FoundationBluePrint>
  {
    const response = await this.cinp.getOne<BluePrint_FoundationBluePrint>( "/api/v1/BluePrint/FoundationBluePrint:" + id + ":" );

    return new BluePrint_FoundationBluePrint( this, response );
  }

  async BluePrint_FoundationBluePrint_create( data: BluePrint_FoundationBluePrint ): Promise<void>
  {
    const response = await this.cinp.create<BluePrint_FoundationBluePrint>( "/api/v1/BluePrint/FoundationBluePrint", data._as_map( true ) );

    data._update( response.data );
  }

  async BluePrint_FoundationBluePrint_update( id: string, data: BluePrint_FoundationBluePrint ): Promise<void>
  {
    data._update( await this.cinp.updateOne<BluePrint_FoundationBluePrint>( "/api/v1/BluePrint/FoundationBluePrint:" + id + ":", data._as_map( false ) ) );
  }

  async BluePrint_FoundationBluePrint_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/BluePrint/FoundationBluePrint:" + id + ":" );
  }

  _BluePrint_FoundationBluePrint_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async BluePrint_FoundationBluePrint_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, BluePrint_FoundationBluePrint>>
  {
    const [ filter_name, filter_value_map ] = this._BluePrint_FoundationBluePrint_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<BluePrint_FoundationBluePrint>( "/api/v1/BluePrint/FoundationBluePrint", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new BluePrint_FoundationBluePrint( this, response[ key ] );
    }

    return response;
  }

  async BluePrint_FoundationBluePrint_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._BluePrint_FoundationBluePrint_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/BluePrint/FoundationBluePrint", filter_name, filter_value_map, position, count );
  }

  async BluePrint_FoundationBluePrint_call_getConfig( id: string ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/BluePrint/FoundationBluePrint:" + id + ":(getConfig)", {} );
    return ( _cinp_result_ as {} );
  }


  // Model FoundationBluePrint (end)
  // Model StructureBluePrint at /api/v1/BluePrint/StructureBluePrint

  async BluePrint_StructureBluePrint_get( id: string ): Promise<BluePrint_StructureBluePrint>
  {
    const response = await this.cinp.getOne<BluePrint_StructureBluePrint>( "/api/v1/BluePrint/StructureBluePrint:" + id + ":" );

    return new BluePrint_StructureBluePrint( this, response );
  }

  async BluePrint_StructureBluePrint_create( data: BluePrint_StructureBluePrint ): Promise<void>
  {
    const response = await this.cinp.create<BluePrint_StructureBluePrint>( "/api/v1/BluePrint/StructureBluePrint", data._as_map( true ) );

    data._update( response.data );
  }

  async BluePrint_StructureBluePrint_update( id: string, data: BluePrint_StructureBluePrint ): Promise<void>
  {
    data._update( await this.cinp.updateOne<BluePrint_StructureBluePrint>( "/api/v1/BluePrint/StructureBluePrint:" + id + ":", data._as_map( false ) ) );
  }

  async BluePrint_StructureBluePrint_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/BluePrint/StructureBluePrint:" + id + ":" );
  }

  _BluePrint_StructureBluePrint_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async BluePrint_StructureBluePrint_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, BluePrint_StructureBluePrint>>
  {
    const [ filter_name, filter_value_map ] = this._BluePrint_StructureBluePrint_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<BluePrint_StructureBluePrint>( "/api/v1/BluePrint/StructureBluePrint", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new BluePrint_StructureBluePrint( this, response[ key ] );
    }

    return response;
  }

  async BluePrint_StructureBluePrint_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._BluePrint_StructureBluePrint_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/BluePrint/StructureBluePrint", filter_name, filter_value_map, position, count );
  }

  async BluePrint_StructureBluePrint_call_getConfig( id: string ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/BluePrint/StructureBluePrint:" + id + ":(getConfig)", {} );
    return ( _cinp_result_ as {} );
  }


  // Model StructureBluePrint (end)
  // Model Script at /api/v1/BluePrint/Script

  async BluePrint_Script_get( id: string ): Promise<BluePrint_Script>
  {
    const response = await this.cinp.getOne<BluePrint_Script>( "/api/v1/BluePrint/Script:" + id + ":" );

    return new BluePrint_Script( this, response );
  }

  async BluePrint_Script_create( data: BluePrint_Script ): Promise<void>
  {
    const response = await this.cinp.create<BluePrint_Script>( "/api/v1/BluePrint/Script", data._as_map( true ) );

    data._update( response.data );
  }

  async BluePrint_Script_update( id: string, data: BluePrint_Script ): Promise<void>
  {
    data._update( await this.cinp.updateOne<BluePrint_Script>( "/api/v1/BluePrint/Script:" + id + ":", data._as_map( false ) ) );
  }

  async BluePrint_Script_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/BluePrint/Script:" + id + ":" );
  }

  _BluePrint_Script_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async BluePrint_Script_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, BluePrint_Script>>
  {
    const [ filter_name, filter_value_map ] = this._BluePrint_Script_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<BluePrint_Script>( "/api/v1/BluePrint/Script", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new BluePrint_Script( this, response[ key ] );
    }

    return response;
  }

  async BluePrint_Script_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._BluePrint_Script_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/BluePrint/Script", filter_name, filter_value_map, position, count );
  }

  // Model Script (end)
  // Model BluePrintScript at /api/v1/BluePrint/BluePrintScript

  async BluePrint_BluePrintScript_get( id: number ): Promise<BluePrint_BluePrintScript>
  {
    const response = await this.cinp.getOne<BluePrint_BluePrintScript>( "/api/v1/BluePrint/BluePrintScript:" + id + ":" );

    return new BluePrint_BluePrintScript( this, response );
  }

  async BluePrint_BluePrintScript_create( data: BluePrint_BluePrintScript ): Promise<void>
  {
    const response = await this.cinp.create<BluePrint_BluePrintScript>( "/api/v1/BluePrint/BluePrintScript", data._as_map( true ) );

    data._update( response.data );
  }

  async BluePrint_BluePrintScript_update( id: number, data: BluePrint_BluePrintScript ): Promise<void>
  {
    data._update( await this.cinp.updateOne<BluePrint_BluePrintScript>( "/api/v1/BluePrint/BluePrintScript:" + id + ":", data._as_map( false ) ) );
  }

  async BluePrint_BluePrintScript_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/BluePrint/BluePrintScript:" + id + ":" );
  }

  _BluePrint_BluePrintScript_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof BluePrint_BluePrintScript._ListFilter_blueprint )
      return [ 'blueprint', { blueprint: filter.blueprint.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async BluePrint_BluePrintScript_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, BluePrint_BluePrintScript>>
  {
    const [ filter_name, filter_value_map ] = this._BluePrint_BluePrintScript_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<BluePrint_BluePrintScript>( "/api/v1/BluePrint/BluePrintScript", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new BluePrint_BluePrintScript( this, response[ key ] );
    }

    return response;
  }

  async BluePrint_BluePrintScript_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._BluePrint_BluePrintScript_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/BluePrint/BluePrintScript", filter_name, filter_value_map, position, count );
  }

  // Model BluePrintScript (end)
  // Model PXE at /api/v1/BluePrint/PXE

  async BluePrint_PXE_get( id: string ): Promise<BluePrint_PXE>
  {
    const response = await this.cinp.getOne<BluePrint_PXE>( "/api/v1/BluePrint/PXE:" + id + ":" );

    return new BluePrint_PXE( this, response );
  }

  async BluePrint_PXE_create( data: BluePrint_PXE ): Promise<void>
  {
    const response = await this.cinp.create<BluePrint_PXE>( "/api/v1/BluePrint/PXE", data._as_map( true ) );

    data._update( response.data );
  }

  async BluePrint_PXE_update( id: string, data: BluePrint_PXE ): Promise<void>
  {
    data._update( await this.cinp.updateOne<BluePrint_PXE>( "/api/v1/BluePrint/PXE:" + id + ":", data._as_map( false ) ) );
  }

  async BluePrint_PXE_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/BluePrint/PXE:" + id + ":" );
  }

  _BluePrint_PXE_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async BluePrint_PXE_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, BluePrint_PXE>>
  {
    const [ filter_name, filter_value_map ] = this._BluePrint_PXE_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<BluePrint_PXE>( "/api/v1/BluePrint/PXE", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new BluePrint_PXE( this, response[ key ] );
    }

    return response;
  }

  async BluePrint_PXE_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._BluePrint_PXE_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/BluePrint/PXE", filter_name, filter_value_map, position, count );
  }

  // Model PXE (end)
  // Namespace BluePrint (end)
  // Namespace Site at /api/v1/Site/ version 0.1
/*

*/

  // Model Site at /api/v1/Site/Site

  async Site_Site_get( id: string ): Promise<Site_Site>
  {
    const response = await this.cinp.getOne<Site_Site>( "/api/v1/Site/Site:" + id + ":" );

    return new Site_Site( this, response );
  }

  async Site_Site_create( data: Site_Site ): Promise<void>
  {
    const response = await this.cinp.create<Site_Site>( "/api/v1/Site/Site", data._as_map( true ) );

    data._update( response.data );
  }

  async Site_Site_update( id: string, data: Site_Site ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Site_Site>( "/api/v1/Site/Site:" + id + ":", data._as_map( false ) ) );
  }

  async Site_Site_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Site/Site:" + id + ":" );
  }

  _Site_Site_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Site_Site_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Site_Site>>
  {
    const [ filter_name, filter_value_map ] = this._Site_Site_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Site_Site>( "/api/v1/Site/Site", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Site_Site( this, response[ key ] );
    }

    return response;
  }

  async Site_Site_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Site_Site_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Site/Site", filter_name, filter_value_map, position, count );
  }

  async Site_Site_call_getConfig( id: string ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Site/Site:" + id + ":(getConfig)", {} );
    return ( _cinp_result_ as {} );
  }


  async Site_Site_call_getDependencyMap( id: string ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Site/Site:" + id + ":(getDependencyMap)", {} );
    return ( _cinp_result_ as {} );
  }


  // Model Site (end)
  // Namespace Site (end)
  // Namespace Survey at /api/v1/Survey/ version 0.1
/*

*/

  // Model Plot at /api/v1/Survey/Plot

  async Survey_Plot_get( id: string ): Promise<Survey_Plot>
  {
    const response = await this.cinp.getOne<Survey_Plot>( "/api/v1/Survey/Plot:" + id + ":" );

    return new Survey_Plot( this, response );
  }

  async Survey_Plot_create( data: Survey_Plot ): Promise<void>
  {
    const response = await this.cinp.create<Survey_Plot>( "/api/v1/Survey/Plot", data._as_map( true ) );

    data._update( response.data );
  }

  async Survey_Plot_update( id: string, data: Survey_Plot ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Survey_Plot>( "/api/v1/Survey/Plot:" + id + ":", data._as_map( false ) ) );
  }

  async Survey_Plot_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Survey/Plot:" + id + ":" );
  }

  _Survey_Plot_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Survey_Plot_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Survey_Plot>>
  {
    const [ filter_name, filter_value_map ] = this._Survey_Plot_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Survey_Plot>( "/api/v1/Survey/Plot", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Survey_Plot( this, response[ key ] );
    }

    return response;
  }

  async Survey_Plot_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Survey_Plot_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Survey/Plot", filter_name, filter_value_map, position, count );
  }

  // Model Plot (end)
  // Model Cartographer at /api/v1/Survey/Cartographer

  async Survey_Cartographer_get( id: string ): Promise<Survey_Cartographer>
  {
    const response = await this.cinp.getOne<Survey_Cartographer>( "/api/v1/Survey/Cartographer:" + id + ":" );

    return new Survey_Cartographer( this, response );
  }

  async Survey_Cartographer_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Survey/Cartographer:" + id + ":" );
  }

  _Survey_Cartographer_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Survey_Cartographer_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Survey_Cartographer>>
  {
    const [ filter_name, filter_value_map ] = this._Survey_Cartographer_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Survey_Cartographer>( "/api/v1/Survey/Cartographer", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Survey_Cartographer( this, response[ key ] );
    }

    return response;
  }

  async Survey_Cartographer_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Survey_Cartographer_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Survey/Cartographer", filter_name, filter_value_map, position, count );
  }

  async Survey_Cartographer_call_assign( id: string ,  foundation: Building_Foundation ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Survey/Cartographer:" + id + ":(assign)", { "foundation": foundation.toURL() } );
    return;
  }


  async Survey_Cartographer_call_register( identifier: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Survey/Cartographer(register)", { "identifier": identifier } );
    return;
  }


  async Survey_Cartographer_call_lookup( id: string ,  info_map: {} ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Survey/Cartographer:" + id + ":(lookup)", { "info_map": info_map } );
    return ( _cinp_result_ as {} );
  }


  async Survey_Cartographer_call_setMessage( id: string ,  message: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Survey/Cartographer:" + id + ":(setMessage)", { "message": message } );
    return;
  }


  async Survey_Cartographer_call_done( id: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Survey/Cartographer:" + id + ":(done)", {} );
    return;
  }


  // Model Cartographer (end)
  // Namespace Survey (end)
  // Namespace Directory at /api/v1/Directory/ version 0.1
/*

*/

  // Model Zone at /api/v1/Directory/Zone

  async Directory_Zone_get( id: number ): Promise<Directory_Zone>
  {
    const response = await this.cinp.getOne<Directory_Zone>( "/api/v1/Directory/Zone:" + id + ":" );

    return new Directory_Zone( this, response );
  }

  async Directory_Zone_create( data: Directory_Zone ): Promise<void>
  {
    const response = await this.cinp.create<Directory_Zone>( "/api/v1/Directory/Zone", data._as_map( true ) );

    data._update( response.data );
  }

  async Directory_Zone_update( id: number, data: Directory_Zone ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Directory_Zone>( "/api/v1/Directory/Zone:" + id + ":", data._as_map( false ) ) );
  }

  async Directory_Zone_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Directory/Zone:" + id + ":" );
  }

  _Directory_Zone_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Directory_Zone_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Directory_Zone>>
  {
    const [ filter_name, filter_value_map ] = this._Directory_Zone_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Directory_Zone>( "/api/v1/Directory/Zone", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Directory_Zone( this, response[ key ] );
    }

    return response;
  }

  async Directory_Zone_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Directory_Zone_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Directory/Zone", filter_name, filter_value_map, position, count );
  }

  // Model Zone (end)
  // Model Entry at /api/v1/Directory/Entry

  async Directory_Entry_get( id: number ): Promise<Directory_Entry>
  {
    const response = await this.cinp.getOne<Directory_Entry>( "/api/v1/Directory/Entry:" + id + ":" );

    return new Directory_Entry( this, response );
  }

  async Directory_Entry_create( data: Directory_Entry ): Promise<void>
  {
    const response = await this.cinp.create<Directory_Entry>( "/api/v1/Directory/Entry", data._as_map( true ) );

    data._update( response.data );
  }

  async Directory_Entry_update( id: number, data: Directory_Entry ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Directory_Entry>( "/api/v1/Directory/Entry:" + id + ":", data._as_map( false ) ) );
  }

  async Directory_Entry_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Directory/Entry:" + id + ":" );
  }

  _Directory_Entry_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Directory_Entry._ListFilter_zone )
      return [ 'zone', { zone: filter.zone.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Directory_Entry_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Directory_Entry>>
  {
    const [ filter_name, filter_value_map ] = this._Directory_Entry_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Directory_Entry>( "/api/v1/Directory/Entry", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Directory_Entry( this, response[ key ] );
    }

    return response;
  }

  async Directory_Entry_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Directory_Entry_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Directory/Entry", filter_name, filter_value_map, position, count );
  }

  // Model Entry (end)
  // Namespace Directory (end)
  // Namespace Utilities at /api/v1/Utilities/ version 0.1
/*

*/

  // Model Networked at /api/v1/Utilities/Networked

  async Utilities_Networked_get( id: number ): Promise<Utilities_Networked>
  {
    const response = await this.cinp.getOne<Utilities_Networked>( "/api/v1/Utilities/Networked:" + id + ":" );

    return new Utilities_Networked( this, response );
  }

  async Utilities_Networked_create( data: Utilities_Networked ): Promise<void>
  {
    const response = await this.cinp.create<Utilities_Networked>( "/api/v1/Utilities/Networked", data._as_map( true ) );

    data._update( response.data );
  }

  async Utilities_Networked_update( id: number, data: Utilities_Networked ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Utilities_Networked>( "/api/v1/Utilities/Networked:" + id + ":", data._as_map( false ) ) );
  }

  async Utilities_Networked_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Utilities/Networked:" + id + ":" );
  }

  _Utilities_Networked_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Utilities_Networked_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Utilities_Networked>>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_Networked_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Utilities_Networked>( "/api/v1/Utilities/Networked", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Utilities_Networked( this, response[ key ] );
    }

    return response;
  }

  async Utilities_Networked_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_Networked_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Utilities/Networked", filter_name, filter_value_map, position, count );
  }

  // Model Networked (end)
  // Model AddressBlock at /api/v1/Utilities/AddressBlock

  async Utilities_AddressBlock_get( id: number ): Promise<Utilities_AddressBlock>
  {
    const response = await this.cinp.getOne<Utilities_AddressBlock>( "/api/v1/Utilities/AddressBlock:" + id + ":" );

    return new Utilities_AddressBlock( this, response );
  }

  async Utilities_AddressBlock_create( data: Utilities_AddressBlock ): Promise<void>
  {
    const response = await this.cinp.create<Utilities_AddressBlock>( "/api/v1/Utilities/AddressBlock", data._as_map( true ) );

    data._update( response.data );
  }

  async Utilities_AddressBlock_update( id: number, data: Utilities_AddressBlock ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Utilities_AddressBlock>( "/api/v1/Utilities/AddressBlock:" + id + ":", data._as_map( false ) ) );
  }

  async Utilities_AddressBlock_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Utilities/AddressBlock:" + id + ":" );
  }

  _Utilities_AddressBlock_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Utilities_AddressBlock._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Utilities_AddressBlock_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Utilities_AddressBlock>>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_AddressBlock_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Utilities_AddressBlock>( "/api/v1/Utilities/AddressBlock", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Utilities_AddressBlock( this, response[ key ] );
    }

    return response;
  }

  async Utilities_AddressBlock_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_AddressBlock_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Utilities/AddressBlock", filter_name, filter_value_map, position, count );
  }

  async Utilities_AddressBlock_call_nextAddress( id: number ,  networked: Utilities_Networked, interface_name: string, is_primary: boolean ): Promise<Utilities_Address>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Utilities/AddressBlock:" + id + ":(nextAddress)", { "networked": networked.toURL(), "interface_name": interface_name, "is_primary": is_primary } );
    return new Utilities_Address( this, _cinp_result_ as Utilities_Address );
  }


  async Utilities_AddressBlock_call_usage( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Utilities/AddressBlock:" + id + ":(usage)", {} );
    return ( _cinp_result_ as {} );
  }


  async Utilities_AddressBlock_call_getWithNameSite( site: Site_Site, name: string ): Promise<Utilities_AddressBlock>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Utilities/AddressBlock(getWithNameSite)", { "site": site.toURL(), "name": name } );
    return new Utilities_AddressBlock( this, _cinp_result_ as Utilities_AddressBlock );
  }


  // Model AddressBlock (end)
  // Model Network at /api/v1/Utilities/Network

  async Utilities_Network_get( id: number ): Promise<Utilities_Network>
  {
    const response = await this.cinp.getOne<Utilities_Network>( "/api/v1/Utilities/Network:" + id + ":" );

    return new Utilities_Network( this, response );
  }

  async Utilities_Network_create( data: Utilities_Network ): Promise<void>
  {
    const response = await this.cinp.create<Utilities_Network>( "/api/v1/Utilities/Network", data._as_map( true ) );

    data._update( response.data );
  }

  async Utilities_Network_update( id: number, data: Utilities_Network ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Utilities_Network>( "/api/v1/Utilities/Network:" + id + ":", data._as_map( false ) ) );
  }

  async Utilities_Network_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Utilities/Network:" + id + ":" );
  }

  _Utilities_Network_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Utilities_Network._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Utilities_Network_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Utilities_Network>>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_Network_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Utilities_Network>( "/api/v1/Utilities/Network", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Utilities_Network( this, response[ key ] );
    }

    return response;
  }

  async Utilities_Network_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_Network_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Utilities/Network", filter_name, filter_value_map, position, count );
  }

  async Utilities_Network_call_getWithNameSite( site: Site_Site, name: string ): Promise<Utilities_Network>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Utilities/Network(getWithNameSite)", { "site": site.toURL(), "name": name } );
    return new Utilities_Network( this, _cinp_result_ as Utilities_Network );
  }


  // Model Network (end)
  // Model NetworkAddressBlock at /api/v1/Utilities/NetworkAddressBlock

  async Utilities_NetworkAddressBlock_get( id: number ): Promise<Utilities_NetworkAddressBlock>
  {
    const response = await this.cinp.getOne<Utilities_NetworkAddressBlock>( "/api/v1/Utilities/NetworkAddressBlock:" + id + ":" );

    return new Utilities_NetworkAddressBlock( this, response );
  }

  async Utilities_NetworkAddressBlock_create( data: Utilities_NetworkAddressBlock ): Promise<void>
  {
    const response = await this.cinp.create<Utilities_NetworkAddressBlock>( "/api/v1/Utilities/NetworkAddressBlock", data._as_map( true ) );

    data._update( response.data );
  }

  async Utilities_NetworkAddressBlock_update( id: number, data: Utilities_NetworkAddressBlock ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Utilities_NetworkAddressBlock>( "/api/v1/Utilities/NetworkAddressBlock:" + id + ":", data._as_map( false ) ) );
  }

  async Utilities_NetworkAddressBlock_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Utilities/NetworkAddressBlock:" + id + ":" );
  }

  _Utilities_NetworkAddressBlock_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Utilities_NetworkAddressBlock._ListFilter_network )
      return [ 'network', { network: filter.network.toURL(),  } ];

    if( filter instanceof Utilities_NetworkAddressBlock._ListFilter_address_block )
      return [ 'address_block', { address_block: filter.address_block.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Utilities_NetworkAddressBlock_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Utilities_NetworkAddressBlock>>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_NetworkAddressBlock_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Utilities_NetworkAddressBlock>( "/api/v1/Utilities/NetworkAddressBlock", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Utilities_NetworkAddressBlock( this, response[ key ] );
    }

    return response;
  }

  async Utilities_NetworkAddressBlock_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_NetworkAddressBlock_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Utilities/NetworkAddressBlock", filter_name, filter_value_map, position, count );
  }

  // Model NetworkAddressBlock (end)
  // Model NetworkInterface at /api/v1/Utilities/NetworkInterface

  // Model NetworkInterface (end)
  // Model RealNetworkInterface at /api/v1/Utilities/RealNetworkInterface

  async Utilities_RealNetworkInterface_get( id: number ): Promise<Utilities_RealNetworkInterface>
  {
    const response = await this.cinp.getOne<Utilities_RealNetworkInterface>( "/api/v1/Utilities/RealNetworkInterface:" + id + ":" );

    return new Utilities_RealNetworkInterface( this, response );
  }

  async Utilities_RealNetworkInterface_create( data: Utilities_RealNetworkInterface ): Promise<void>
  {
    const response = await this.cinp.create<Utilities_RealNetworkInterface>( "/api/v1/Utilities/RealNetworkInterface", data._as_map( true ) );

    data._update( response.data );
  }

  async Utilities_RealNetworkInterface_update( id: number, data: Utilities_RealNetworkInterface ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Utilities_RealNetworkInterface>( "/api/v1/Utilities/RealNetworkInterface:" + id + ":", data._as_map( false ) ) );
  }

  async Utilities_RealNetworkInterface_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Utilities/RealNetworkInterface:" + id + ":" );
  }

  _Utilities_RealNetworkInterface_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Utilities_RealNetworkInterface._ListFilter_foundation )
      return [ 'foundation', { foundation: filter.foundation.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Utilities_RealNetworkInterface_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Utilities_RealNetworkInterface>>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_RealNetworkInterface_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Utilities_RealNetworkInterface>( "/api/v1/Utilities/RealNetworkInterface", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Utilities_RealNetworkInterface( this, response[ key ] );
    }

    return response;
  }

  async Utilities_RealNetworkInterface_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_RealNetworkInterface_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Utilities/RealNetworkInterface", filter_name, filter_value_map, position, count );
  }

  // Model RealNetworkInterface (end)
  // Model AbstractNetworkInterface at /api/v1/Utilities/AbstractNetworkInterface

  async Utilities_AbstractNetworkInterface_get( id: number ): Promise<Utilities_AbstractNetworkInterface>
  {
    const response = await this.cinp.getOne<Utilities_AbstractNetworkInterface>( "/api/v1/Utilities/AbstractNetworkInterface:" + id + ":" );

    return new Utilities_AbstractNetworkInterface( this, response );
  }

  async Utilities_AbstractNetworkInterface_create( data: Utilities_AbstractNetworkInterface ): Promise<void>
  {
    const response = await this.cinp.create<Utilities_AbstractNetworkInterface>( "/api/v1/Utilities/AbstractNetworkInterface", data._as_map( true ) );

    data._update( response.data );
  }

  async Utilities_AbstractNetworkInterface_update( id: number, data: Utilities_AbstractNetworkInterface ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Utilities_AbstractNetworkInterface>( "/api/v1/Utilities/AbstractNetworkInterface:" + id + ":", data._as_map( false ) ) );
  }

  async Utilities_AbstractNetworkInterface_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Utilities/AbstractNetworkInterface:" + id + ":" );
  }

  _Utilities_AbstractNetworkInterface_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Utilities_AbstractNetworkInterface._ListFilter_structure )
      return [ 'structure', { structure: filter.structure.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Utilities_AbstractNetworkInterface_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Utilities_AbstractNetworkInterface>>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_AbstractNetworkInterface_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Utilities_AbstractNetworkInterface>( "/api/v1/Utilities/AbstractNetworkInterface", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Utilities_AbstractNetworkInterface( this, response[ key ] );
    }

    return response;
  }

  async Utilities_AbstractNetworkInterface_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_AbstractNetworkInterface_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Utilities/AbstractNetworkInterface", filter_name, filter_value_map, position, count );
  }

  // Model AbstractNetworkInterface (end)
  // Model AggregatedNetworkInterface at /api/v1/Utilities/AggregatedNetworkInterface

  async Utilities_AggregatedNetworkInterface_get( id: Utilities_NetworkInterface ): Promise<Utilities_AggregatedNetworkInterface>
  {
    const response = await this.cinp.getOne<Utilities_AggregatedNetworkInterface>( "/api/v1/Utilities/AggregatedNetworkInterface:" + id + ":" );

    return new Utilities_AggregatedNetworkInterface( this, response );
  }

  async Utilities_AggregatedNetworkInterface_create( data: Utilities_AggregatedNetworkInterface ): Promise<void>
  {
    const response = await this.cinp.create<Utilities_AggregatedNetworkInterface>( "/api/v1/Utilities/AggregatedNetworkInterface", data._as_map( true ) );

    data._update( response.data );
  }

  async Utilities_AggregatedNetworkInterface_update( id: Utilities_NetworkInterface, data: Utilities_AggregatedNetworkInterface ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Utilities_AggregatedNetworkInterface>( "/api/v1/Utilities/AggregatedNetworkInterface:" + id + ":", data._as_map( false ) ) );
  }

  async Utilities_AggregatedNetworkInterface_delete( id: Utilities_NetworkInterface ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Utilities/AggregatedNetworkInterface:" + id + ":" );
  }

  _Utilities_AggregatedNetworkInterface_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Utilities_AggregatedNetworkInterface._ListFilter_structure )
      return [ 'structure', { structure: filter.structure.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Utilities_AggregatedNetworkInterface_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Utilities_AggregatedNetworkInterface>>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_AggregatedNetworkInterface_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Utilities_AggregatedNetworkInterface>( "/api/v1/Utilities/AggregatedNetworkInterface", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Utilities_AggregatedNetworkInterface( this, response[ key ] );
    }

    return response;
  }

  async Utilities_AggregatedNetworkInterface_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_AggregatedNetworkInterface_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Utilities/AggregatedNetworkInterface", filter_name, filter_value_map, position, count );
  }

  // Model AggregatedNetworkInterface (end)
  // Model BaseAddress at /api/v1/Utilities/BaseAddress

  async Utilities_BaseAddress_call_lookup( ip_address: string, site: Site_Site ): Promise<Utilities_BaseAddress>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Utilities/BaseAddress(lookup)", { "ip_address": ip_address, "site": site.toURL() } );
    return new Utilities_BaseAddress( this, _cinp_result_ as Utilities_BaseAddress );
  }


  // Model BaseAddress (end)
  // Model Address at /api/v1/Utilities/Address

  async Utilities_Address_get( id: number ): Promise<Utilities_Address>
  {
    const response = await this.cinp.getOne<Utilities_Address>( "/api/v1/Utilities/Address:" + id + ":" );

    return new Utilities_Address( this, response );
  }

  async Utilities_Address_create( data: Utilities_Address ): Promise<void>
  {
    const response = await this.cinp.create<Utilities_Address>( "/api/v1/Utilities/Address", data._as_map( true ) );

    data._update( response.data );
  }

  async Utilities_Address_update( id: number, data: Utilities_Address ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Utilities_Address>( "/api/v1/Utilities/Address:" + id + ":", data._as_map( false ) ) );
  }

  async Utilities_Address_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Utilities/Address:" + id + ":" );
  }

  _Utilities_Address_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Utilities_Address._ListFilter_address_block )
      return [ 'address_block', { address_block: filter.address_block.toURL(),  } ];

    if( filter instanceof Utilities_Address._ListFilter_structure )
      return [ 'structure', { structure: filter.structure.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Utilities_Address_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Utilities_Address>>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_Address_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Utilities_Address>( "/api/v1/Utilities/Address", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Utilities_Address( this, response[ key ] );
    }

    return response;
  }

  async Utilities_Address_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_Address_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Utilities/Address", filter_name, filter_value_map, position, count );
  }

  // Model Address (end)
  // Model ReservedAddress at /api/v1/Utilities/ReservedAddress

  async Utilities_ReservedAddress_get( id: number ): Promise<Utilities_ReservedAddress>
  {
    const response = await this.cinp.getOne<Utilities_ReservedAddress>( "/api/v1/Utilities/ReservedAddress:" + id + ":" );

    return new Utilities_ReservedAddress( this, response );
  }

  async Utilities_ReservedAddress_create( data: Utilities_ReservedAddress ): Promise<void>
  {
    const response = await this.cinp.create<Utilities_ReservedAddress>( "/api/v1/Utilities/ReservedAddress", data._as_map( true ) );

    data._update( response.data );
  }

  async Utilities_ReservedAddress_update( id: number, data: Utilities_ReservedAddress ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Utilities_ReservedAddress>( "/api/v1/Utilities/ReservedAddress:" + id + ":", data._as_map( false ) ) );
  }

  async Utilities_ReservedAddress_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Utilities/ReservedAddress:" + id + ":" );
  }

  _Utilities_ReservedAddress_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Utilities_ReservedAddress._ListFilter_address_block )
      return [ 'address_block', { address_block: filter.address_block.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Utilities_ReservedAddress_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Utilities_ReservedAddress>>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_ReservedAddress_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Utilities_ReservedAddress>( "/api/v1/Utilities/ReservedAddress", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Utilities_ReservedAddress( this, response[ key ] );
    }

    return response;
  }

  async Utilities_ReservedAddress_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_ReservedAddress_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Utilities/ReservedAddress", filter_name, filter_value_map, position, count );
  }

  // Model ReservedAddress (end)
  // Model DynamicAddress at /api/v1/Utilities/DynamicAddress

  async Utilities_DynamicAddress_get( id: number ): Promise<Utilities_DynamicAddress>
  {
    const response = await this.cinp.getOne<Utilities_DynamicAddress>( "/api/v1/Utilities/DynamicAddress:" + id + ":" );

    return new Utilities_DynamicAddress( this, response );
  }

  async Utilities_DynamicAddress_create( data: Utilities_DynamicAddress ): Promise<void>
  {
    const response = await this.cinp.create<Utilities_DynamicAddress>( "/api/v1/Utilities/DynamicAddress", data._as_map( true ) );

    data._update( response.data );
  }

  async Utilities_DynamicAddress_update( id: number, data: Utilities_DynamicAddress ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Utilities_DynamicAddress>( "/api/v1/Utilities/DynamicAddress:" + id + ":", data._as_map( false ) ) );
  }

  async Utilities_DynamicAddress_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Utilities/DynamicAddress:" + id + ":" );
  }

  _Utilities_DynamicAddress_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Utilities_DynamicAddress._ListFilter_address_block )
      return [ 'address_block', { address_block: filter.address_block.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Utilities_DynamicAddress_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Utilities_DynamicAddress>>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_DynamicAddress_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Utilities_DynamicAddress>( "/api/v1/Utilities/DynamicAddress", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Utilities_DynamicAddress( this, response[ key ] );
    }

    return response;
  }

  async Utilities_DynamicAddress_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Utilities_DynamicAddress_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Utilities/DynamicAddress", filter_name, filter_value_map, position, count );
  }

  // Model DynamicAddress (end)
  // Namespace Utilities (end)
  // Namespace Building at /api/v1/Building/ version 0.1
/*

*/

  // Model Foundation at /api/v1/Building/Foundation

  async Building_Foundation_get( id: string ): Promise<Building_Foundation>
  {
    const response = await this.cinp.getOne<Building_Foundation>( "/api/v1/Building/Foundation:" + id + ":" );

    return new Building_Foundation( this, response );
  }

  async Building_Foundation_update( id: string, data: Building_Foundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Building_Foundation>( "/api/v1/Building/Foundation:" + id + ":", data._as_map( false ) ) );
  }

  async Building_Foundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Building/Foundation:" + id + ":" );
  }

  _Building_Foundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Building_Foundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    if( filter instanceof Building_Foundation._ListFilter_todo )
      return [ 'todo', { site: filter.site.toURL(), has_dependancies: filter.has_dependancies, foundation_class: filter.foundation_class,  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Building_Foundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Building_Foundation>>
  {
    const [ filter_name, filter_value_map ] = this._Building_Foundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Building_Foundation>( "/api/v1/Building/Foundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Building_Foundation( this, response[ key ] );
    }

    return response;
  }

  async Building_Foundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Building_Foundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Building/Foundation", filter_name, filter_value_map, position, count );
  }

  async Building_Foundation_call_setIdMap( id: string ,  id_map: {} ): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Foundation:" + id + ":(setIdMap)", { "id_map": id_map } );
    return ( _cinp_result_ as string );
  }


  async Building_Foundation_call_doCreate( id: string ): Promise<number>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Foundation:" + id + ":(doCreate)", {} );
    return ( _cinp_result_ as number );
  }


  async Building_Foundation_call_doDestroy( id: string ): Promise<number>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Foundation:" + id + ":(doDestroy)", {} );
    return ( _cinp_result_ as number );
  }


  async Building_Foundation_call_doJob( id: string ,  name: string ): Promise<number>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Foundation:" + id + ":(doJob)", { "name": name } );
    return ( _cinp_result_ as number );
  }


  async Building_Foundation_call_getJob( id: string ): Promise<Foreman_FoundationJob>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Foundation:" + id + ":(getJob)", {} );
    return new Foreman_FoundationJob( this, _cinp_result_ as Foreman_FoundationJob );
  }


  async Building_Foundation_call_getFoundationTypes(): Promise<string[]>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Foundation(getFoundationTypes)", {} );
    return ( _cinp_result_ as string[] );
  }


  async Building_Foundation_call_getConfig( id: string ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Foundation:" + id + ":(getConfig)", {} );
    return ( _cinp_result_ as {} );
  }


  async Building_Foundation_call_getInterfaceList( id: string ): Promise<{}[]>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Foundation:" + id + ":(getInterfaceList)", {} );
    return ( _cinp_result_ as {}[] );
  }


  // Model Foundation (end)
  // Model Structure at /api/v1/Building/Structure

  async Building_Structure_get( id: number ): Promise<Building_Structure>
  {
    const response = await this.cinp.getOne<Building_Structure>( "/api/v1/Building/Structure:" + id + ":" );

    return new Building_Structure( this, response );
  }

  async Building_Structure_create( data: Building_Structure ): Promise<void>
  {
    const response = await this.cinp.create<Building_Structure>( "/api/v1/Building/Structure", data._as_map( true ) );

    data._update( response.data );
  }

  async Building_Structure_update( id: number, data: Building_Structure ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Building_Structure>( "/api/v1/Building/Structure:" + id + ":", data._as_map( false ) ) );
  }

  async Building_Structure_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Building/Structure:" + id + ":" );
  }

  _Building_Structure_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Building_Structure._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Building_Structure_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Building_Structure>>
  {
    const [ filter_name, filter_value_map ] = this._Building_Structure_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Building_Structure>( "/api/v1/Building/Structure", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Building_Structure( this, response[ key ] );
    }

    return response;
  }

  async Building_Structure_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Building_Structure_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Building/Structure", filter_name, filter_value_map, position, count );
  }

  async Building_Structure_call_doCreate( id: number ): Promise<number>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Structure:" + id + ":(doCreate)", {} );
    return ( _cinp_result_ as number );
  }


  async Building_Structure_call_doDestroy( id: number ): Promise<number>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Structure:" + id + ":(doDestroy)", {} );
    return ( _cinp_result_ as number );
  }


  async Building_Structure_call_doJob( id: number ,  name: string ): Promise<number>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Structure:" + id + ":(doJob)", { "name": name } );
    return ( _cinp_result_ as number );
  }


  async Building_Structure_call_getJob( id: number ): Promise<Foreman_StructureJob>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Structure:" + id + ":(getJob)", {} );
    return new Foreman_StructureJob( this, _cinp_result_ as Foreman_StructureJob );
  }


  async Building_Structure_call_getConfig( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Structure:" + id + ":(getConfig)", {} );
    return ( _cinp_result_ as {} );
  }


  async Building_Structure_call_updateConfig( id: number ,  config_value_map: {} ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Structure:" + id + ":(updateConfig)", { "config_value_map": config_value_map } );
    return ( _cinp_result_ as {} );
  }


  async Building_Structure_call_getWithHostnameSite( site: Site_Site, hostname: string ): Promise<Building_Structure>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Structure(getWithHostnameSite)", { "site": site.toURL(), "hostname": hostname } );
    return new Building_Structure( this, _cinp_result_ as Building_Structure );
  }


  // Model Structure (end)
  // Model Complex at /api/v1/Building/Complex

  async Building_Complex_get( id: string ): Promise<Building_Complex>
  {
    const response = await this.cinp.getOne<Building_Complex>( "/api/v1/Building/Complex:" + id + ":" );

    return new Building_Complex( this, response );
  }

  async Building_Complex_create( data: Building_Complex ): Promise<void>
  {
    const response = await this.cinp.create<Building_Complex>( "/api/v1/Building/Complex", data._as_map( true ) );

    data._update( response.data );
  }

  async Building_Complex_update( id: string, data: Building_Complex ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Building_Complex>( "/api/v1/Building/Complex:" + id + ":", data._as_map( false ) ) );
  }

  async Building_Complex_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Building/Complex:" + id + ":" );
  }

  _Building_Complex_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Building_Complex._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Building_Complex_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Building_Complex>>
  {
    const [ filter_name, filter_value_map ] = this._Building_Complex_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Building_Complex>( "/api/v1/Building/Complex", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Building_Complex( this, response[ key ] );
    }

    return response;
  }

  async Building_Complex_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Building_Complex_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Building/Complex", filter_name, filter_value_map, position, count );
  }

  async Building_Complex_call_createFoundation( id: string ,  hostname: string, site: Site_Site, interface_map_list: {}[] ): Promise<Building_Foundation>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Complex:" + id + ":(createFoundation)", { "hostname": hostname, "site": site.toURL(), "interface_map_list": interface_map_list } );
    return new Building_Foundation( this, _cinp_result_ as Building_Foundation );
  }


  // Model Complex (end)
  // Model ComplexStructure at /api/v1/Building/ComplexStructure

  async Building_ComplexStructure_get( id: number ): Promise<Building_ComplexStructure>
  {
    const response = await this.cinp.getOne<Building_ComplexStructure>( "/api/v1/Building/ComplexStructure:" + id + ":" );

    return new Building_ComplexStructure( this, response );
  }

  async Building_ComplexStructure_create( data: Building_ComplexStructure ): Promise<void>
  {
    const response = await this.cinp.create<Building_ComplexStructure>( "/api/v1/Building/ComplexStructure", data._as_map( true ) );

    data._update( response.data );
  }

  async Building_ComplexStructure_update( id: number, data: Building_ComplexStructure ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Building_ComplexStructure>( "/api/v1/Building/ComplexStructure:" + id + ":", data._as_map( false ) ) );
  }

  async Building_ComplexStructure_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Building/ComplexStructure:" + id + ":" );
  }

  _Building_ComplexStructure_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Building_ComplexStructure._ListFilter_complex )
      return [ 'complex', { complex: filter.complex.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Building_ComplexStructure_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Building_ComplexStructure>>
  {
    const [ filter_name, filter_value_map ] = this._Building_ComplexStructure_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Building_ComplexStructure>( "/api/v1/Building/ComplexStructure", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Building_ComplexStructure( this, response[ key ] );
    }

    return response;
  }

  async Building_ComplexStructure_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Building_ComplexStructure_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Building/ComplexStructure", filter_name, filter_value_map, position, count );
  }

  async Building_ComplexStructure_call_getConfig( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/ComplexStructure:" + id + ":(getConfig)", {} );
    return ( _cinp_result_ as {} );
  }


  // Model ComplexStructure (end)
  // Model Dependency at /api/v1/Building/Dependency

  async Building_Dependency_get( id: number ): Promise<Building_Dependency>
  {
    const response = await this.cinp.getOne<Building_Dependency>( "/api/v1/Building/Dependency:" + id + ":" );

    return new Building_Dependency( this, response );
  }

  async Building_Dependency_create( data: Building_Dependency ): Promise<void>
  {
    const response = await this.cinp.create<Building_Dependency>( "/api/v1/Building/Dependency", data._as_map( true ) );

    data._update( response.data );
  }

  async Building_Dependency_update( id: number, data: Building_Dependency ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Building_Dependency>( "/api/v1/Building/Dependency:" + id + ":", data._as_map( false ) ) );
  }

  async Building_Dependency_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Building/Dependency:" + id + ":" );
  }

  _Building_Dependency_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Building_Dependency._ListFilter_foundation )
      return [ 'foundation', { foundation: filter.foundation.toURL(),  } ];

    if( filter instanceof Building_Dependency._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Building_Dependency_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Building_Dependency>>
  {
    const [ filter_name, filter_value_map ] = this._Building_Dependency_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Building_Dependency>( "/api/v1/Building/Dependency", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Building_Dependency( this, response[ key ] );
    }

    return response;
  }

  async Building_Dependency_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Building_Dependency_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Building/Dependency", filter_name, filter_value_map, position, count );
  }

  async Building_Dependency_call_getJob( id: number ): Promise<Foreman_DependencyJob>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Building/Dependency:" + id + ":(getJob)", {} );
    return new Foreman_DependencyJob( this, _cinp_result_ as Foreman_DependencyJob );
  }


  // Model Dependency (end)
  // Namespace Building (end)
  // Namespace Foreman at /api/v1/Foreman/ version 0.1
/*

*/

  // Model BaseJob at /api/v1/Foreman/BaseJob

  async Foreman_BaseJob_call_pause( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/BaseJob:" + id + ":(pause)", {} );
    return;
  }


  async Foreman_BaseJob_call_resume( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/BaseJob:" + id + ":(resume)", {} );
    return;
  }


  async Foreman_BaseJob_call_reset( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/BaseJob:" + id + ":(reset)", {} );
    return;
  }


  async Foreman_BaseJob_call_rollback( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/BaseJob:" + id + ":(rollback)", {} );
    return;
  }


  async Foreman_BaseJob_call_clearDispatched( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/BaseJob:" + id + ":(clearDispatched)", {} );
    return;
  }


  async Foreman_BaseJob_call_jobStats( site: Site_Site ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/BaseJob(jobStats)", { "site": site.toURL() } );
    return ( _cinp_result_ as {} );
  }


  async Foreman_BaseJob_call_jobRunnerVariables( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/BaseJob:" + id + ":(jobRunnerVariables)", {} );
    return ( _cinp_result_ as {} );
  }


  async Foreman_BaseJob_call_jobRunnerState( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/BaseJob:" + id + ":(jobRunnerState)", {} );
    return ( _cinp_result_ as {} );
  }


  async Foreman_BaseJob_call_signalComplete( id: number ,  cookie: string ): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/BaseJob:" + id + ":(signalComplete)", { "cookie": cookie } );
    return ( _cinp_result_ as string );
  }


  async Foreman_BaseJob_call_signalAlert( id: number ,  msg: string ): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/BaseJob:" + id + ":(signalAlert)", { "msg": msg } );
    return ( _cinp_result_ as string );
  }


  async Foreman_BaseJob_call_postMessage( id: number ,  msg: string ): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/BaseJob:" + id + ":(postMessage)", { "msg": msg } );
    return ( _cinp_result_ as string );
  }


  // Model BaseJob (end)
  // Model FoundationJob at /api/v1/Foreman/FoundationJob

  async Foreman_FoundationJob_get( id: number ): Promise<Foreman_FoundationJob>
  {
    const response = await this.cinp.getOne<Foreman_FoundationJob>( "/api/v1/Foreman/FoundationJob:" + id + ":" );

    return new Foreman_FoundationJob( this, response );
  }

  _Foreman_FoundationJob_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Foreman_FoundationJob._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Foreman_FoundationJob_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Foreman_FoundationJob>>
  {
    const [ filter_name, filter_value_map ] = this._Foreman_FoundationJob_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Foreman_FoundationJob>( "/api/v1/Foreman/FoundationJob", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Foreman_FoundationJob( this, response[ key ] );
    }

    return response;
  }

  async Foreman_FoundationJob_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Foreman_FoundationJob_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Foreman/FoundationJob", filter_name, filter_value_map, position, count );
  }

  async Foreman_FoundationJob_call_pause( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/FoundationJob:" + id + ":(pause)", {} );
    return;
  }


  async Foreman_FoundationJob_call_resume( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/FoundationJob:" + id + ":(resume)", {} );
    return;
  }


  async Foreman_FoundationJob_call_reset( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/FoundationJob:" + id + ":(reset)", {} );
    return;
  }


  async Foreman_FoundationJob_call_rollback( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/FoundationJob:" + id + ":(rollback)", {} );
    return;
  }


  async Foreman_FoundationJob_call_jobRunnerVariables( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/FoundationJob:" + id + ":(jobRunnerVariables)", {} );
    return ( _cinp_result_ as {} );
  }


  async Foreman_FoundationJob_call_jobRunnerState( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/FoundationJob:" + id + ":(jobRunnerState)", {} );
    return ( _cinp_result_ as {} );
  }


  // Model FoundationJob (end)
  // Model StructureJob at /api/v1/Foreman/StructureJob

  async Foreman_StructureJob_get( id: number ): Promise<Foreman_StructureJob>
  {
    const response = await this.cinp.getOne<Foreman_StructureJob>( "/api/v1/Foreman/StructureJob:" + id + ":" );

    return new Foreman_StructureJob( this, response );
  }

  _Foreman_StructureJob_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Foreman_StructureJob._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Foreman_StructureJob_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Foreman_StructureJob>>
  {
    const [ filter_name, filter_value_map ] = this._Foreman_StructureJob_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Foreman_StructureJob>( "/api/v1/Foreman/StructureJob", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Foreman_StructureJob( this, response[ key ] );
    }

    return response;
  }

  async Foreman_StructureJob_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Foreman_StructureJob_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Foreman/StructureJob", filter_name, filter_value_map, position, count );
  }

  async Foreman_StructureJob_call_pause( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/StructureJob:" + id + ":(pause)", {} );
    return;
  }


  async Foreman_StructureJob_call_resume( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/StructureJob:" + id + ":(resume)", {} );
    return;
  }


  async Foreman_StructureJob_call_reset( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/StructureJob:" + id + ":(reset)", {} );
    return;
  }


  async Foreman_StructureJob_call_rollback( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/StructureJob:" + id + ":(rollback)", {} );
    return;
  }


  async Foreman_StructureJob_call_jobRunnerVariables( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/StructureJob:" + id + ":(jobRunnerVariables)", {} );
    return ( _cinp_result_ as {} );
  }


  async Foreman_StructureJob_call_jobRunnerState( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/StructureJob:" + id + ":(jobRunnerState)", {} );
    return ( _cinp_result_ as {} );
  }


  // Model StructureJob (end)
  // Model DependencyJob at /api/v1/Foreman/DependencyJob

  async Foreman_DependencyJob_get( id: number ): Promise<Foreman_DependencyJob>
  {
    const response = await this.cinp.getOne<Foreman_DependencyJob>( "/api/v1/Foreman/DependencyJob:" + id + ":" );

    return new Foreman_DependencyJob( this, response );
  }

  _Foreman_DependencyJob_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Foreman_DependencyJob._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Foreman_DependencyJob_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Foreman_DependencyJob>>
  {
    const [ filter_name, filter_value_map ] = this._Foreman_DependencyJob_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Foreman_DependencyJob>( "/api/v1/Foreman/DependencyJob", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Foreman_DependencyJob( this, response[ key ] );
    }

    return response;
  }

  async Foreman_DependencyJob_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Foreman_DependencyJob_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Foreman/DependencyJob", filter_name, filter_value_map, position, count );
  }

  async Foreman_DependencyJob_call_pause( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/DependencyJob:" + id + ":(pause)", {} );
    return;
  }


  async Foreman_DependencyJob_call_resume( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/DependencyJob:" + id + ":(resume)", {} );
    return;
  }


  async Foreman_DependencyJob_call_reset( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/DependencyJob:" + id + ":(reset)", {} );
    return;
  }


  async Foreman_DependencyJob_call_rollback( id: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/Foreman/DependencyJob:" + id + ":(rollback)", {} );
    return;
  }


  async Foreman_DependencyJob_call_jobRunnerVariables( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/DependencyJob:" + id + ":(jobRunnerVariables)", {} );
    return ( _cinp_result_ as {} );
  }


  async Foreman_DependencyJob_call_jobRunnerState( id: number ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Foreman/DependencyJob:" + id + ":(jobRunnerState)", {} );
    return ( _cinp_result_ as {} );
  }


  // Model DependencyJob (end)
  // Model JobLog at /api/v1/Foreman/JobLog

  async Foreman_JobLog_get( id: number ): Promise<Foreman_JobLog>
  {
    const response = await this.cinp.getOne<Foreman_JobLog>( "/api/v1/Foreman/JobLog:" + id + ":" );

    return new Foreman_JobLog( this, response );
  }

  _Foreman_JobLog_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Foreman_JobLog._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    if( filter instanceof Foreman_JobLog._ListFilter_job )
      return [ 'job', { job_id: filter.job_id,  } ];

    if( filter instanceof Foreman_JobLog._ListFilter_structure )
      return [ 'structure', { structure: filter.structure.toURL(),  } ];

    if( filter instanceof Foreman_JobLog._ListFilter_foundation )
      return [ 'foundation', { foundation: filter.foundation.toURL(),  } ];

    if( filter instanceof Foreman_JobLog._ListFilter_dependency )
      return [ 'dependency', { dependency: filter.dependency.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Foreman_JobLog_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Foreman_JobLog>>
  {
    const [ filter_name, filter_value_map ] = this._Foreman_JobLog_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Foreman_JobLog>( "/api/v1/Foreman/JobLog", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Foreman_JobLog( this, response[ key ] );
    }

    return response;
  }

  async Foreman_JobLog_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Foreman_JobLog_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Foreman/JobLog", filter_name, filter_value_map, position, count );
  }

  // Model JobLog (end)
  // Namespace Foreman (end)
  // Namespace SubContractor at /api/v1/SubContractor/ version 0.1
/*

*/

  // Model Dispatch at /api/v1/SubContractor/Dispatch

  async SubContractor_Dispatch_call_getJobs( site: Site_Site, module_list: string[], max_jobs: number ): Promise<{}[]>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/SubContractor/Dispatch(getJobs)", { "site": site.toURL(), "module_list": module_list, "max_jobs": max_jobs } );
    return ( _cinp_result_ as {}[] );
  }


  async SubContractor_Dispatch_call_jobResults( job_id: number, cookie: string, data: {} ): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/SubContractor/Dispatch(jobResults)", { "job_id": job_id, "cookie": cookie, "data": data } );
    return ( _cinp_result_ as string );
  }


  async SubContractor_Dispatch_call_jobError( job_id: number, cookie: string, msg: string ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/SubContractor/Dispatch(jobError)", { "job_id": job_id, "cookie": cookie, "msg": msg } );
    return;
  }


  // Model Dispatch (end)
  // Model DHCPd at /api/v1/SubContractor/DHCPd

  async SubContractor_DHCPd_call_getDynamicPools( site: Site_Site ): Promise<{}[]>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/SubContractor/DHCPd(getDynamicPools)", { "site": site.toURL() } );
    return ( _cinp_result_ as {}[] );
  }


  async SubContractor_DHCPd_call_getStaticPools( site: Site_Site ): Promise<{}>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/SubContractor/DHCPd(getStaticPools)", { "site": site.toURL() } );
    return ( _cinp_result_ as {} );
  }


  // Model DHCPd (end)
  // Namespace SubContractor (end)
  // Namespace PostOffice at /api/v1/PostOffice/ version 0.1
/*

*/

  // Model FoundationPost at /api/v1/PostOffice/FoundationPost

  async PostOffice_FoundationPost_get( id: number ): Promise<PostOffice_FoundationPost>
  {
    const response = await this.cinp.getOne<PostOffice_FoundationPost>( "/api/v1/PostOffice/FoundationPost:" + id + ":" );

    return new PostOffice_FoundationPost( this, response );
  }

  _PostOffice_FoundationPost_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async PostOffice_FoundationPost_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, PostOffice_FoundationPost>>
  {
    const [ filter_name, filter_value_map ] = this._PostOffice_FoundationPost_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<PostOffice_FoundationPost>( "/api/v1/PostOffice/FoundationPost", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new PostOffice_FoundationPost( this, response[ key ] );
    }

    return response;
  }

  async PostOffice_FoundationPost_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._PostOffice_FoundationPost_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/PostOffice/FoundationPost", filter_name, filter_value_map, position, count );
  }

  // Model FoundationPost (end)
  // Model StructurePost at /api/v1/PostOffice/StructurePost

  async PostOffice_StructurePost_get( id: number ): Promise<PostOffice_StructurePost>
  {
    const response = await this.cinp.getOne<PostOffice_StructurePost>( "/api/v1/PostOffice/StructurePost:" + id + ":" );

    return new PostOffice_StructurePost( this, response );
  }

  _PostOffice_StructurePost_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async PostOffice_StructurePost_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, PostOffice_StructurePost>>
  {
    const [ filter_name, filter_value_map ] = this._PostOffice_StructurePost_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<PostOffice_StructurePost>( "/api/v1/PostOffice/StructurePost", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new PostOffice_StructurePost( this, response[ key ] );
    }

    return response;
  }

  async PostOffice_StructurePost_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._PostOffice_StructurePost_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/PostOffice/StructurePost", filter_name, filter_value_map, position, count );
  }

  // Model StructurePost (end)
  // Model FoundationBox at /api/v1/PostOffice/FoundationBox

  async PostOffice_FoundationBox_get( id: number ): Promise<PostOffice_FoundationBox>
  {
    const response = await this.cinp.getOne<PostOffice_FoundationBox>( "/api/v1/PostOffice/FoundationBox:" + id + ":" );

    return new PostOffice_FoundationBox( this, response );
  }

  async PostOffice_FoundationBox_create( data: PostOffice_FoundationBox ): Promise<void>
  {
    const response = await this.cinp.create<PostOffice_FoundationBox>( "/api/v1/PostOffice/FoundationBox", data._as_map( true ) );

    data._update( response.data );
  }

  async PostOffice_FoundationBox_update( id: number, data: PostOffice_FoundationBox ): Promise<void>
  {
    data._update( await this.cinp.updateOne<PostOffice_FoundationBox>( "/api/v1/PostOffice/FoundationBox:" + id + ":", data._as_map( false ) ) );
  }

  async PostOffice_FoundationBox_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/PostOffice/FoundationBox:" + id + ":" );
  }

  _PostOffice_FoundationBox_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async PostOffice_FoundationBox_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, PostOffice_FoundationBox>>
  {
    const [ filter_name, filter_value_map ] = this._PostOffice_FoundationBox_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<PostOffice_FoundationBox>( "/api/v1/PostOffice/FoundationBox", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new PostOffice_FoundationBox( this, response[ key ] );
    }

    return response;
  }

  async PostOffice_FoundationBox_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._PostOffice_FoundationBox_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/PostOffice/FoundationBox", filter_name, filter_value_map, position, count );
  }

  async PostOffice_FoundationBox_call_extend( id: number ,  additional_hours: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/PostOffice/FoundationBox:" + id + ":(extend)", { "additional_hours": additional_hours } );
    return;
  }


  // Model FoundationBox (end)
  // Model StructureBox at /api/v1/PostOffice/StructureBox

  async PostOffice_StructureBox_get( id: number ): Promise<PostOffice_StructureBox>
  {
    const response = await this.cinp.getOne<PostOffice_StructureBox>( "/api/v1/PostOffice/StructureBox:" + id + ":" );

    return new PostOffice_StructureBox( this, response );
  }

  async PostOffice_StructureBox_create( data: PostOffice_StructureBox ): Promise<void>
  {
    const response = await this.cinp.create<PostOffice_StructureBox>( "/api/v1/PostOffice/StructureBox", data._as_map( true ) );

    data._update( response.data );
  }

  async PostOffice_StructureBox_update( id: number, data: PostOffice_StructureBox ): Promise<void>
  {
    data._update( await this.cinp.updateOne<PostOffice_StructureBox>( "/api/v1/PostOffice/StructureBox:" + id + ":", data._as_map( false ) ) );
  }

  async PostOffice_StructureBox_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/PostOffice/StructureBox:" + id + ":" );
  }

  _PostOffice_StructureBox_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async PostOffice_StructureBox_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, PostOffice_StructureBox>>
  {
    const [ filter_name, filter_value_map ] = this._PostOffice_StructureBox_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<PostOffice_StructureBox>( "/api/v1/PostOffice/StructureBox", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new PostOffice_StructureBox( this, response[ key ] );
    }

    return response;
  }

  async PostOffice_StructureBox_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._PostOffice_StructureBox_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/PostOffice/StructureBox", filter_name, filter_value_map, position, count );
  }

  async PostOffice_StructureBox_call_extend( id: number ,  additional_hours: number ): Promise<void>
  {
    await this.cinp.callOne( "/api/v1/PostOffice/StructureBox:" + id + ":(extend)", { "additional_hours": additional_hours } );
    return;
  }


  // Model StructureBox (end)
  // Namespace PostOffice (end)
  // Namespace Records at /api/v1/Records/ version 0.1
/*

*/

  // Model Recorder at /api/v1/Records/Recorder

  async Records_Recorder_call_query( group: 'Site' | 'BluePrint' | 'Structure' | 'Foundation', query: string, fields: string, max_results: number ): Promise<string[]>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Records/Recorder(query)", { "group": group, "query": query, "fields": fields, "max_results": max_results } );
    return ( _cinp_result_ as string[] );
  }


  async Records_Recorder_call_queryObjects( group: 'Site' | 'BluePrint' | 'Structure' | 'Foundation', query: string, max_results: number ): Promise<string>
  {
    const _cinp_result_ = await this.cinp.callOne( "/api/v1/Records/Recorder(queryObjects)", { "group": group, "query": query, "max_results": max_results } );
    return ( _cinp_result_ as string );
  }


  // Model Recorder (end)
  // Namespace Records (end)
  // Namespace VirtualBox at /api/v1/VirtualBox/ version 0.1
/*
Plugin for VirtualBox.
*/

  // Model VirtualBoxComplex at /api/v1/VirtualBox/VirtualBoxComplex

  async VirtualBox_VirtualBoxComplex_get( id: string ): Promise<VirtualBox_VirtualBoxComplex>
  {
    const response = await this.cinp.getOne<VirtualBox_VirtualBoxComplex>( "/api/v1/VirtualBox/VirtualBoxComplex:" + id + ":" );

    return new VirtualBox_VirtualBoxComplex( this, response );
  }

  async VirtualBox_VirtualBoxComplex_create( data: VirtualBox_VirtualBoxComplex ): Promise<void>
  {
    const response = await this.cinp.create<VirtualBox_VirtualBoxComplex>( "/api/v1/VirtualBox/VirtualBoxComplex", data._as_map( true ) );

    data._update( response.data );
  }

  async VirtualBox_VirtualBoxComplex_update( id: string, data: VirtualBox_VirtualBoxComplex ): Promise<void>
  {
    data._update( await this.cinp.updateOne<VirtualBox_VirtualBoxComplex>( "/api/v1/VirtualBox/VirtualBoxComplex:" + id + ":", data._as_map( false ) ) );
  }

  async VirtualBox_VirtualBoxComplex_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/VirtualBox/VirtualBoxComplex:" + id + ":" );
  }

  _VirtualBox_VirtualBoxComplex_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async VirtualBox_VirtualBoxComplex_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, VirtualBox_VirtualBoxComplex>>
  {
    const [ filter_name, filter_value_map ] = this._VirtualBox_VirtualBoxComplex_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<VirtualBox_VirtualBoxComplex>( "/api/v1/VirtualBox/VirtualBoxComplex", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new VirtualBox_VirtualBoxComplex( this, response[ key ] );
    }

    return response;
  }

  async VirtualBox_VirtualBoxComplex_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._VirtualBox_VirtualBoxComplex_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/VirtualBox/VirtualBoxComplex", filter_name, filter_value_map, position, count );
  }

  // Model VirtualBoxComplex (end)
  // Model VirtualBoxFoundation at /api/v1/VirtualBox/VirtualBoxFoundation

  async VirtualBox_VirtualBoxFoundation_get( id: string ): Promise<VirtualBox_VirtualBoxFoundation>
  {
    const response = await this.cinp.getOne<VirtualBox_VirtualBoxFoundation>( "/api/v1/VirtualBox/VirtualBoxFoundation:" + id + ":" );

    return new VirtualBox_VirtualBoxFoundation( this, response );
  }

  async VirtualBox_VirtualBoxFoundation_create( data: VirtualBox_VirtualBoxFoundation ): Promise<void>
  {
    const response = await this.cinp.create<VirtualBox_VirtualBoxFoundation>( "/api/v1/VirtualBox/VirtualBoxFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async VirtualBox_VirtualBoxFoundation_update( id: string, data: VirtualBox_VirtualBoxFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<VirtualBox_VirtualBoxFoundation>( "/api/v1/VirtualBox/VirtualBoxFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async VirtualBox_VirtualBoxFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/VirtualBox/VirtualBoxFoundation:" + id + ":" );
  }

  _VirtualBox_VirtualBoxFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof VirtualBox_VirtualBoxFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async VirtualBox_VirtualBoxFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, VirtualBox_VirtualBoxFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._VirtualBox_VirtualBoxFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<VirtualBox_VirtualBoxFoundation>( "/api/v1/VirtualBox/VirtualBoxFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new VirtualBox_VirtualBoxFoundation( this, response[ key ] );
    }

    return response;
  }

  async VirtualBox_VirtualBoxFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._VirtualBox_VirtualBoxFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/VirtualBox/VirtualBoxFoundation", filter_name, filter_value_map, position, count );
  }

  // Model VirtualBoxFoundation (end)
  // Namespace VirtualBox (end)
  // Namespace LibVirt at /api/v1/LibVirt/ version 0.1
/*
Plugin for VirtualBox.
*/

  // Model LibVirtComplex at /api/v1/LibVirt/LibVirtComplex

  async LibVirt_LibVirtComplex_get( id: string ): Promise<LibVirt_LibVirtComplex>
  {
    const response = await this.cinp.getOne<LibVirt_LibVirtComplex>( "/api/v1/LibVirt/LibVirtComplex:" + id + ":" );

    return new LibVirt_LibVirtComplex( this, response );
  }

  async LibVirt_LibVirtComplex_create( data: LibVirt_LibVirtComplex ): Promise<void>
  {
    const response = await this.cinp.create<LibVirt_LibVirtComplex>( "/api/v1/LibVirt/LibVirtComplex", data._as_map( true ) );

    data._update( response.data );
  }

  async LibVirt_LibVirtComplex_update( id: string, data: LibVirt_LibVirtComplex ): Promise<void>
  {
    data._update( await this.cinp.updateOne<LibVirt_LibVirtComplex>( "/api/v1/LibVirt/LibVirtComplex:" + id + ":", data._as_map( false ) ) );
  }

  async LibVirt_LibVirtComplex_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/LibVirt/LibVirtComplex:" + id + ":" );
  }

  _LibVirt_LibVirtComplex_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async LibVirt_LibVirtComplex_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, LibVirt_LibVirtComplex>>
  {
    const [ filter_name, filter_value_map ] = this._LibVirt_LibVirtComplex_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<LibVirt_LibVirtComplex>( "/api/v1/LibVirt/LibVirtComplex", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new LibVirt_LibVirtComplex( this, response[ key ] );
    }

    return response;
  }

  async LibVirt_LibVirtComplex_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._LibVirt_LibVirtComplex_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/LibVirt/LibVirtComplex", filter_name, filter_value_map, position, count );
  }

  // Model LibVirtComplex (end)
  // Model LibVirtFoundation at /api/v1/LibVirt/LibVirtFoundation

  async LibVirt_LibVirtFoundation_get( id: string ): Promise<LibVirt_LibVirtFoundation>
  {
    const response = await this.cinp.getOne<LibVirt_LibVirtFoundation>( "/api/v1/LibVirt/LibVirtFoundation:" + id + ":" );

    return new LibVirt_LibVirtFoundation( this, response );
  }

  async LibVirt_LibVirtFoundation_create( data: LibVirt_LibVirtFoundation ): Promise<void>
  {
    const response = await this.cinp.create<LibVirt_LibVirtFoundation>( "/api/v1/LibVirt/LibVirtFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async LibVirt_LibVirtFoundation_update( id: string, data: LibVirt_LibVirtFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<LibVirt_LibVirtFoundation>( "/api/v1/LibVirt/LibVirtFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async LibVirt_LibVirtFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/LibVirt/LibVirtFoundation:" + id + ":" );
  }

  _LibVirt_LibVirtFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof LibVirt_LibVirtFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async LibVirt_LibVirtFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, LibVirt_LibVirtFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._LibVirt_LibVirtFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<LibVirt_LibVirtFoundation>( "/api/v1/LibVirt/LibVirtFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new LibVirt_LibVirtFoundation( this, response[ key ] );
    }

    return response;
  }

  async LibVirt_LibVirtFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._LibVirt_LibVirtFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/LibVirt/LibVirtFoundation", filter_name, filter_value_map, position, count );
  }

  // Model LibVirtFoundation (end)
  // Namespace LibVirt (end)
  // Namespace VCenter at /api/v1/VCenter/ version 0.1
/*

*/

  // Model VCenterComplex at /api/v1/VCenter/VCenterComplex

  async VCenter_VCenterComplex_get( id: string ): Promise<VCenter_VCenterComplex>
  {
    const response = await this.cinp.getOne<VCenter_VCenterComplex>( "/api/v1/VCenter/VCenterComplex:" + id + ":" );

    return new VCenter_VCenterComplex( this, response );
  }

  async VCenter_VCenterComplex_create( data: VCenter_VCenterComplex ): Promise<void>
  {
    const response = await this.cinp.create<VCenter_VCenterComplex>( "/api/v1/VCenter/VCenterComplex", data._as_map( true ) );

    data._update( response.data );
  }

  async VCenter_VCenterComplex_update( id: string, data: VCenter_VCenterComplex ): Promise<void>
  {
    data._update( await this.cinp.updateOne<VCenter_VCenterComplex>( "/api/v1/VCenter/VCenterComplex:" + id + ":", data._as_map( false ) ) );
  }

  async VCenter_VCenterComplex_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/VCenter/VCenterComplex:" + id + ":" );
  }

  _VCenter_VCenterComplex_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async VCenter_VCenterComplex_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, VCenter_VCenterComplex>>
  {
    const [ filter_name, filter_value_map ] = this._VCenter_VCenterComplex_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<VCenter_VCenterComplex>( "/api/v1/VCenter/VCenterComplex", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new VCenter_VCenterComplex( this, response[ key ] );
    }

    return response;
  }

  async VCenter_VCenterComplex_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._VCenter_VCenterComplex_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/VCenter/VCenterComplex", filter_name, filter_value_map, position, count );
  }

  // Model VCenterComplex (end)
  // Model VCenterFoundation at /api/v1/VCenter/VCenterFoundation

  async VCenter_VCenterFoundation_get( id: string ): Promise<VCenter_VCenterFoundation>
  {
    const response = await this.cinp.getOne<VCenter_VCenterFoundation>( "/api/v1/VCenter/VCenterFoundation:" + id + ":" );

    return new VCenter_VCenterFoundation( this, response );
  }

  async VCenter_VCenterFoundation_create( data: VCenter_VCenterFoundation ): Promise<void>
  {
    const response = await this.cinp.create<VCenter_VCenterFoundation>( "/api/v1/VCenter/VCenterFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async VCenter_VCenterFoundation_update( id: string, data: VCenter_VCenterFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<VCenter_VCenterFoundation>( "/api/v1/VCenter/VCenterFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async VCenter_VCenterFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/VCenter/VCenterFoundation:" + id + ":" );
  }

  _VCenter_VCenterFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof VCenter_VCenterFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async VCenter_VCenterFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, VCenter_VCenterFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._VCenter_VCenterFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<VCenter_VCenterFoundation>( "/api/v1/VCenter/VCenterFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new VCenter_VCenterFoundation( this, response[ key ] );
    }

    return response;
  }

  async VCenter_VCenterFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._VCenter_VCenterFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/VCenter/VCenterFoundation", filter_name, filter_value_map, position, count );
  }

  // Model VCenterFoundation (end)
  // Namespace VCenter (end)
  // Namespace Docker at /api/v1/Docker/ version 0.1
/*

*/

  // Model DockerComplex at /api/v1/Docker/DockerComplex

  async Docker_DockerComplex_get( id: string ): Promise<Docker_DockerComplex>
  {
    const response = await this.cinp.getOne<Docker_DockerComplex>( "/api/v1/Docker/DockerComplex:" + id + ":" );

    return new Docker_DockerComplex( this, response );
  }

  async Docker_DockerComplex_create( data: Docker_DockerComplex ): Promise<void>
  {
    const response = await this.cinp.create<Docker_DockerComplex>( "/api/v1/Docker/DockerComplex", data._as_map( true ) );

    data._update( response.data );
  }

  async Docker_DockerComplex_update( id: string, data: Docker_DockerComplex ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Docker_DockerComplex>( "/api/v1/Docker/DockerComplex:" + id + ":", data._as_map( false ) ) );
  }

  async Docker_DockerComplex_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Docker/DockerComplex:" + id + ":" );
  }

  _Docker_DockerComplex_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Docker_DockerComplex_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Docker_DockerComplex>>
  {
    const [ filter_name, filter_value_map ] = this._Docker_DockerComplex_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Docker_DockerComplex>( "/api/v1/Docker/DockerComplex", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Docker_DockerComplex( this, response[ key ] );
    }

    return response;
  }

  async Docker_DockerComplex_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Docker_DockerComplex_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Docker/DockerComplex", filter_name, filter_value_map, position, count );
  }

  // Model DockerComplex (end)
  // Model DockerFoundation at /api/v1/Docker/DockerFoundation

  async Docker_DockerFoundation_get( id: string ): Promise<Docker_DockerFoundation>
  {
    const response = await this.cinp.getOne<Docker_DockerFoundation>( "/api/v1/Docker/DockerFoundation:" + id + ":" );

    return new Docker_DockerFoundation( this, response );
  }

  async Docker_DockerFoundation_create( data: Docker_DockerFoundation ): Promise<void>
  {
    const response = await this.cinp.create<Docker_DockerFoundation>( "/api/v1/Docker/DockerFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async Docker_DockerFoundation_update( id: string, data: Docker_DockerFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Docker_DockerFoundation>( "/api/v1/Docker/DockerFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async Docker_DockerFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Docker/DockerFoundation:" + id + ":" );
  }

  _Docker_DockerFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Docker_DockerFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Docker_DockerFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Docker_DockerFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._Docker_DockerFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Docker_DockerFoundation>( "/api/v1/Docker/DockerFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Docker_DockerFoundation( this, response[ key ] );
    }

    return response;
  }

  async Docker_DockerFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Docker_DockerFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Docker/DockerFoundation", filter_name, filter_value_map, position, count );
  }

  // Model DockerFoundation (end)
  // Model DockerPort at /api/v1/Docker/DockerPort

  async Docker_DockerPort_get( id: number ): Promise<Docker_DockerPort>
  {
    const response = await this.cinp.getOne<Docker_DockerPort>( "/api/v1/Docker/DockerPort:" + id + ":" );

    return new Docker_DockerPort( this, response );
  }

  async Docker_DockerPort_create( data: Docker_DockerPort ): Promise<void>
  {
    const response = await this.cinp.create<Docker_DockerPort>( "/api/v1/Docker/DockerPort", data._as_map( true ) );

    data._update( response.data );
  }

  async Docker_DockerPort_update( id: number, data: Docker_DockerPort ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Docker_DockerPort>( "/api/v1/Docker/DockerPort:" + id + ":", data._as_map( false ) ) );
  }

  async Docker_DockerPort_delete( id: number ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Docker/DockerPort:" + id + ":" );
  }

  _Docker_DockerPort_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Docker_DockerPort_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Docker_DockerPort>>
  {
    const [ filter_name, filter_value_map ] = this._Docker_DockerPort_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Docker_DockerPort>( "/api/v1/Docker/DockerPort", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Docker_DockerPort( this, response[ key ] );
    }

    return response;
  }

  async Docker_DockerPort_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Docker_DockerPort_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Docker/DockerPort", filter_name, filter_value_map, position, count );
  }

  // Model DockerPort (end)
  // Namespace Docker (end)
  // Namespace IPMI at /api/v1/IPMI/ version 0.1
/*

*/

  // Model IPMIFoundation at /api/v1/IPMI/IPMIFoundation

  async IPMI_IPMIFoundation_get( id: string ): Promise<IPMI_IPMIFoundation>
  {
    const response = await this.cinp.getOne<IPMI_IPMIFoundation>( "/api/v1/IPMI/IPMIFoundation:" + id + ":" );

    return new IPMI_IPMIFoundation( this, response );
  }

  async IPMI_IPMIFoundation_create( data: IPMI_IPMIFoundation ): Promise<void>
  {
    const response = await this.cinp.create<IPMI_IPMIFoundation>( "/api/v1/IPMI/IPMIFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async IPMI_IPMIFoundation_update( id: string, data: IPMI_IPMIFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<IPMI_IPMIFoundation>( "/api/v1/IPMI/IPMIFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async IPMI_IPMIFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/IPMI/IPMIFoundation:" + id + ":" );
  }

  _IPMI_IPMIFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof IPMI_IPMIFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async IPMI_IPMIFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, IPMI_IPMIFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._IPMI_IPMIFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<IPMI_IPMIFoundation>( "/api/v1/IPMI/IPMIFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new IPMI_IPMIFoundation( this, response[ key ] );
    }

    return response;
  }

  async IPMI_IPMIFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._IPMI_IPMIFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/IPMI/IPMIFoundation", filter_name, filter_value_map, position, count );
  }

  // Model IPMIFoundation (end)
  // Namespace IPMI (end)
  // Namespace Manual at /api/v1/Manual/ version 0.1
/*

*/

  // Model ManualComplex at /api/v1/Manual/ManualComplex

  async Manual_ManualComplex_get( id: string ): Promise<Manual_ManualComplex>
  {
    const response = await this.cinp.getOne<Manual_ManualComplex>( "/api/v1/Manual/ManualComplex:" + id + ":" );

    return new Manual_ManualComplex( this, response );
  }

  async Manual_ManualComplex_create( data: Manual_ManualComplex ): Promise<void>
  {
    const response = await this.cinp.create<Manual_ManualComplex>( "/api/v1/Manual/ManualComplex", data._as_map( true ) );

    data._update( response.data );
  }

  async Manual_ManualComplex_update( id: string, data: Manual_ManualComplex ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Manual_ManualComplex>( "/api/v1/Manual/ManualComplex:" + id + ":", data._as_map( false ) ) );
  }

  async Manual_ManualComplex_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Manual/ManualComplex:" + id + ":" );
  }

  _Manual_ManualComplex_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Manual_ManualComplex_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Manual_ManualComplex>>
  {
    const [ filter_name, filter_value_map ] = this._Manual_ManualComplex_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Manual_ManualComplex>( "/api/v1/Manual/ManualComplex", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Manual_ManualComplex( this, response[ key ] );
    }

    return response;
  }

  async Manual_ManualComplex_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Manual_ManualComplex_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Manual/ManualComplex", filter_name, filter_value_map, position, count );
  }

  // Model ManualComplex (end)
  // Model ManualFoundation at /api/v1/Manual/ManualFoundation

  async Manual_ManualFoundation_get( id: string ): Promise<Manual_ManualFoundation>
  {
    const response = await this.cinp.getOne<Manual_ManualFoundation>( "/api/v1/Manual/ManualFoundation:" + id + ":" );

    return new Manual_ManualFoundation( this, response );
  }

  async Manual_ManualFoundation_create( data: Manual_ManualFoundation ): Promise<void>
  {
    const response = await this.cinp.create<Manual_ManualFoundation>( "/api/v1/Manual/ManualFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async Manual_ManualFoundation_update( id: string, data: Manual_ManualFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Manual_ManualFoundation>( "/api/v1/Manual/ManualFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async Manual_ManualFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Manual/ManualFoundation:" + id + ":" );
  }

  _Manual_ManualFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Manual_ManualFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Manual_ManualFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Manual_ManualFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._Manual_ManualFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Manual_ManualFoundation>( "/api/v1/Manual/ManualFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Manual_ManualFoundation( this, response[ key ] );
    }

    return response;
  }

  async Manual_ManualFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Manual_ManualFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Manual/ManualFoundation", filter_name, filter_value_map, position, count );
  }

  // Model ManualFoundation (end)
  // Model ManualComplexedFoundation at /api/v1/Manual/ManualComplexedFoundation

  async Manual_ManualComplexedFoundation_get( id: string ): Promise<Manual_ManualComplexedFoundation>
  {
    const response = await this.cinp.getOne<Manual_ManualComplexedFoundation>( "/api/v1/Manual/ManualComplexedFoundation:" + id + ":" );

    return new Manual_ManualComplexedFoundation( this, response );
  }

  async Manual_ManualComplexedFoundation_create( data: Manual_ManualComplexedFoundation ): Promise<void>
  {
    const response = await this.cinp.create<Manual_ManualComplexedFoundation>( "/api/v1/Manual/ManualComplexedFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async Manual_ManualComplexedFoundation_update( id: string, data: Manual_ManualComplexedFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Manual_ManualComplexedFoundation>( "/api/v1/Manual/ManualComplexedFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async Manual_ManualComplexedFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Manual/ManualComplexedFoundation:" + id + ":" );
  }

  _Manual_ManualComplexedFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Manual_ManualComplexedFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Manual_ManualComplexedFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Manual_ManualComplexedFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._Manual_ManualComplexedFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Manual_ManualComplexedFoundation>( "/api/v1/Manual/ManualComplexedFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Manual_ManualComplexedFoundation( this, response[ key ] );
    }

    return response;
  }

  async Manual_ManualComplexedFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Manual_ManualComplexedFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Manual/ManualComplexedFoundation", filter_name, filter_value_map, position, count );
  }

  // Model ManualComplexedFoundation (end)
  // Namespace Manual (end)
  // Namespace Proxmox at /api/v1/Proxmox/ version 0.1
/*

*/

  // Model ProxmoxComplex at /api/v1/Proxmox/ProxmoxComplex

  async Proxmox_ProxmoxComplex_get( id: string ): Promise<Proxmox_ProxmoxComplex>
  {
    const response = await this.cinp.getOne<Proxmox_ProxmoxComplex>( "/api/v1/Proxmox/ProxmoxComplex:" + id + ":" );

    return new Proxmox_ProxmoxComplex( this, response );
  }

  async Proxmox_ProxmoxComplex_create( data: Proxmox_ProxmoxComplex ): Promise<void>
  {
    const response = await this.cinp.create<Proxmox_ProxmoxComplex>( "/api/v1/Proxmox/ProxmoxComplex", data._as_map( true ) );

    data._update( response.data );
  }

  async Proxmox_ProxmoxComplex_update( id: string, data: Proxmox_ProxmoxComplex ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Proxmox_ProxmoxComplex>( "/api/v1/Proxmox/ProxmoxComplex:" + id + ":", data._as_map( false ) ) );
  }

  async Proxmox_ProxmoxComplex_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Proxmox/ProxmoxComplex:" + id + ":" );
  }

  _Proxmox_ProxmoxComplex_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Proxmox_ProxmoxComplex_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Proxmox_ProxmoxComplex>>
  {
    const [ filter_name, filter_value_map ] = this._Proxmox_ProxmoxComplex_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Proxmox_ProxmoxComplex>( "/api/v1/Proxmox/ProxmoxComplex", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Proxmox_ProxmoxComplex( this, response[ key ] );
    }

    return response;
  }

  async Proxmox_ProxmoxComplex_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Proxmox_ProxmoxComplex_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Proxmox/ProxmoxComplex", filter_name, filter_value_map, position, count );
  }

  // Model ProxmoxComplex (end)
  // Model ProxmoxFoundation at /api/v1/Proxmox/ProxmoxFoundation

  async Proxmox_ProxmoxFoundation_get( id: string ): Promise<Proxmox_ProxmoxFoundation>
  {
    const response = await this.cinp.getOne<Proxmox_ProxmoxFoundation>( "/api/v1/Proxmox/ProxmoxFoundation:" + id + ":" );

    return new Proxmox_ProxmoxFoundation( this, response );
  }

  async Proxmox_ProxmoxFoundation_create( data: Proxmox_ProxmoxFoundation ): Promise<void>
  {
    const response = await this.cinp.create<Proxmox_ProxmoxFoundation>( "/api/v1/Proxmox/ProxmoxFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async Proxmox_ProxmoxFoundation_update( id: string, data: Proxmox_ProxmoxFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Proxmox_ProxmoxFoundation>( "/api/v1/Proxmox/ProxmoxFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async Proxmox_ProxmoxFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Proxmox/ProxmoxFoundation:" + id + ":" );
  }

  _Proxmox_ProxmoxFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Proxmox_ProxmoxFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Proxmox_ProxmoxFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Proxmox_ProxmoxFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._Proxmox_ProxmoxFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Proxmox_ProxmoxFoundation>( "/api/v1/Proxmox/ProxmoxFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Proxmox_ProxmoxFoundation( this, response[ key ] );
    }

    return response;
  }

  async Proxmox_ProxmoxFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Proxmox_ProxmoxFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Proxmox/ProxmoxFoundation", filter_name, filter_value_map, position, count );
  }

  // Model ProxmoxFoundation (end)
  // Namespace Proxmox (end)
  // Namespace Azure at /api/v1/Azure/ version 0.1
/*

*/

  // Model AzureComplex at /api/v1/Azure/AzureComplex

  async Azure_AzureComplex_get( id: string ): Promise<Azure_AzureComplex>
  {
    const response = await this.cinp.getOne<Azure_AzureComplex>( "/api/v1/Azure/AzureComplex:" + id + ":" );

    return new Azure_AzureComplex( this, response );
  }

  async Azure_AzureComplex_create( data: Azure_AzureComplex ): Promise<void>
  {
    const response = await this.cinp.create<Azure_AzureComplex>( "/api/v1/Azure/AzureComplex", data._as_map( true ) );

    data._update( response.data );
  }

  async Azure_AzureComplex_update( id: string, data: Azure_AzureComplex ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Azure_AzureComplex>( "/api/v1/Azure/AzureComplex:" + id + ":", data._as_map( false ) ) );
  }

  async Azure_AzureComplex_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Azure/AzureComplex:" + id + ":" );
  }

  _Azure_AzureComplex_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Azure_AzureComplex_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Azure_AzureComplex>>
  {
    const [ filter_name, filter_value_map ] = this._Azure_AzureComplex_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Azure_AzureComplex>( "/api/v1/Azure/AzureComplex", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Azure_AzureComplex( this, response[ key ] );
    }

    return response;
  }

  async Azure_AzureComplex_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Azure_AzureComplex_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Azure/AzureComplex", filter_name, filter_value_map, position, count );
  }

  // Model AzureComplex (end)
  // Model AzureFoundation at /api/v1/Azure/AzureFoundation

  async Azure_AzureFoundation_get( id: string ): Promise<Azure_AzureFoundation>
  {
    const response = await this.cinp.getOne<Azure_AzureFoundation>( "/api/v1/Azure/AzureFoundation:" + id + ":" );

    return new Azure_AzureFoundation( this, response );
  }

  async Azure_AzureFoundation_create( data: Azure_AzureFoundation ): Promise<void>
  {
    const response = await this.cinp.create<Azure_AzureFoundation>( "/api/v1/Azure/AzureFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async Azure_AzureFoundation_update( id: string, data: Azure_AzureFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Azure_AzureFoundation>( "/api/v1/Azure/AzureFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async Azure_AzureFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Azure/AzureFoundation:" + id + ":" );
  }

  _Azure_AzureFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Azure_AzureFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Azure_AzureFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Azure_AzureFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._Azure_AzureFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Azure_AzureFoundation>( "/api/v1/Azure/AzureFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Azure_AzureFoundation( this, response[ key ] );
    }

    return response;
  }

  async Azure_AzureFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Azure_AzureFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Azure/AzureFoundation", filter_name, filter_value_map, position, count );
  }

  // Model AzureFoundation (end)
  // Namespace Azure (end)
  // Namespace AMT at /api/v1/AMT/ version 0.1
/*

*/

  // Model AMTFoundation at /api/v1/AMT/AMTFoundation

  async AMT_AMTFoundation_get( id: string ): Promise<AMT_AMTFoundation>
  {
    const response = await this.cinp.getOne<AMT_AMTFoundation>( "/api/v1/AMT/AMTFoundation:" + id + ":" );

    return new AMT_AMTFoundation( this, response );
  }

  async AMT_AMTFoundation_create( data: AMT_AMTFoundation ): Promise<void>
  {
    const response = await this.cinp.create<AMT_AMTFoundation>( "/api/v1/AMT/AMTFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async AMT_AMTFoundation_update( id: string, data: AMT_AMTFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<AMT_AMTFoundation>( "/api/v1/AMT/AMTFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async AMT_AMTFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/AMT/AMTFoundation:" + id + ":" );
  }

  _AMT_AMTFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof AMT_AMTFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async AMT_AMTFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, AMT_AMTFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._AMT_AMTFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<AMT_AMTFoundation>( "/api/v1/AMT/AMTFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new AMT_AMTFoundation( this, response[ key ] );
    }

    return response;
  }

  async AMT_AMTFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._AMT_AMTFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/AMT/AMTFoundation", filter_name, filter_value_map, position, count );
  }

  // Model AMTFoundation (end)
  // Namespace AMT (end)
  // Namespace Test at /api/v1/Test/ version 0.1
/*

*/

  // Model TestComplex at /api/v1/Test/TestComplex

  async Test_TestComplex_get( id: string ): Promise<Test_TestComplex>
  {
    const response = await this.cinp.getOne<Test_TestComplex>( "/api/v1/Test/TestComplex:" + id + ":" );

    return new Test_TestComplex( this, response );
  }

  async Test_TestComplex_create( data: Test_TestComplex ): Promise<void>
  {
    const response = await this.cinp.create<Test_TestComplex>( "/api/v1/Test/TestComplex", data._as_map( true ) );

    data._update( response.data );
  }

  async Test_TestComplex_update( id: string, data: Test_TestComplex ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Test_TestComplex>( "/api/v1/Test/TestComplex:" + id + ":", data._as_map( false ) ) );
  }

  async Test_TestComplex_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Test/TestComplex:" + id + ":" );
  }

  _Test_TestComplex_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Test_TestComplex_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Test_TestComplex>>
  {
    const [ filter_name, filter_value_map ] = this._Test_TestComplex_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Test_TestComplex>( "/api/v1/Test/TestComplex", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Test_TestComplex( this, response[ key ] );
    }

    return response;
  }

  async Test_TestComplex_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Test_TestComplex_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Test/TestComplex", filter_name, filter_value_map, position, count );
  }

  // Model TestComplex (end)
  // Model TestFoundation at /api/v1/Test/TestFoundation

  async Test_TestFoundation_get( id: string ): Promise<Test_TestFoundation>
  {
    const response = await this.cinp.getOne<Test_TestFoundation>( "/api/v1/Test/TestFoundation:" + id + ":" );

    return new Test_TestFoundation( this, response );
  }

  async Test_TestFoundation_create( data: Test_TestFoundation ): Promise<void>
  {
    const response = await this.cinp.create<Test_TestFoundation>( "/api/v1/Test/TestFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async Test_TestFoundation_update( id: string, data: Test_TestFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Test_TestFoundation>( "/api/v1/Test/TestFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async Test_TestFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Test/TestFoundation:" + id + ":" );
  }

  _Test_TestFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Test_TestFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Test_TestFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Test_TestFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._Test_TestFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Test_TestFoundation>( "/api/v1/Test/TestFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Test_TestFoundation( this, response[ key ] );
    }

    return response;
  }

  async Test_TestFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Test_TestFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Test/TestFoundation", filter_name, filter_value_map, position, count );
  }

  // Model TestFoundation (end)
  // Model TestComplexedFoundation at /api/v1/Test/TestComplexedFoundation

  async Test_TestComplexedFoundation_get( id: string ): Promise<Test_TestComplexedFoundation>
  {
    const response = await this.cinp.getOne<Test_TestComplexedFoundation>( "/api/v1/Test/TestComplexedFoundation:" + id + ":" );

    return new Test_TestComplexedFoundation( this, response );
  }

  async Test_TestComplexedFoundation_create( data: Test_TestComplexedFoundation ): Promise<void>
  {
    const response = await this.cinp.create<Test_TestComplexedFoundation>( "/api/v1/Test/TestComplexedFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async Test_TestComplexedFoundation_update( id: string, data: Test_TestComplexedFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<Test_TestComplexedFoundation>( "/api/v1/Test/TestComplexedFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async Test_TestComplexedFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/Test/TestComplexedFoundation:" + id + ":" );
  }

  _Test_TestComplexedFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof Test_TestComplexedFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async Test_TestComplexedFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, Test_TestComplexedFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._Test_TestComplexedFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<Test_TestComplexedFoundation>( "/api/v1/Test/TestComplexedFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new Test_TestComplexedFoundation( this, response[ key ] );
    }

    return response;
  }

  async Test_TestComplexedFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._Test_TestComplexedFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/Test/TestComplexedFoundation", filter_name, filter_value_map, position, count );
  }

  // Model TestComplexedFoundation (end)
  // Namespace Test (end)
  // Namespace AWS at /api/v1/AWS/ version 0.1
/*

*/

  // Model AWSEC2Foundation at /api/v1/AWS/AWSEC2Foundation

  async AWS_AWSEC2Foundation_get( id: string ): Promise<AWS_AWSEC2Foundation>
  {
    const response = await this.cinp.getOne<AWS_AWSEC2Foundation>( "/api/v1/AWS/AWSEC2Foundation:" + id + ":" );

    return new AWS_AWSEC2Foundation( this, response );
  }

  async AWS_AWSEC2Foundation_create( data: AWS_AWSEC2Foundation ): Promise<void>
  {
    const response = await this.cinp.create<AWS_AWSEC2Foundation>( "/api/v1/AWS/AWSEC2Foundation", data._as_map( true ) );

    data._update( response.data );
  }

  async AWS_AWSEC2Foundation_update( id: string, data: AWS_AWSEC2Foundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<AWS_AWSEC2Foundation>( "/api/v1/AWS/AWSEC2Foundation:" + id + ":", data._as_map( false ) ) );
  }

  async AWS_AWSEC2Foundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/AWS/AWSEC2Foundation:" + id + ":" );
  }

  _AWS_AWSEC2Foundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof AWS_AWSEC2Foundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async AWS_AWSEC2Foundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, AWS_AWSEC2Foundation>>
  {
    const [ filter_name, filter_value_map ] = this._AWS_AWSEC2Foundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<AWS_AWSEC2Foundation>( "/api/v1/AWS/AWSEC2Foundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new AWS_AWSEC2Foundation( this, response[ key ] );
    }

    return response;
  }

  async AWS_AWSEC2Foundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._AWS_AWSEC2Foundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/AWS/AWSEC2Foundation", filter_name, filter_value_map, position, count );
  }

  // Model AWSEC2Foundation (end)
  // Namespace AWS (end)
  // Namespace RedFish at /api/v1/RedFish/ version 0.1
/*

*/

  // Model RedFishFoundation at /api/v1/RedFish/RedFishFoundation

  async RedFish_RedFishFoundation_get( id: string ): Promise<RedFish_RedFishFoundation>
  {
    const response = await this.cinp.getOne<RedFish_RedFishFoundation>( "/api/v1/RedFish/RedFishFoundation:" + id + ":" );

    return new RedFish_RedFishFoundation( this, response );
  }

  async RedFish_RedFishFoundation_create( data: RedFish_RedFishFoundation ): Promise<void>
  {
    const response = await this.cinp.create<RedFish_RedFishFoundation>( "/api/v1/RedFish/RedFishFoundation", data._as_map( true ) );

    data._update( response.data );
  }

  async RedFish_RedFishFoundation_update( id: string, data: RedFish_RedFishFoundation ): Promise<void>
  {
    data._update( await this.cinp.updateOne<RedFish_RedFishFoundation>( "/api/v1/RedFish/RedFishFoundation:" + id + ":", data._as_map( false ) ) );
  }

  async RedFish_RedFishFoundation_delete( id: string ): Promise<void>
  {
    await this.cinp.delete( "/api/v1/RedFish/RedFishFoundation:" + id + ":" );
  }

  _RedFish_RedFishFoundation_filter_lookup( filter: ListFilter | undefined ): [ string | undefined, Record<string, any> | undefined ]
  {
    if( filter === undefined || filter instanceof ListFilterAll )
      return [ undefined, undefined ];


    if( filter instanceof RedFish_RedFishFoundation._ListFilter_site )
      return [ 'site', { site: filter.site.toURL(),  } ];

    throw TypeError( 'Filter ' + filter + ' is not valid' );
  }

  async RedFish_RedFishFoundation_get_multi( { filter, position, count }: ListParameters ): Promise<Record<string, RedFish_RedFishFoundation>>
  {
    const [ filter_name, filter_value_map ] = this._RedFish_RedFishFoundation_filter_lookup( filter );

    const response = await this.cinp.getFilteredObjects<RedFish_RedFishFoundation>( "/api/v1/RedFish/RedFishFoundation", filter_name, filter_value_map, position, count );

    for ( var key in response )
    {
      response[ key ] = new RedFish_RedFishFoundation( this, response[ key ] );
    }

    return response;
  }

  async RedFish_RedFishFoundation_list( { filter, position, count }: ListParameters ): Promise<List>
  {
    const [ filter_name, filter_value_map ] = this._RedFish_RedFishFoundation_filter_lookup( filter );
    return await this.cinp.list( "/api/v1/RedFish/RedFishFoundation", filter_name, filter_value_map, position, count );
  }

  // Model RedFishFoundation (end)
  // Namespace RedFish (end)
  // Namespace  (end)
}

export default Contractor;
// Model User from Auth at /api/v1/Auth/User
/*

*/
export class Auth_User
{
  private _service: Contractor;



  constructor( service: Contractor, source?: ModelConstructorSource<Auth_User> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Auth_User;
      this._update( source );
    }
  }

  _update( data: ModelUpdate<Auth_User> ): void
  {
    data = data as Auth_User;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return {  };

    return {  };
  }



  async _call_login(  username: string, password: string  ): Promise<string>
  {
    return this._service.Auth_User_call_login( username, password  );
  }


  async _call_logout(  ): Promise<void>
  {
    return this._service.Auth_User_call_logout( );
  }


  async _call_whoami(  ): Promise<string>
  {
    return this._service.Auth_User_call_whoami( );
  }


  async _call_changePassword(  password: string  ): Promise<void>
  {
    return this._service.Auth_User_call_changePassword( password  );
  }


}

export namespace Auth_User
{

}

// Model BluePrint from BluePrint at /api/v1/BluePrint/BluePrint
/*
BluePrint(name, description, config_values, updated, created)
*/
export class BluePrint_BluePrint
{
  private _service: Contractor;

  public name: string = '';

  public description: string | undefined = undefined;
  public config_values: {} | undefined = {};
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public script_map: {} | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<BluePrint_BluePrint> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as BluePrint_BluePrint;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/BluePrint/BluePrint' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/BluePrint/BluePrint:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<BluePrint_BluePrint> ): void
  {
    data = data as BluePrint_BluePrint;
    this.description = data.description;
    this.config_values = data.config_values;
    this.updated = data.updated;
    this.created = data.created;
    this.script_map = data.script_map;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'description': this.description, 'config_values': this.config_values,  };

    return { 'description': this.description, 'config_values': this.config_values,  };
  }



  async _call_getConfig(  ): Promise<{}>
  {
    return this._service.BluePrint_BluePrint_call_getConfig( this.name );
  }


}

export namespace BluePrint_BluePrint
{

}

// Model FoundationBluePrint from BluePrint at /api/v1/BluePrint/FoundationBluePrint
/*
FoundationBluePrint(name, description, config_values, updated, created, blueprint_ptr, foundation_type_list, validation_template, physical_interface_names)
*/
export class BluePrint_FoundationBluePrint
{
  private _service: Contractor;

  public name: string = '';

  public description: string | undefined = undefined;
  public config_values: {} | undefined = {};
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public foundation_type_list: string[] | undefined = [];
  public validation_template: {} | undefined = {};
  public physical_interface_names: string[] | undefined = [];
  public parent_list: BluePrint_FoundationBluePrint[] | undefined = [];
  public script_map: {} | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<BluePrint_FoundationBluePrint> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as BluePrint_FoundationBluePrint;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/BluePrint/FoundationBluePrint' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/BluePrint/FoundationBluePrint:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<BluePrint_FoundationBluePrint> ): void
  {
    data = data as BluePrint_FoundationBluePrint;
    this.description = data.description;
    this.config_values = data.config_values;
    this.updated = data.updated;
    this.created = data.created;
    this.foundation_type_list = data.foundation_type_list;
    this.validation_template = data.validation_template;
    this.physical_interface_names = data.physical_interface_names;
    if( data.parent_list !== undefined )
      this.parent_list = data.parent_list.map( ( uri: string ) => { return new BluePrint_FoundationBluePrint( this._service, uri ) } );
    this.script_map = data.script_map;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'description': this.description, 'config_values': this.config_values, 'foundation_type_list': this.foundation_type_list, 'validation_template': this.validation_template, 'physical_interface_names': this.physical_interface_names, 'parent_list': this.parent_list !== undefined ? this.parent_list.map( i => i.toURL() ) : undefined,  };

    return { 'description': this.description, 'config_values': this.config_values, 'foundation_type_list': this.foundation_type_list, 'validation_template': this.validation_template, 'physical_interface_names': this.physical_interface_names, 'parent_list': this.parent_list !== undefined ? this.parent_list.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.BluePrint_FoundationBluePrint_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.BluePrint_FoundationBluePrint_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.BluePrint_FoundationBluePrint_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.BluePrint_FoundationBluePrint_delete( this.name );
  }


  async _call_getConfig(  ): Promise<{}>
  {
    return this._service.BluePrint_FoundationBluePrint_call_getConfig( this.name );
  }


}

export namespace BluePrint_FoundationBluePrint
{

}

// Model StructureBluePrint from BluePrint at /api/v1/BluePrint/StructureBluePrint
/*
StructureBluePrint(name, description, config_values, updated, created, blueprint_ptr)
*/
export class BluePrint_StructureBluePrint
{
  private _service: Contractor;

  public name: string = '';

  public description: string | undefined = undefined;
  public config_values: {} | undefined = {};
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public parent_list: BluePrint_StructureBluePrint[] | undefined = [];
  public foundation_blueprint_list: BluePrint_FoundationBluePrint[] | undefined = [];
  public script_map: {} | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<BluePrint_StructureBluePrint> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as BluePrint_StructureBluePrint;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/BluePrint/StructureBluePrint' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/BluePrint/StructureBluePrint:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<BluePrint_StructureBluePrint> ): void
  {
    data = data as BluePrint_StructureBluePrint;
    this.description = data.description;
    this.config_values = data.config_values;
    this.updated = data.updated;
    this.created = data.created;
    if( data.parent_list !== undefined )
      this.parent_list = data.parent_list.map( ( uri: string ) => { return new BluePrint_StructureBluePrint( this._service, uri ) } );
    if( data.foundation_blueprint_list !== undefined )
      this.foundation_blueprint_list = data.foundation_blueprint_list.map( ( uri: string ) => { return new BluePrint_FoundationBluePrint( this._service, uri ) } );
    this.script_map = data.script_map;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'description': this.description, 'config_values': this.config_values, 'parent_list': this.parent_list !== undefined ? this.parent_list.map( i => i.toURL() ) : undefined, 'foundation_blueprint_list': this.foundation_blueprint_list !== undefined ? this.foundation_blueprint_list.map( i => i.toURL() ) : undefined,  };

    return { 'description': this.description, 'config_values': this.config_values, 'parent_list': this.parent_list !== undefined ? this.parent_list.map( i => i.toURL() ) : undefined, 'foundation_blueprint_list': this.foundation_blueprint_list !== undefined ? this.foundation_blueprint_list.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.BluePrint_StructureBluePrint_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.BluePrint_StructureBluePrint_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.BluePrint_StructureBluePrint_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.BluePrint_StructureBluePrint_delete( this.name );
  }


  async _call_getConfig(  ): Promise<{}>
  {
    return this._service.BluePrint_StructureBluePrint_call_getConfig( this.name );
  }


}

export namespace BluePrint_StructureBluePrint
{

}

// Model Script from BluePrint at /api/v1/BluePrint/Script
/*
Script(name, description, script, updated, created)
*/
export class BluePrint_Script
{
  private _service: Contractor;

  public name: string = '';

  public description: string | undefined = undefined;
  public script: string | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<BluePrint_Script> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as BluePrint_Script;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/BluePrint/Script' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/BluePrint/Script:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<BluePrint_Script> ): void
  {
    data = data as BluePrint_Script;
    this.description = data.description;
    this.script = data.script;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'description': this.description, 'script': this.script,  };

    return { 'description': this.description, 'script': this.script,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.BluePrint_Script_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.BluePrint_Script_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.BluePrint_Script_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.BluePrint_Script_delete( this.name );
  }


}

export namespace BluePrint_Script
{

}

// Model BluePrintScript from BluePrint at /api/v1/BluePrint/BluePrintScript
/*
BluePrintScript(id, blueprint, script, name, updated, created)
*/
export class BluePrint_BluePrintScript
{
  private _service: Contractor;

  public id: number = 0;

  public blueprint: BluePrint_BluePrint | undefined = undefined;
  public script: BluePrint_Script | undefined = undefined;
  public name: string | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<BluePrint_BluePrintScript> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as BluePrint_BluePrintScript;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/BluePrint/BluePrintScript' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/BluePrint/BluePrintScript:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<BluePrint_BluePrintScript> ): void
  {
    data = data as BluePrint_BluePrintScript;
    this.blueprint = data.blueprint !== null ? new BluePrint_BluePrint( this._service, data.blueprint ) : undefined;
    this.script = data.script !== null ? new BluePrint_Script( this._service, data.script ) : undefined;
    this.name = data.name;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'script': this.script !== undefined ? this.script.toURL() : undefined, 'name': this.name,  };

    return { 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'script': this.script !== undefined ? this.script.toURL() : undefined, 'name': this.name,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.BluePrint_BluePrintScript_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.BluePrint_BluePrintScript_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.BluePrint_BluePrintScript_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.BluePrint_BluePrintScript_delete( this.id );
  }


}

export namespace BluePrint_BluePrintScript
{

  export class _ListFilter_blueprint implements ListFilter
  {
    public blueprint: BluePrint_BluePrint;

    constructor( blueprint: BluePrint_BluePrint )
    {
      this.blueprint = blueprint;
    }

  };


}

// Model PXE from BluePrint at /api/v1/BluePrint/PXE
/*
PXE(name, boot_script, template, updated, created)
*/
export class BluePrint_PXE
{
  private _service: Contractor;

  public name: string = '';

  public boot_script: string | undefined = undefined;
  public template: string | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<BluePrint_PXE> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as BluePrint_PXE;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/BluePrint/PXE' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/BluePrint/PXE:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<BluePrint_PXE> ): void
  {
    data = data as BluePrint_PXE;
    this.boot_script = data.boot_script;
    this.template = data.template;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'boot_script': this.boot_script, 'template': this.template,  };

    return { 'boot_script': this.boot_script, 'template': this.template,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.BluePrint_PXE_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.BluePrint_PXE_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.BluePrint_PXE_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.BluePrint_PXE_delete( this.name );
  }


}

export namespace BluePrint_PXE
{

}

// Model Site from Site at /api/v1/Site/Site
/*
Site(name, zone, description, parent, config_values, updated, created)
*/
export class Site_Site
{
  private _service: Contractor;

  public name: string = '';

  public zone: Directory_Zone | undefined = undefined;
  public description: string | undefined = undefined;
  public parent: Site_Site | undefined = undefined;
  public config_values: {} | undefined = {};
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Site_Site> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Site_Site;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Site/Site' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Site/Site:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Site_Site> ): void
  {
    data = data as Site_Site;
    this.zone = data.zone !== null ? new Directory_Zone( this._service, data.zone ) : undefined;
    this.description = data.description;
    this.parent = data.parent !== null ? new Site_Site( this._service, data.parent ) : undefined;
    this.config_values = data.config_values;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'zone': this.zone !== undefined ? this.zone.toURL() : undefined, 'description': this.description, 'parent': this.parent !== undefined ? this.parent.toURL() : undefined, 'config_values': this.config_values,  };

    return { 'zone': this.zone !== undefined ? this.zone.toURL() : undefined, 'description': this.description, 'parent': this.parent !== undefined ? this.parent.toURL() : undefined, 'config_values': this.config_values,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Site_Site_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Site_Site_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Site_Site_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Site_Site_delete( this.name );
  }


  async _call_getConfig(  ): Promise<{}>
  {
    return this._service.Site_Site_call_getConfig( this.name );
  }


  async _call_getDependencyMap(  ): Promise<{}>
  {
    return this._service.Site_Site_call_getDependencyMap( this.name );
  }


}

export namespace Site_Site
{

}

// Model Plot from Survey at /api/v1/Survey/Plot
/*
Plot(name, corners, parent, updated, created)
*/
export class Survey_Plot
{
  private _service: Contractor;

  public name: string = '';

  public corners: string | undefined = undefined;
  public parent: Survey_Plot | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Survey_Plot> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Survey_Plot;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Survey/Plot' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Survey/Plot:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Survey_Plot> ): void
  {
    data = data as Survey_Plot;
    this.corners = data.corners;
    this.parent = data.parent !== null ? new Survey_Plot( this._service, data.parent ) : undefined;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'corners': this.corners, 'parent': this.parent !== undefined ? this.parent.toURL() : undefined,  };

    return { 'corners': this.corners, 'parent': this.parent !== undefined ? this.parent.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Survey_Plot_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Survey_Plot_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Survey_Plot_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Survey_Plot_delete( this.name );
  }


}

export namespace Survey_Plot
{

}

// Model Cartographer from Survey at /api/v1/Survey/Cartographer
/*
Cartographer(identifier, foundation, message, info_map, last_checkin, updated, created)
*/
export class Survey_Cartographer
{
  private _service: Contractor;

  public identifier: string = '';

  public foundation: Building_Foundation | undefined = undefined;
  public message: string | undefined = undefined;
  public info_map: {} | undefined = {};
  public last_checkin: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Survey_Cartographer> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Survey_Cartographer;
      this.identifier = source.identifier;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Survey/Cartographer' ) )
    {
      this.identifier = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.identifier = source;
    }
  }

  toString(): string
  {
    return this.identifier.toString();
  }

  toURL(): string
  {
    return '/api/v1/Survey/Cartographer:' + this.identifier.toString() + ':';
  }

  _update( data: ModelUpdate<Survey_Cartographer> ): void
  {
    data = data as Survey_Cartographer;
    this.foundation = data.foundation !== null ? new Building_Foundation( this._service, data.foundation ) : undefined;
    this.message = data.message;
    this.info_map = data.info_map;
    this.last_checkin = data.last_checkin;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'identifier': this.identifier, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined, 'message': this.message, 'info_map': this.info_map, 'last_checkin': this.last_checkin,  };

    return { 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined, 'message': this.message, 'info_map': this.info_map, 'last_checkin': this.last_checkin,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Survey_Cartographer_get( this.identifier );

    this._update( record );
  }

  async _delete(): Promise<void>
  {
    return this._service.Survey_Cartographer_delete( this.identifier );
  }


  async _call_assign(  foundation: Building_Foundation  ): Promise<void>
  {
    return this._service.Survey_Cartographer_call_assign( this.identifier,  foundation  );
  }


  async _call_register(  identifier: string  ): Promise<void>
  {
    return this._service.Survey_Cartographer_call_register( identifier  );
  }


  async _call_lookup(  info_map: {}  ): Promise<{}>
  {
    return this._service.Survey_Cartographer_call_lookup( this.identifier,  info_map  );
  }


  async _call_setMessage(  message: string  ): Promise<void>
  {
    return this._service.Survey_Cartographer_call_setMessage( this.identifier,  message  );
  }


  async _call_done(  ): Promise<void>
  {
    return this._service.Survey_Cartographer_call_done( this.identifier );
  }


}

export namespace Survey_Cartographer
{

}

// Model Zone from Directory at /api/v1/Directory/Zone
/*
Zone(id, name, parent, ttl, refresh, retry, expire, minimum, updated, created)
*/
export class Directory_Zone
{
  private _service: Contractor;

  public id: number = 0;

  public name: string | undefined = undefined;
  public parent: Directory_Zone | undefined = undefined;
  public ttl: number | undefined = 3600;
  public refresh: number | undefined = 86400;
  public retry: number | undefined = 7200;
  public expire: number | undefined = 36000;
  public minimum: number | undefined = 172800;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public fqdn: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Directory_Zone> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Directory_Zone;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Directory/Zone' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Directory/Zone:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Directory_Zone> ): void
  {
    data = data as Directory_Zone;
    this.name = data.name;
    this.parent = data.parent !== null ? new Directory_Zone( this._service, data.parent ) : undefined;
    this.ttl = data.ttl;
    this.refresh = data.refresh;
    this.retry = data.retry;
    this.expire = data.expire;
    this.minimum = data.minimum;
    this.updated = data.updated;
    this.created = data.created;
    this.fqdn = data.fqdn;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'parent': this.parent !== undefined ? this.parent.toURL() : undefined, 'ttl': this.ttl, 'refresh': this.refresh, 'retry': this.retry, 'expire': this.expire, 'minimum': this.minimum,  };

    return { 'name': this.name, 'parent': this.parent !== undefined ? this.parent.toURL() : undefined, 'ttl': this.ttl, 'refresh': this.refresh, 'retry': this.retry, 'expire': this.expire, 'minimum': this.minimum,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Directory_Zone_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Directory_Zone_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Directory_Zone_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Directory_Zone_delete( this.id );
  }


}

export namespace Directory_Zone
{

}

// Model Entry from Directory at /api/v1/Directory/Entry
/*
Entry(id, zone, type, name, priority, weight, port, target, updated, created)
*/
export class Directory_Entry
{
  private _service: Contractor;

  public id: number = 0;

  public zone: Directory_Zone | undefined = undefined;
  public type: 'MX' | 'SRV' | 'CNAME' | 'TXT' | undefined = undefined;
  public name: string | undefined = undefined;
  public priority: number | undefined = undefined;
  public weight: number | undefined = undefined;
  public port: number | undefined = undefined;
  public target: string | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Directory_Entry> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Directory_Entry;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Directory/Entry' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Directory/Entry:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Directory_Entry> ): void
  {
    data = data as Directory_Entry;
    this.zone = data.zone !== null ? new Directory_Zone( this._service, data.zone ) : undefined;
    this.type = data.type;
    this.name = data.name;
    this.priority = data.priority;
    this.weight = data.weight;
    this.port = data.port;
    this.target = data.target;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'zone': this.zone !== undefined ? this.zone.toURL() : undefined, 'type': this.type, 'name': this.name, 'priority': this.priority, 'weight': this.weight, 'port': this.port, 'target': this.target,  };

    return { 'zone': this.zone !== undefined ? this.zone.toURL() : undefined, 'type': this.type, 'name': this.name, 'priority': this.priority, 'weight': this.weight, 'port': this.port, 'target': this.target,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Directory_Entry_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Directory_Entry_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Directory_Entry_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Directory_Entry_delete( this.id );
  }


}

export namespace Directory_Entry
{

  export class _ListFilter_zone implements ListFilter
  {
    public zone: Directory_Zone;

    constructor( zone: Directory_Zone )
    {
      this.zone = zone;
    }

  };


}

// Model Networked from Utilities at /api/v1/Utilities/Networked
/*
Networked(id, hostname, site)
*/
export class Utilities_Networked
{
  private _service: Contractor;

  public id: number = 0;

  public hostname: string | undefined = undefined;
  public site: Site_Site | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_Networked> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_Networked;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/Networked' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/Networked:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_Networked> ): void
  {
    data = data as Utilities_Networked;
    this.hostname = data.hostname;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'hostname': this.hostname, 'site': this.site !== undefined ? this.site.toURL() : undefined,  };

    return { 'hostname': this.hostname, 'site': this.site !== undefined ? this.site.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Utilities_Networked_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Utilities_Networked_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Utilities_Networked_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Utilities_Networked_delete( this.id );
  }


}

export namespace Utilities_Networked
{

}

// Model AddressBlock from Utilities at /api/v1/Utilities/AddressBlock
/*
AddressBlock(id, name, site, subnet, prefix, gateway_offset, _max_address, updated, created)
*/
export class Utilities_AddressBlock
{
  private _service: Contractor;

  public id: number = 0;

  public name: string | undefined = undefined;
  public site: Site_Site | undefined = undefined;
  public subnet: string | undefined = undefined;
  public prefix: number | undefined = undefined;
  public gateway_offset: number | undefined = undefined;
  public _max_address: string | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public gateway: string | undefined = undefined;
  public netmask: string | undefined = undefined;
  public size: string | undefined = undefined;
  public isIpV4: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_AddressBlock> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_AddressBlock;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/AddressBlock' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/AddressBlock:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_AddressBlock> ): void
  {
    data = data as Utilities_AddressBlock;
    this.name = data.name;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.subnet = data.subnet;
    this.prefix = data.prefix;
    this.gateway_offset = data.gateway_offset;
    this._max_address = data._max_address;
    this.updated = data.updated;
    this.created = data.created;
    this.gateway = data.gateway;
    this.netmask = data.netmask;
    this.size = data.size;
    this.isIpV4 = data.isIpV4;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'subnet': this.subnet, 'prefix': this.prefix, 'gateway_offset': this.gateway_offset,  };

    return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'subnet': this.subnet, 'prefix': this.prefix, 'gateway_offset': this.gateway_offset,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Utilities_AddressBlock_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Utilities_AddressBlock_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Utilities_AddressBlock_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Utilities_AddressBlock_delete( this.id );
  }


  async _call_nextAddress(  networked: Utilities_Networked, interface_name: string, is_primary: boolean  ): Promise<Utilities_Address>
  {
    return this._service.Utilities_AddressBlock_call_nextAddress( this.id,  networked, interface_name, is_primary  );
  }


  async _call_usage(  ): Promise<{}>
  {
    return this._service.Utilities_AddressBlock_call_usage( this.id );
  }


  async _call_getWithNameSite(  site: Site_Site, name: string  ): Promise<Utilities_AddressBlock>
  {
    return this._service.Utilities_AddressBlock_call_getWithNameSite( site, name  );
  }


}

export namespace Utilities_AddressBlock
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model Network from Utilities at /api/v1/Utilities/Network
/*
Network(id, name, site, mtu, updated, created)
*/
export class Utilities_Network
{
  private _service: Contractor;

  public id: number = 0;

  public name: string | undefined = undefined;
  public site: Site_Site | undefined = undefined;
  public mtu: number | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public address_block_list: Utilities_AddressBlock[] | undefined = [];

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_Network> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_Network;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/Network' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/Network:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_Network> ): void
  {
    data = data as Utilities_Network;
    this.name = data.name;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.mtu = data.mtu;
    this.updated = data.updated;
    this.created = data.created;
    if( data.address_block_list !== undefined )
      this.address_block_list = data.address_block_list.map( ( uri: string ) => { return new Utilities_AddressBlock( this._service, uri ) } );
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'mtu': this.mtu, 'address_block_list': this.address_block_list !== undefined ? this.address_block_list.map( i => i.toURL() ) : undefined,  };

    return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'mtu': this.mtu, 'address_block_list': this.address_block_list !== undefined ? this.address_block_list.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Utilities_Network_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Utilities_Network_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Utilities_Network_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Utilities_Network_delete( this.id );
  }


  async _call_getWithNameSite(  site: Site_Site, name: string  ): Promise<Utilities_Network>
  {
    return this._service.Utilities_Network_call_getWithNameSite( site, name  );
  }


}

export namespace Utilities_Network
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model NetworkAddressBlock from Utilities at /api/v1/Utilities/NetworkAddressBlock
/*
NetworkAddressBlock(id, network, address_block, vlan, updated, created)
*/
export class Utilities_NetworkAddressBlock
{
  private _service: Contractor;

  public id: number = 0;

  public network: Utilities_Network | undefined = undefined;
  public address_block: Utilities_AddressBlock | undefined = undefined;
  public vlan: number | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_NetworkAddressBlock> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_NetworkAddressBlock;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/NetworkAddressBlock' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/NetworkAddressBlock:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_NetworkAddressBlock> ): void
  {
    data = data as Utilities_NetworkAddressBlock;
    this.network = data.network !== null ? new Utilities_Network( this._service, data.network ) : undefined;
    this.address_block = data.address_block !== null ? new Utilities_AddressBlock( this._service, data.address_block ) : undefined;
    this.vlan = data.vlan;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'network': this.network !== undefined ? this.network.toURL() : undefined, 'address_block': this.address_block !== undefined ? this.address_block.toURL() : undefined, 'vlan': this.vlan,  };

    return { 'network': this.network !== undefined ? this.network.toURL() : undefined, 'address_block': this.address_block !== undefined ? this.address_block.toURL() : undefined, 'vlan': this.vlan,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Utilities_NetworkAddressBlock_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Utilities_NetworkAddressBlock_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Utilities_NetworkAddressBlock_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Utilities_NetworkAddressBlock_delete( this.id );
  }


}

export namespace Utilities_NetworkAddressBlock
{

  export class _ListFilter_network implements ListFilter
  {
    public network: Utilities_Network;

    constructor( network: Utilities_Network )
    {
      this.network = network;
    }

  };


  export class _ListFilter_address_block implements ListFilter
  {
    public address_block: Utilities_AddressBlock;

    constructor( address_block: Utilities_AddressBlock )
    {
      this.address_block = address_block;
    }

  };


}

// Model NetworkInterface from Utilities at /api/v1/Utilities/NetworkInterface
/*
NetworkInterface(id, name, network, updated, created)
*/
export class Utilities_NetworkInterface
{
  private _service: Contractor;

  public id: number = 0;

  public name: string | undefined = undefined;
  public network: Utilities_Network | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_NetworkInterface> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_NetworkInterface;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/NetworkInterface' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/NetworkInterface:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_NetworkInterface> ): void
  {
    data = data as Utilities_NetworkInterface;
    this.name = data.name;
    this.network = data.network !== null ? new Utilities_Network( this._service, data.network ) : undefined;
    this.updated = data.updated;
    this.created = data.created;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'network': this.network !== undefined ? this.network.toURL() : undefined,  };

    return { 'name': this.name, 'network': this.network !== undefined ? this.network.toURL() : undefined,  };
  }


}

export namespace Utilities_NetworkInterface
{

}

// Model RealNetworkInterface from Utilities at /api/v1/Utilities/RealNetworkInterface
/*
RealNetworkInterface(id, name, network, updated, created, networkinterface_ptr, mac, foundation, is_provisioning, physical_location, link_name, pxe)
*/
export class Utilities_RealNetworkInterface
{
  private _service: Contractor;

  public id: number = 0;

  public name: string | undefined = undefined;
  public network: Utilities_Network | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public mac: string | undefined = undefined;
  public foundation: Building_Foundation | undefined = undefined;
  public is_provisioning: boolean | undefined = false;
  public physical_location: string | undefined = undefined;
  public link_name: string | undefined = undefined;
  public pxe: BluePrint_PXE | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_RealNetworkInterface> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_RealNetworkInterface;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/RealNetworkInterface' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/RealNetworkInterface:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_RealNetworkInterface> ): void
  {
    data = data as Utilities_RealNetworkInterface;
    this.name = data.name;
    this.network = data.network !== null ? new Utilities_Network( this._service, data.network ) : undefined;
    this.updated = data.updated;
    this.created = data.created;
    this.mac = data.mac;
    this.foundation = data.foundation !== null ? new Building_Foundation( this._service, data.foundation ) : undefined;
    this.is_provisioning = data.is_provisioning;
    this.physical_location = data.physical_location;
    this.link_name = data.link_name;
    this.pxe = data.pxe !== null ? new BluePrint_PXE( this._service, data.pxe ) : undefined;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'network': this.network !== undefined ? this.network.toURL() : undefined, 'mac': this.mac, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined, 'is_provisioning': this.is_provisioning, 'physical_location': this.physical_location, 'link_name': this.link_name, 'pxe': this.pxe !== undefined ? this.pxe.toURL() : undefined,  };

    return { 'name': this.name, 'network': this.network !== undefined ? this.network.toURL() : undefined, 'mac': this.mac, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined, 'is_provisioning': this.is_provisioning, 'physical_location': this.physical_location, 'link_name': this.link_name, 'pxe': this.pxe !== undefined ? this.pxe.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Utilities_RealNetworkInterface_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Utilities_RealNetworkInterface_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Utilities_RealNetworkInterface_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Utilities_RealNetworkInterface_delete( this.id );
  }


}

export namespace Utilities_RealNetworkInterface
{

  export class _ListFilter_foundation implements ListFilter
  {
    public foundation: Building_Foundation;

    constructor( foundation: Building_Foundation )
    {
      this.foundation = foundation;
    }

  };


}

// Model AbstractNetworkInterface from Utilities at /api/v1/Utilities/AbstractNetworkInterface
/*
AbstractNetworkInterface(id, name, network, updated, created, networkinterface_ptr, structure)
*/
export class Utilities_AbstractNetworkInterface
{
  private _service: Contractor;

  public id: number = 0;

  public name: string | undefined = undefined;
  public network: Utilities_Network | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public structure: Building_Structure | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_AbstractNetworkInterface> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_AbstractNetworkInterface;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/AbstractNetworkInterface' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/AbstractNetworkInterface:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_AbstractNetworkInterface> ): void
  {
    data = data as Utilities_AbstractNetworkInterface;
    this.name = data.name;
    this.network = data.network !== null ? new Utilities_Network( this._service, data.network ) : undefined;
    this.updated = data.updated;
    this.created = data.created;
    this.structure = data.structure !== null ? new Building_Structure( this._service, data.structure ) : undefined;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'network': this.network !== undefined ? this.network.toURL() : undefined, 'structure': this.structure !== undefined ? this.structure.toURL() : undefined,  };

    return { 'name': this.name, 'network': this.network !== undefined ? this.network.toURL() : undefined, 'structure': this.structure !== undefined ? this.structure.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Utilities_AbstractNetworkInterface_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Utilities_AbstractNetworkInterface_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Utilities_AbstractNetworkInterface_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Utilities_AbstractNetworkInterface_delete( this.id );
  }


}

export namespace Utilities_AbstractNetworkInterface
{

  export class _ListFilter_structure implements ListFilter
  {
    public structure: Building_Structure;

    constructor( structure: Building_Structure )
    {
      this.structure = structure;
    }

  };


}

// Model AggregatedNetworkInterface from Utilities at /api/v1/Utilities/AggregatedNetworkInterface
/*
AggregatedNetworkInterface(id, name, network, updated, created, networkinterface_ptr, structure, abstractnetworkinterface_ptr, primary_interface, parameters)
*/
export class Utilities_AggregatedNetworkInterface
{
  private _service: Contractor;

  public networkinterface_ptr: Utilities_NetworkInterface = undefined as unknown as Utilities_NetworkInterface;

  public name: string | undefined = undefined;
  public network: Utilities_Network | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public structure: Building_Structure | undefined = undefined;
  public primary_interface: Utilities_NetworkInterface | undefined = undefined;
  public parameters: {} | undefined = {};
  public secondary_interfaces: Utilities_NetworkInterface[] | undefined = [];
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_AggregatedNetworkInterface> | any )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_AggregatedNetworkInterface;
      this.networkinterface_ptr = source.networkinterface_ptr;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/AggregatedNetworkInterface' ) )
    {
      this.networkinterface_ptr = new Utilities_NetworkInterface( service, source.split( ':' )[ 1 ] );
    }
    else if( source instanceof Utilities_NetworkInterface )
    {
      this.networkinterface_ptr = source;
    }
  }

  toString(): string
  {
    return this.networkinterface_ptr.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/AggregatedNetworkInterface:' + this.networkinterface_ptr.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_AggregatedNetworkInterface> ): void
  {
    data = data as Utilities_AggregatedNetworkInterface;
    this.name = data.name;
    this.network = data.network !== null ? new Utilities_Network( this._service, data.network ) : undefined;
    this.updated = data.updated;
    this.created = data.created;
    this.structure = data.structure !== null ? new Building_Structure( this._service, data.structure ) : undefined;
    this.primary_interface = data.primary_interface !== null ? new Utilities_NetworkInterface( this._service, data.primary_interface ) : undefined;
    this.parameters = data.parameters;
    if( data.secondary_interfaces !== undefined )
      this.secondary_interfaces = data.secondary_interfaces.map( ( uri: string ) => { return new Utilities_NetworkInterface( this._service, uri ) } );
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'network': this.network !== undefined ? this.network.toURL() : undefined, 'structure': this.structure !== undefined ? this.structure.toURL() : undefined, 'primary_interface': this.primary_interface !== undefined ? this.primary_interface.toURL() : undefined, 'parameters': this.parameters, 'secondary_interfaces': this.secondary_interfaces !== undefined ? this.secondary_interfaces.map( i => i.toURL() ) : undefined,  };

    return { 'name': this.name, 'network': this.network !== undefined ? this.network.toURL() : undefined, 'structure': this.structure !== undefined ? this.structure.toURL() : undefined, 'primary_interface': this.primary_interface !== undefined ? this.primary_interface.toURL() : undefined, 'parameters': this.parameters, 'secondary_interfaces': this.secondary_interfaces !== undefined ? this.secondary_interfaces.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Utilities_AggregatedNetworkInterface_get( this.networkinterface_ptr );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Utilities_AggregatedNetworkInterface_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Utilities_AggregatedNetworkInterface_update( this.networkinterface_ptr, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Utilities_AggregatedNetworkInterface_delete( this.networkinterface_ptr );
  }


}

export namespace Utilities_AggregatedNetworkInterface
{

  export class _ListFilter_structure implements ListFilter
  {
    public structure: Building_Structure;

    constructor( structure: Building_Structure )
    {
      this.structure = structure;
    }

  };


}

// Model BaseAddress from Utilities at /api/v1/Utilities/BaseAddress
/*
BaseAddress(id, address_block, offset, updated, created)
*/
export class Utilities_BaseAddress
{
  private _service: Contractor;

  public id: number = 0;

  public address_block: Utilities_AddressBlock | undefined = undefined;
  public offset: number | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public type: string | undefined = undefined;
  public ip_address: string | undefined = undefined;
  public subnet: string | undefined = undefined;
  public netmask: string | undefined = undefined;
  public prefix: string | undefined = undefined;
  public gateway: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_BaseAddress> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_BaseAddress;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/BaseAddress' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/BaseAddress:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_BaseAddress> ): void
  {
    data = data as Utilities_BaseAddress;
    this.address_block = data.address_block !== null ? new Utilities_AddressBlock( this._service, data.address_block ) : undefined;
    this.offset = data.offset;
    this.updated = data.updated;
    this.created = data.created;
    this.type = data.type;
    this.ip_address = data.ip_address;
    this.subnet = data.subnet;
    this.netmask = data.netmask;
    this.prefix = data.prefix;
    this.gateway = data.gateway;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'address_block': this.address_block !== undefined ? this.address_block.toURL() : undefined, 'offset': this.offset,  };

    return { 'address_block': this.address_block !== undefined ? this.address_block.toURL() : undefined, 'offset': this.offset,  };
  }



  async _call_lookup(  ip_address: string, site: Site_Site  ): Promise<Utilities_BaseAddress>
  {
    return this._service.Utilities_BaseAddress_call_lookup( ip_address, site  );
  }


}

export namespace Utilities_BaseAddress
{

}

// Model Address from Utilities at /api/v1/Utilities/Address
/*
Address(id, address_block, offset, updated, created, baseaddress_ptr, networked, interface_name, alias_index, pointer, is_primary)
*/
export class Utilities_Address
{
  private _service: Contractor;

  public id: number = 0;

  public address_block: Utilities_AddressBlock | undefined = undefined;
  public offset: number | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public networked: Utilities_Networked | undefined = undefined;
  public interface_name: string | undefined = undefined;
  public alias_index: number | undefined = undefined;
  public pointer: Utilities_Address | undefined = undefined;
  public is_primary: boolean | undefined = false;
  public type: string | undefined = undefined;
  public ip_address: string | undefined = undefined;
  public subnet: string | undefined = undefined;
  public netmask: string | undefined = undefined;
  public prefix: string | undefined = undefined;
  public gateway: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_Address> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_Address;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/Address' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/Address:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_Address> ): void
  {
    data = data as Utilities_Address;
    this.address_block = data.address_block !== null ? new Utilities_AddressBlock( this._service, data.address_block ) : undefined;
    this.offset = data.offset;
    this.updated = data.updated;
    this.created = data.created;
    this.networked = data.networked !== null ? new Utilities_Networked( this._service, data.networked ) : undefined;
    this.interface_name = data.interface_name;
    this.alias_index = data.alias_index;
    this.pointer = data.pointer !== null ? new Utilities_Address( this._service, data.pointer ) : undefined;
    this.is_primary = data.is_primary;
    this.type = data.type;
    this.ip_address = data.ip_address;
    this.subnet = data.subnet;
    this.netmask = data.netmask;
    this.prefix = data.prefix;
    this.gateway = data.gateway;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'address_block': this.address_block !== undefined ? this.address_block.toURL() : undefined, 'offset': this.offset, 'networked': this.networked !== undefined ? this.networked.toURL() : undefined, 'interface_name': this.interface_name, 'alias_index': this.alias_index, 'pointer': this.pointer !== undefined ? this.pointer.toURL() : undefined, 'is_primary': this.is_primary,  };

    return { 'address_block': this.address_block !== undefined ? this.address_block.toURL() : undefined, 'offset': this.offset, 'networked': this.networked !== undefined ? this.networked.toURL() : undefined, 'interface_name': this.interface_name, 'alias_index': this.alias_index, 'pointer': this.pointer !== undefined ? this.pointer.toURL() : undefined, 'is_primary': this.is_primary,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Utilities_Address_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Utilities_Address_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Utilities_Address_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Utilities_Address_delete( this.id );
  }


}

export namespace Utilities_Address
{

  export class _ListFilter_address_block implements ListFilter
  {
    public address_block: Utilities_AddressBlock;

    constructor( address_block: Utilities_AddressBlock )
    {
      this.address_block = address_block;
    }

  };


  export class _ListFilter_structure implements ListFilter
  {
    public structure: Building_Structure;

    constructor( structure: Building_Structure )
    {
      this.structure = structure;
    }

  };


}

// Model ReservedAddress from Utilities at /api/v1/Utilities/ReservedAddress
/*
ReservedAddress(id, address_block, offset, updated, created, baseaddress_ptr, reason)
*/
export class Utilities_ReservedAddress
{
  private _service: Contractor;

  public id: number = 0;

  public address_block: Utilities_AddressBlock | undefined = undefined;
  public offset: number | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public reason: string | undefined = undefined;
  public type: string | undefined = undefined;
  public ip_address: string | undefined = undefined;
  public subnet: string | undefined = undefined;
  public netmask: string | undefined = undefined;
  public prefix: string | undefined = undefined;
  public gateway: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_ReservedAddress> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_ReservedAddress;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/ReservedAddress' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/ReservedAddress:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_ReservedAddress> ): void
  {
    data = data as Utilities_ReservedAddress;
    this.address_block = data.address_block !== null ? new Utilities_AddressBlock( this._service, data.address_block ) : undefined;
    this.offset = data.offset;
    this.updated = data.updated;
    this.created = data.created;
    this.reason = data.reason;
    this.type = data.type;
    this.ip_address = data.ip_address;
    this.subnet = data.subnet;
    this.netmask = data.netmask;
    this.prefix = data.prefix;
    this.gateway = data.gateway;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'address_block': this.address_block !== undefined ? this.address_block.toURL() : undefined, 'offset': this.offset, 'reason': this.reason,  };

    return { 'address_block': this.address_block !== undefined ? this.address_block.toURL() : undefined, 'offset': this.offset, 'reason': this.reason,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Utilities_ReservedAddress_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Utilities_ReservedAddress_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Utilities_ReservedAddress_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Utilities_ReservedAddress_delete( this.id );
  }


}

export namespace Utilities_ReservedAddress
{

  export class _ListFilter_address_block implements ListFilter
  {
    public address_block: Utilities_AddressBlock;

    constructor( address_block: Utilities_AddressBlock )
    {
      this.address_block = address_block;
    }

  };


}

// Model DynamicAddress from Utilities at /api/v1/Utilities/DynamicAddress
/*
DynamicAddress(id, address_block, offset, updated, created, baseaddress_ptr, pxe)
*/
export class Utilities_DynamicAddress
{
  private _service: Contractor;

  public id: number = 0;

  public address_block: Utilities_AddressBlock | undefined = undefined;
  public offset: number | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public pxe: BluePrint_PXE | undefined = undefined;
  public type: string | undefined = undefined;
  public ip_address: string | undefined = undefined;
  public subnet: string | undefined = undefined;
  public netmask: string | undefined = undefined;
  public prefix: string | undefined = undefined;
  public gateway: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Utilities_DynamicAddress> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Utilities_DynamicAddress;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Utilities/DynamicAddress' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Utilities/DynamicAddress:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Utilities_DynamicAddress> ): void
  {
    data = data as Utilities_DynamicAddress;
    this.address_block = data.address_block !== null ? new Utilities_AddressBlock( this._service, data.address_block ) : undefined;
    this.offset = data.offset;
    this.updated = data.updated;
    this.created = data.created;
    this.pxe = data.pxe !== null ? new BluePrint_PXE( this._service, data.pxe ) : undefined;
    this.type = data.type;
    this.ip_address = data.ip_address;
    this.subnet = data.subnet;
    this.netmask = data.netmask;
    this.prefix = data.prefix;
    this.gateway = data.gateway;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'address_block': this.address_block !== undefined ? this.address_block.toURL() : undefined, 'offset': this.offset, 'pxe': this.pxe !== undefined ? this.pxe.toURL() : undefined,  };

    return { 'address_block': this.address_block !== undefined ? this.address_block.toURL() : undefined, 'offset': this.offset, 'pxe': this.pxe !== undefined ? this.pxe.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Utilities_DynamicAddress_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Utilities_DynamicAddress_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Utilities_DynamicAddress_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Utilities_DynamicAddress_delete( this.id );
  }


}

export namespace Utilities_DynamicAddress
{

  export class _ListFilter_address_block implements ListFilter
  {
    public address_block: Utilities_AddressBlock;

    constructor( address_block: Utilities_AddressBlock )
    {
      this.address_block = address_block;
    }

  };


}

// Model Foundation from Building at /api/v1/Building/Foundation
/*
Foundation(locator, site, blueprint, id_map, located_at, built_at, updated, created)
*/
export class Building_Foundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;
  public structure: Building_Structure | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Building_Foundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Building_Foundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Building/Foundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/Building/Foundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<Building_Foundation> ): void
  {
    data = data as Building_Foundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
    this.structure = data.structure !== null ? new Building_Structure( this._service, data.structure ) : undefined;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Building_Foundation_get( this.locator );

    this._update( record );
  }

  async _save(): Promise<void>
  {
    return this._service.Building_Foundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Building_Foundation_delete( this.locator );
  }


  async _call_setIdMap(  id_map: {}  ): Promise<string>
  {
    return this._service.Building_Foundation_call_setIdMap( this.locator,  id_map  );
  }


  async _call_doCreate(  ): Promise<number>
  {
    return this._service.Building_Foundation_call_doCreate( this.locator );
  }


  async _call_doDestroy(  ): Promise<number>
  {
    return this._service.Building_Foundation_call_doDestroy( this.locator );
  }


  async _call_doJob(  name: string  ): Promise<number>
  {
    return this._service.Building_Foundation_call_doJob( this.locator,  name  );
  }


  async _call_getJob(  ): Promise<Foreman_FoundationJob>
  {
    return this._service.Building_Foundation_call_getJob( this.locator );
  }


  async _call_getFoundationTypes(  ): Promise<string[]>
  {
    return this._service.Building_Foundation_call_getFoundationTypes( );
  }


  async _call_getConfig(  ): Promise<{}>
  {
    return this._service.Building_Foundation_call_getConfig( this.locator );
  }


  async _call_getInterfaceList(  ): Promise<{}[]>
  {
    return this._service.Building_Foundation_call_getInterfaceList( this.locator );
  }


}

export namespace Building_Foundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


  export class _ListFilter_todo implements ListFilter
  {
    public site: Site_Site;
    public has_dependancies: boolean;
    public foundation_class: string;

    constructor( site: Site_Site, has_dependancies: boolean, foundation_class: string )
    {
      this.site = site;
      this.has_dependancies = has_dependancies;
      this.foundation_class = foundation_class;
    }

  };


}

// Model Structure from Building at /api/v1/Building/Structure
/*
Structure(id, hostname, site, networked_ptr, blueprint, foundation, config_uuid, config_values, built_at, updated, created)
*/
export class Building_Structure
{
  private _service: Contractor;

  public id: number = 0;

  public hostname: string | undefined = undefined;
  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_StructureBluePrint | undefined = undefined;
  public foundation: Building_Foundation | undefined = undefined;
  public config_uuid: string | undefined = '<undefined>';
  public config_values: {} | undefined = {};
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public state: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Building_Structure> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Building_Structure;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Building/Structure' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Building/Structure:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Building_Structure> ): void
  {
    data = data as Building_Structure;
    this.hostname = data.hostname;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_StructureBluePrint( this._service, data.blueprint ) : undefined;
    this.foundation = data.foundation !== null ? new Building_Foundation( this._service, data.foundation ) : undefined;
    this.config_uuid = data.config_uuid;
    this.config_values = data.config_values;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.state = data.state;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'hostname': this.hostname, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined, 'config_values': this.config_values,  };

    return { 'hostname': this.hostname, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined, 'config_values': this.config_values,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Building_Structure_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Building_Structure_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Building_Structure_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Building_Structure_delete( this.id );
  }


  async _call_doCreate(  ): Promise<number>
  {
    return this._service.Building_Structure_call_doCreate( this.id );
  }


  async _call_doDestroy(  ): Promise<number>
  {
    return this._service.Building_Structure_call_doDestroy( this.id );
  }


  async _call_doJob(  name: string  ): Promise<number>
  {
    return this._service.Building_Structure_call_doJob( this.id,  name  );
  }


  async _call_getJob(  ): Promise<Foreman_StructureJob>
  {
    return this._service.Building_Structure_call_getJob( this.id );
  }


  async _call_getConfig(  ): Promise<{}>
  {
    return this._service.Building_Structure_call_getConfig( this.id );
  }


  async _call_updateConfig(  config_value_map: {}  ): Promise<{}>
  {
    return this._service.Building_Structure_call_updateConfig( this.id,  config_value_map  );
  }


  async _call_getWithHostnameSite(  site: Site_Site, hostname: string  ): Promise<Building_Structure>
  {
    return this._service.Building_Structure_call_getWithHostnameSite( site, hostname  );
  }


}

export namespace Building_Structure
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model Complex from Building at /api/v1/Building/Complex
/*
Complex(name, site, description, built_percentage, updated, created)
*/
export class Building_Complex
{
  private _service: Contractor;

  public name: string = '';

  public site: Site_Site | undefined = undefined;
  public description: string | undefined = undefined;
  public built_percentage: number | undefined = 90;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public members: Building_Structure[] | undefined = [];
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Building_Complex> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Building_Complex;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Building/Complex' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Building/Complex:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Building_Complex> ): void
  {
    data = data as Building_Complex;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.description = data.description;
    this.built_percentage = data.built_percentage;
    this.updated = data.updated;
    this.created = data.created;
    if( data.members !== undefined )
      this.members = data.members.map( ( uri: string ) => { return new Building_Structure( this._service, uri ) } );
    this.state = data.state;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Building_Complex_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Building_Complex_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Building_Complex_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Building_Complex_delete( this.name );
  }


  async _call_createFoundation(  hostname: string, site: Site_Site, interface_map_list: {}[]  ): Promise<Building_Foundation>
  {
    return this._service.Building_Complex_call_createFoundation( this.name,  hostname, site, interface_map_list  );
  }


}

export namespace Building_Complex
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model ComplexStructure from Building at /api/v1/Building/ComplexStructure
/*
ComplexStructure(id, complex, structure, updated, created)
*/
export class Building_ComplexStructure
{
  private _service: Contractor;

  public id: number = 0;

  public complex: Building_Complex | undefined = undefined;
  public structure: Building_Structure | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Building_ComplexStructure> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Building_ComplexStructure;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Building/ComplexStructure' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Building/ComplexStructure:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Building_ComplexStructure> ): void
  {
    data = data as Building_ComplexStructure;
    this.complex = data.complex !== null ? new Building_Complex( this._service, data.complex ) : undefined;
    this.structure = data.structure !== null ? new Building_Structure( this._service, data.structure ) : undefined;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'complex': this.complex !== undefined ? this.complex.toURL() : undefined, 'structure': this.structure !== undefined ? this.structure.toURL() : undefined,  };

    return { 'complex': this.complex !== undefined ? this.complex.toURL() : undefined, 'structure': this.structure !== undefined ? this.structure.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Building_ComplexStructure_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Building_ComplexStructure_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Building_ComplexStructure_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Building_ComplexStructure_delete( this.id );
  }


  async _call_getConfig(  ): Promise<{}>
  {
    return this._service.Building_ComplexStructure_call_getConfig( this.id );
  }


}

export namespace Building_ComplexStructure
{

  export class _ListFilter_complex implements ListFilter
  {
    public complex: Building_Complex;

    constructor( complex: Building_Complex )
    {
      this.complex = complex;
    }

  };


}

// Model Dependency from Building at /api/v1/Building/Dependency
/*
Dependency(id, structure, dependency, foundation, script_structure, link, create_script_name, destroy_script_name, built_at, updated, created)
*/
export class Building_Dependency
{
  private _service: Contractor;

  public id: number = 0;

  public structure: Building_Structure | undefined = undefined;
  public dependency: Building_Dependency | undefined = undefined;
  public foundation: Building_Foundation | undefined = undefined;
  public script_structure: Building_Structure | undefined = undefined;
  public link: 'soft' | 'hard' | undefined = undefined;
  public create_script_name: string | undefined = undefined;
  public destroy_script_name: string | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public state: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Building_Dependency> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Building_Dependency;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Building/Dependency' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Building/Dependency:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Building_Dependency> ): void
  {
    data = data as Building_Dependency;
    this.structure = data.structure !== null ? new Building_Structure( this._service, data.structure ) : undefined;
    this.dependency = data.dependency !== null ? new Building_Dependency( this._service, data.dependency ) : undefined;
    this.foundation = data.foundation !== null ? new Building_Foundation( this._service, data.foundation ) : undefined;
    this.script_structure = data.script_structure !== null ? new Building_Structure( this._service, data.script_structure ) : undefined;
    this.link = data.link;
    this.create_script_name = data.create_script_name;
    this.destroy_script_name = data.destroy_script_name;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.state = data.state;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'structure': this.structure !== undefined ? this.structure.toURL() : undefined, 'dependency': this.dependency !== undefined ? this.dependency.toURL() : undefined, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined, 'script_structure': this.script_structure !== undefined ? this.script_structure.toURL() : undefined, 'link': this.link, 'create_script_name': this.create_script_name, 'destroy_script_name': this.destroy_script_name,  };

    return { 'structure': this.structure !== undefined ? this.structure.toURL() : undefined, 'dependency': this.dependency !== undefined ? this.dependency.toURL() : undefined, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined, 'script_structure': this.script_structure !== undefined ? this.script_structure.toURL() : undefined, 'link': this.link, 'create_script_name': this.create_script_name, 'destroy_script_name': this.destroy_script_name,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Building_Dependency_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Building_Dependency_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Building_Dependency_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Building_Dependency_delete( this.id );
  }


  async _call_getJob(  ): Promise<Foreman_DependencyJob>
  {
    return this._service.Building_Dependency_call_getJob( this.id );
  }


}

export namespace Building_Dependency
{

  export class _ListFilter_foundation implements ListFilter
  {
    public foundation: Building_Foundation;

    constructor( foundation: Building_Foundation )
    {
      this.foundation = foundation;
    }

  };


  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model BaseJob from Foreman at /api/v1/Foreman/BaseJob
/*
BaseJob(id, site, state, status, message, script_runner, script_name, updated, created)
*/
export class Foreman_BaseJob
{
  private _service: Contractor;

  public id: number = 0;

  public site: Site_Site | undefined = undefined;
  public state: 'queued' | 'waiting' | 'done' | 'paused' | 'error' | 'aborted' | undefined = undefined;
  public status: string | undefined = '[]';
  public message: string | undefined = '';
  public script_name: string | undefined = 'False';
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public can_start: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Foreman_BaseJob> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Foreman_BaseJob;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Foreman/BaseJob' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Foreman/BaseJob:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Foreman_BaseJob> ): void
  {
    data = data as Foreman_BaseJob;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.state = data.state;
    this.status = data.status;
    this.message = data.message;
    this.script_name = data.script_name;
    this.updated = data.updated;
    this.created = data.created;
    this.can_start = data.can_start;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'state': this.state, 'status': this.status, 'message': this.message,  };

    return { 'state': this.state, 'status': this.status, 'message': this.message,  };
  }



  async _call_pause(  ): Promise<void>
  {
    return this._service.Foreman_BaseJob_call_pause( this.id );
  }


  async _call_resume(  ): Promise<void>
  {
    return this._service.Foreman_BaseJob_call_resume( this.id );
  }


  async _call_reset(  ): Promise<void>
  {
    return this._service.Foreman_BaseJob_call_reset( this.id );
  }


  async _call_rollback(  ): Promise<void>
  {
    return this._service.Foreman_BaseJob_call_rollback( this.id );
  }


  async _call_clearDispatched(  ): Promise<void>
  {
    return this._service.Foreman_BaseJob_call_clearDispatched( this.id );
  }


  async _call_jobStats(  site: Site_Site  ): Promise<{}>
  {
    return this._service.Foreman_BaseJob_call_jobStats( site  );
  }


  async _call_jobRunnerVariables(  ): Promise<{}>
  {
    return this._service.Foreman_BaseJob_call_jobRunnerVariables( this.id );
  }


  async _call_jobRunnerState(  ): Promise<{}>
  {
    return this._service.Foreman_BaseJob_call_jobRunnerState( this.id );
  }


  async _call_signalComplete(  cookie: string  ): Promise<string>
  {
    return this._service.Foreman_BaseJob_call_signalComplete( this.id,  cookie  );
  }


  async _call_signalAlert(  msg: string  ): Promise<string>
  {
    return this._service.Foreman_BaseJob_call_signalAlert( this.id,  msg  );
  }


  async _call_postMessage(  msg: string  ): Promise<string>
  {
    return this._service.Foreman_BaseJob_call_postMessage( this.id,  msg  );
  }


}

export namespace Foreman_BaseJob
{

}

// Model FoundationJob from Foreman at /api/v1/Foreman/FoundationJob
/*
FoundationJob(id, site, state, status, message, script_runner, script_name, updated, created, basejob_ptr, foundation)
*/
export class Foreman_FoundationJob
{
  private _service: Contractor;

  public id: number = 0;

  public site: Site_Site | undefined = undefined;
  public state: 'queued' | 'waiting' | 'done' | 'paused' | 'error' | 'aborted' | undefined = undefined;
  public status: string | undefined = '[]';
  public message: string | undefined = '';
  public script_name: string | undefined = 'False';
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public foundation: Building_Foundation | undefined = undefined;
  public can_start: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Foreman_FoundationJob> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Foreman_FoundationJob;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Foreman/FoundationJob' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Foreman/FoundationJob:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Foreman_FoundationJob> ): void
  {
    data = data as Foreman_FoundationJob;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.state = data.state;
    this.status = data.status;
    this.message = data.message;
    this.script_name = data.script_name;
    this.updated = data.updated;
    this.created = data.created;
    this.foundation = data.foundation !== null ? new Building_Foundation( this._service, data.foundation ) : undefined;
    this.can_start = data.can_start;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'state': this.state, 'status': this.status, 'message': this.message,  };

    return { 'state': this.state, 'status': this.status, 'message': this.message,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Foreman_FoundationJob_get( this.id );

    this._update( record );
  }


  async _call_pause(  ): Promise<void>
  {
    return this._service.Foreman_FoundationJob_call_pause( this.id );
  }


  async _call_resume(  ): Promise<void>
  {
    return this._service.Foreman_FoundationJob_call_resume( this.id );
  }


  async _call_reset(  ): Promise<void>
  {
    return this._service.Foreman_FoundationJob_call_reset( this.id );
  }


  async _call_rollback(  ): Promise<void>
  {
    return this._service.Foreman_FoundationJob_call_rollback( this.id );
  }


  async _call_jobRunnerVariables(  ): Promise<{}>
  {
    return this._service.Foreman_FoundationJob_call_jobRunnerVariables( this.id );
  }


  async _call_jobRunnerState(  ): Promise<{}>
  {
    return this._service.Foreman_FoundationJob_call_jobRunnerState( this.id );
  }


}

export namespace Foreman_FoundationJob
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model StructureJob from Foreman at /api/v1/Foreman/StructureJob
/*
StructureJob(id, site, state, status, message, script_runner, script_name, updated, created, basejob_ptr, structure)
*/
export class Foreman_StructureJob
{
  private _service: Contractor;

  public id: number = 0;

  public site: Site_Site | undefined = undefined;
  public state: 'queued' | 'waiting' | 'done' | 'paused' | 'error' | 'aborted' | undefined = undefined;
  public status: string | undefined = '[]';
  public message: string | undefined = '';
  public script_name: string | undefined = 'False';
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public structure: Building_Structure | undefined = undefined;
  public can_start: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Foreman_StructureJob> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Foreman_StructureJob;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Foreman/StructureJob' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Foreman/StructureJob:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Foreman_StructureJob> ): void
  {
    data = data as Foreman_StructureJob;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.state = data.state;
    this.status = data.status;
    this.message = data.message;
    this.script_name = data.script_name;
    this.updated = data.updated;
    this.created = data.created;
    this.structure = data.structure !== null ? new Building_Structure( this._service, data.structure ) : undefined;
    this.can_start = data.can_start;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'state': this.state, 'status': this.status, 'message': this.message,  };

    return { 'state': this.state, 'status': this.status, 'message': this.message,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Foreman_StructureJob_get( this.id );

    this._update( record );
  }


  async _call_pause(  ): Promise<void>
  {
    return this._service.Foreman_StructureJob_call_pause( this.id );
  }


  async _call_resume(  ): Promise<void>
  {
    return this._service.Foreman_StructureJob_call_resume( this.id );
  }


  async _call_reset(  ): Promise<void>
  {
    return this._service.Foreman_StructureJob_call_reset( this.id );
  }


  async _call_rollback(  ): Promise<void>
  {
    return this._service.Foreman_StructureJob_call_rollback( this.id );
  }


  async _call_jobRunnerVariables(  ): Promise<{}>
  {
    return this._service.Foreman_StructureJob_call_jobRunnerVariables( this.id );
  }


  async _call_jobRunnerState(  ): Promise<{}>
  {
    return this._service.Foreman_StructureJob_call_jobRunnerState( this.id );
  }


}

export namespace Foreman_StructureJob
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model DependencyJob from Foreman at /api/v1/Foreman/DependencyJob
/*
DependencyJob(id, site, state, status, message, script_runner, script_name, updated, created, basejob_ptr, dependency)
*/
export class Foreman_DependencyJob
{
  private _service: Contractor;

  public id: number = 0;

  public site: Site_Site | undefined = undefined;
  public state: 'queued' | 'waiting' | 'done' | 'paused' | 'error' | 'aborted' | undefined = undefined;
  public status: string | undefined = '[]';
  public message: string | undefined = '';
  public script_name: string | undefined = 'False';
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public dependency: Building_Dependency | undefined = undefined;
  public c: string | undefined = undefined;
  public a: string | undefined = undefined;
  public n: string | undefined = undefined;
  public _: string | undefined = undefined;
  public s: string | undefined = undefined;
  public t: string | undefined = undefined;
  public r: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Foreman_DependencyJob> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Foreman_DependencyJob;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Foreman/DependencyJob' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Foreman/DependencyJob:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Foreman_DependencyJob> ): void
  {
    data = data as Foreman_DependencyJob;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.state = data.state;
    this.status = data.status;
    this.message = data.message;
    this.script_name = data.script_name;
    this.updated = data.updated;
    this.created = data.created;
    this.dependency = data.dependency !== null ? new Building_Dependency( this._service, data.dependency ) : undefined;
    this.c = data.c;
    this.a = data.a;
    this.n = data.n;
    this._ = data._;
    this.s = data.s;
    this.t = data.t;
    this.r = data.r;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'state': this.state, 'status': this.status, 'message': this.message,  };

    return { 'state': this.state, 'status': this.status, 'message': this.message,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Foreman_DependencyJob_get( this.id );

    this._update( record );
  }


  async _call_pause(  ): Promise<void>
  {
    return this._service.Foreman_DependencyJob_call_pause( this.id );
  }


  async _call_resume(  ): Promise<void>
  {
    return this._service.Foreman_DependencyJob_call_resume( this.id );
  }


  async _call_reset(  ): Promise<void>
  {
    return this._service.Foreman_DependencyJob_call_reset( this.id );
  }


  async _call_rollback(  ): Promise<void>
  {
    return this._service.Foreman_DependencyJob_call_rollback( this.id );
  }


  async _call_jobRunnerVariables(  ): Promise<{}>
  {
    return this._service.Foreman_DependencyJob_call_jobRunnerVariables( this.id );
  }


  async _call_jobRunnerState(  ): Promise<{}>
  {
    return this._service.Foreman_DependencyJob_call_jobRunnerState( this.id );
  }


}

export namespace Foreman_DependencyJob
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model JobLog from Foreman at /api/v1/Foreman/JobLog
/*
JobLog(id, site, job_id, target_id, target_class, target_description, script_name, creator, started_at, finished_at, canceled_by, canceled_at, updated, created)
*/
export class Foreman_JobLog
{
  private _service: Contractor;

  public id: number = 0;

  public site: Site_Site | undefined = undefined;
  public job_id: number | undefined = undefined;
  public target_id: string | undefined = undefined;
  public target_class: string | undefined = undefined;
  public target_description: string | undefined = undefined;
  public script_name: string | undefined = undefined;
  public creator: string | undefined = undefined;
  public started_at: Date | undefined = undefined;
  public finished_at: Date | undefined = undefined;
  public canceled_by: string | undefined = undefined;
  public canceled_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Foreman_JobLog> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Foreman_JobLog;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Foreman/JobLog' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Foreman/JobLog:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Foreman_JobLog> ): void
  {
    data = data as Foreman_JobLog;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.job_id = data.job_id;
    this.target_id = data.target_id;
    this.target_class = data.target_class;
    this.target_description = data.target_description;
    this.script_name = data.script_name;
    this.creator = data.creator;
    this.started_at = data.started_at;
    this.finished_at = data.finished_at;
    this.canceled_by = data.canceled_by;
    this.canceled_at = data.canceled_at;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'job_id': this.job_id, 'target_id': this.target_id, 'target_class': this.target_class, 'target_description': this.target_description, 'script_name': this.script_name, 'creator': this.creator, 'started_at': this.started_at, 'finished_at': this.finished_at, 'canceled_by': this.canceled_by, 'canceled_at': this.canceled_at,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'job_id': this.job_id, 'target_id': this.target_id, 'target_class': this.target_class, 'target_description': this.target_description, 'script_name': this.script_name, 'creator': this.creator, 'started_at': this.started_at, 'finished_at': this.finished_at, 'canceled_by': this.canceled_by, 'canceled_at': this.canceled_at,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Foreman_JobLog_get( this.id );

    this._update( record );
  }

}

export namespace Foreman_JobLog
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


  export class _ListFilter_job implements ListFilter
  {
    public job_id: number;

    constructor( job_id: number )
    {
      this.job_id = job_id;
    }

  };


  export class _ListFilter_structure implements ListFilter
  {
    public structure: Building_Structure;

    constructor( structure: Building_Structure )
    {
      this.structure = structure;
    }

  };


  export class _ListFilter_foundation implements ListFilter
  {
    public foundation: Building_Foundation;

    constructor( foundation: Building_Foundation )
    {
      this.foundation = foundation;
    }

  };


  export class _ListFilter_dependency implements ListFilter
  {
    public dependency: Building_Dependency;

    constructor( dependency: Building_Dependency )
    {
      this.dependency = dependency;
    }

  };


}

// Model Dispatch from SubContractor at /api/v1/SubContractor/Dispatch
/*

*/
export class SubContractor_Dispatch
{
  private _service: Contractor;



  constructor( service: Contractor, source?: ModelConstructorSource<SubContractor_Dispatch> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as SubContractor_Dispatch;
      this._update( source );
    }
  }

  _update( data: ModelUpdate<SubContractor_Dispatch> ): void
  {
    data = data as SubContractor_Dispatch;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return {  };

    return {  };
  }



  async _call_getJobs(  site: Site_Site, module_list: string[], max_jobs: number  ): Promise<{}[]>
  {
    return this._service.SubContractor_Dispatch_call_getJobs( site, module_list, max_jobs  );
  }


  async _call_jobResults(  job_id: number, cookie: string, data: {}  ): Promise<string>
  {
    return this._service.SubContractor_Dispatch_call_jobResults( job_id, cookie, data  );
  }


  async _call_jobError(  job_id: number, cookie: string, msg: string  ): Promise<void>
  {
    return this._service.SubContractor_Dispatch_call_jobError( job_id, cookie, msg  );
  }


}

export namespace SubContractor_Dispatch
{

}

// Model DHCPd from SubContractor at /api/v1/SubContractor/DHCPd
/*

*/
export class SubContractor_DHCPd
{
  private _service: Contractor;



  constructor( service: Contractor, source?: ModelConstructorSource<SubContractor_DHCPd> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as SubContractor_DHCPd;
      this._update( source );
    }
  }

  _update( data: ModelUpdate<SubContractor_DHCPd> ): void
  {
    data = data as SubContractor_DHCPd;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return {  };

    return {  };
  }



  async _call_getDynamicPools(  site: Site_Site  ): Promise<{}[]>
  {
    return this._service.SubContractor_DHCPd_call_getDynamicPools( site  );
  }


  async _call_getStaticPools(  site: Site_Site  ): Promise<{}>
  {
    return this._service.SubContractor_DHCPd_call_getStaticPools( site  );
  }


}

export namespace SubContractor_DHCPd
{

}

// Model FoundationPost from PostOffice at /api/v1/PostOffice/FoundationPost
/*
FoundationPost(id, name, updated, created, foundation)
*/
export class PostOffice_FoundationPost
{
  private _service: Contractor;

  public id: number = 0;

  public name: string | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public foundation: Building_Foundation | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<PostOffice_FoundationPost> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as PostOffice_FoundationPost;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/PostOffice/FoundationPost' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/PostOffice/FoundationPost:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<PostOffice_FoundationPost> ): void
  {
    data = data as PostOffice_FoundationPost;
    this.name = data.name;
    this.updated = data.updated;
    this.created = data.created;
    this.foundation = data.foundation !== null ? new Building_Foundation( this._service, data.foundation ) : undefined;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined,  };

    return { 'name': this.name, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.PostOffice_FoundationPost_get( this.id );

    this._update( record );
  }


}

export namespace PostOffice_FoundationPost
{

}

// Model StructurePost from PostOffice at /api/v1/PostOffice/StructurePost
/*
StructurePost(id, name, updated, created, structure)
*/
export class PostOffice_StructurePost
{
  private _service: Contractor;

  public id: number = 0;

  public name: string | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public structure: Building_Structure | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<PostOffice_StructurePost> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as PostOffice_StructurePost;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/PostOffice/StructurePost' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/PostOffice/StructurePost:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<PostOffice_StructurePost> ): void
  {
    data = data as PostOffice_StructurePost;
    this.name = data.name;
    this.updated = data.updated;
    this.created = data.created;
    this.structure = data.structure !== null ? new Building_Structure( this._service, data.structure ) : undefined;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'structure': this.structure !== undefined ? this.structure.toURL() : undefined,  };

    return { 'name': this.name, 'structure': this.structure !== undefined ? this.structure.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.PostOffice_StructurePost_get( this.id );

    this._update( record );
  }


}

export namespace PostOffice_StructurePost
{

}

// Model FoundationBox from PostOffice at /api/v1/PostOffice/FoundationBox
/*
FoundationBox(id, url, proxy, type, one_shot, extra_data, expires, updated, created, foundation)
*/
export class PostOffice_FoundationBox
{
  private _service: Contractor;

  public id: number = 0;

  public url: string | undefined = undefined;
  public proxy: string | undefined = undefined;
  public type: 'post' | 'call' | undefined = undefined;
  public one_shot: boolean | undefined = true;
  public extra_data: {} | undefined = {};
  public expires: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public foundation: Building_Foundation | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<PostOffice_FoundationBox> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as PostOffice_FoundationBox;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/PostOffice/FoundationBox' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/PostOffice/FoundationBox:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<PostOffice_FoundationBox> ): void
  {
    data = data as PostOffice_FoundationBox;
    this.url = data.url;
    this.proxy = data.proxy;
    this.type = data.type;
    this.one_shot = data.one_shot;
    this.extra_data = data.extra_data;
    this.expires = data.expires;
    this.updated = data.updated;
    this.created = data.created;
    this.foundation = data.foundation !== null ? new Building_Foundation( this._service, data.foundation ) : undefined;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'url': this.url, 'proxy': this.proxy, 'type': this.type, 'one_shot': this.one_shot, 'extra_data': this.extra_data, 'expires': this.expires, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined,  };

    return { 'url': this.url, 'proxy': this.proxy, 'type': this.type, 'one_shot': this.one_shot, 'extra_data': this.extra_data, 'expires': this.expires, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.PostOffice_FoundationBox_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.PostOffice_FoundationBox_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.PostOffice_FoundationBox_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.PostOffice_FoundationBox_delete( this.id );
  }


  async _call_extend(  additional_hours: number  ): Promise<void>
  {
    return this._service.PostOffice_FoundationBox_call_extend( this.id,  additional_hours  );
  }


}

export namespace PostOffice_FoundationBox
{

}

// Model StructureBox from PostOffice at /api/v1/PostOffice/StructureBox
/*
StructureBox(id, url, proxy, type, one_shot, extra_data, expires, updated, created, structure)
*/
export class PostOffice_StructureBox
{
  private _service: Contractor;

  public id: number = 0;

  public url: string | undefined = undefined;
  public proxy: string | undefined = undefined;
  public type: 'post' | 'call' | undefined = undefined;
  public one_shot: boolean | undefined = true;
  public extra_data: {} | undefined = {};
  public expires: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public structure: Building_Structure | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<PostOffice_StructureBox> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as PostOffice_StructureBox;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/PostOffice/StructureBox' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/PostOffice/StructureBox:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<PostOffice_StructureBox> ): void
  {
    data = data as PostOffice_StructureBox;
    this.url = data.url;
    this.proxy = data.proxy;
    this.type = data.type;
    this.one_shot = data.one_shot;
    this.extra_data = data.extra_data;
    this.expires = data.expires;
    this.updated = data.updated;
    this.created = data.created;
    this.structure = data.structure !== null ? new Building_Structure( this._service, data.structure ) : undefined;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'url': this.url, 'proxy': this.proxy, 'type': this.type, 'one_shot': this.one_shot, 'extra_data': this.extra_data, 'expires': this.expires, 'structure': this.structure !== undefined ? this.structure.toURL() : undefined,  };

    return { 'url': this.url, 'proxy': this.proxy, 'type': this.type, 'one_shot': this.one_shot, 'extra_data': this.extra_data, 'expires': this.expires, 'structure': this.structure !== undefined ? this.structure.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.PostOffice_StructureBox_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.PostOffice_StructureBox_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.PostOffice_StructureBox_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.PostOffice_StructureBox_delete( this.id );
  }


  async _call_extend(  additional_hours: number  ): Promise<void>
  {
    return this._service.PostOffice_StructureBox_call_extend( this.id,  additional_hours  );
  }


}

export namespace PostOffice_StructureBox
{

}

// Model Recorder from Records at /api/v1/Records/Recorder
/*

*/
export class Records_Recorder
{
  private _service: Contractor;



  constructor( service: Contractor, source?: ModelConstructorSource<Records_Recorder> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Records_Recorder;
      this._update( source );
    }
  }

  _update( data: ModelUpdate<Records_Recorder> ): void
  {
    data = data as Records_Recorder;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return {  };

    return {  };
  }



  async _call_query(  group: 'Site' | 'BluePrint' | 'Structure' | 'Foundation', query: string, fields: string, max_results: number  ): Promise<string[]>
  {
    return this._service.Records_Recorder_call_query( group, query, fields, max_results  );
  }


  async _call_queryObjects(  group: 'Site' | 'BluePrint' | 'Structure' | 'Foundation', query: string, max_results: number  ): Promise<string>
  {
    return this._service.Records_Recorder_call_queryObjects( group, query, max_results  );
  }


}

export namespace Records_Recorder
{

}

// Model VirtualBoxComplex from VirtualBox at /api/v1/VirtualBox/VirtualBoxComplex
/*
Complex for VirtualBox.  Only one member is allowed.  The ip address
for managing the VirtualBox instance comes from the member's primary ip.
*/
export class VirtualBox_VirtualBoxComplex
{
  private _service: Contractor;

  public name: string = '';

  public site: Site_Site | undefined = undefined;
  public description: string | undefined = undefined;
  public built_percentage: number | undefined = 90;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public virtualbox_username: string | undefined = undefined;
  public virtualbox_password: string | undefined = undefined;
  public members: Building_Structure[] | undefined = [];
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<VirtualBox_VirtualBoxComplex> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as VirtualBox_VirtualBoxComplex;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/VirtualBox/VirtualBoxComplex' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/VirtualBox/VirtualBoxComplex:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<VirtualBox_VirtualBoxComplex> ): void
  {
    data = data as VirtualBox_VirtualBoxComplex;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.description = data.description;
    this.built_percentage = data.built_percentage;
    this.updated = data.updated;
    this.created = data.created;
    this.virtualbox_username = data.virtualbox_username;
    this.virtualbox_password = data.virtualbox_password;
    if( data.members !== undefined )
      this.members = data.members.map( ( uri: string ) => { return new Building_Structure( this._service, uri ) } );
    this.state = data.state;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'virtualbox_username': this.virtualbox_username, 'virtualbox_password': this.virtualbox_password, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'virtualbox_username': this.virtualbox_username, 'virtualbox_password': this.virtualbox_password, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.VirtualBox_VirtualBoxComplex_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.VirtualBox_VirtualBoxComplex_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.VirtualBox_VirtualBoxComplex_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.VirtualBox_VirtualBoxComplex_delete( this.name );
  }


}

export namespace VirtualBox_VirtualBoxComplex
{

}

// Model VirtualBoxFoundation from VirtualBox at /api/v1/VirtualBox/VirtualBoxFoundation
/*
Foundation for use with VirtualBoxComplex.  Foundation instances are tracked
by the internal VirtualBox Hardware UUID.
*/
export class VirtualBox_VirtualBoxFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public virtualbox_complex: VirtualBox_VirtualBoxComplex | undefined = undefined;
  public virtualbox_uuid: string | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<VirtualBox_VirtualBoxFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as VirtualBox_VirtualBoxFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/VirtualBox/VirtualBoxFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/VirtualBox/VirtualBoxFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<VirtualBox_VirtualBoxFoundation> ): void
  {
    data = data as VirtualBox_VirtualBoxFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.virtualbox_complex = data.virtualbox_complex !== null ? new VirtualBox_VirtualBoxComplex( this._service, data.virtualbox_complex ) : undefined;
    this.virtualbox_uuid = data.virtualbox_uuid;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'virtualbox_complex': this.virtualbox_complex !== undefined ? this.virtualbox_complex.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'virtualbox_complex': this.virtualbox_complex !== undefined ? this.virtualbox_complex.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.VirtualBox_VirtualBoxFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.VirtualBox_VirtualBoxFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.VirtualBox_VirtualBoxFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.VirtualBox_VirtualBoxFoundation_delete( this.locator );
  }


}

export namespace VirtualBox_VirtualBoxFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model LibVirtComplex from LibVirt at /api/v1/LibVirt/LibVirtComplex
/*
Complex for LibVirt.  Only one member is allowed.  The ip address
for managing the LibVirt instance comes from the member's primary ip.
*/
export class LibVirt_LibVirtComplex
{
  private _service: Contractor;

  public name: string = '';

  public site: Site_Site | undefined = undefined;
  public description: string | undefined = undefined;
  public built_percentage: number | undefined = 90;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public members: Building_Structure[] | undefined = [];
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<LibVirt_LibVirtComplex> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as LibVirt_LibVirtComplex;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/LibVirt/LibVirtComplex' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/LibVirt/LibVirtComplex:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<LibVirt_LibVirtComplex> ): void
  {
    data = data as LibVirt_LibVirtComplex;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.description = data.description;
    this.built_percentage = data.built_percentage;
    this.updated = data.updated;
    this.created = data.created;
    if( data.members !== undefined )
      this.members = data.members.map( ( uri: string ) => { return new Building_Structure( this._service, uri ) } );
    this.state = data.state;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.LibVirt_LibVirtComplex_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.LibVirt_LibVirtComplex_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.LibVirt_LibVirtComplex_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.LibVirt_LibVirtComplex_delete( this.name );
  }


}

export namespace LibVirt_LibVirtComplex
{

}

// Model LibVirtFoundation from LibVirt at /api/v1/LibVirt/LibVirtFoundation
/*
Foundation for use with LibVirtComplex.  Foundation instances are tracked
by the LibVirt UUID.
*/
export class LibVirt_LibVirtFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public libvirt_complex: LibVirt_LibVirtComplex | undefined = undefined;
  public libvirt_uuid: string | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<LibVirt_LibVirtFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as LibVirt_LibVirtFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/LibVirt/LibVirtFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/LibVirt/LibVirtFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<LibVirt_LibVirtFoundation> ): void
  {
    data = data as LibVirt_LibVirtFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.libvirt_complex = data.libvirt_complex !== null ? new LibVirt_LibVirtComplex( this._service, data.libvirt_complex ) : undefined;
    this.libvirt_uuid = data.libvirt_uuid;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'libvirt_complex': this.libvirt_complex !== undefined ? this.libvirt_complex.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'libvirt_complex': this.libvirt_complex !== undefined ? this.libvirt_complex.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.LibVirt_LibVirtFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.LibVirt_LibVirtFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.LibVirt_LibVirtFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.LibVirt_LibVirtFoundation_delete( this.locator );
  }


}

export namespace LibVirt_LibVirtFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model VCenterComplex from VCenter at /api/v1/VCenter/VCenterComplex
/*
VCenterComplex(name, site, description, built_percentage, updated, created, complex_ptr, vcenter_host, vcenter_username, vcenter_password, vcenter_datacenter, vcenter_cluster)
*/
export class VCenter_VCenterComplex
{
  private _service: Contractor;

  public name: string = '';

  public site: Site_Site | undefined = undefined;
  public description: string | undefined = undefined;
  public built_percentage: number | undefined = 90;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public vcenter_host: Building_Structure | undefined = undefined;
  public vcenter_username: string | undefined = undefined;
  public vcenter_password: string | undefined = undefined;
  public vcenter_datacenter: string | undefined = undefined;
  public vcenter_cluster: string | undefined = undefined;
  public members: Building_Structure[] | undefined = [];
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<VCenter_VCenterComplex> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as VCenter_VCenterComplex;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/VCenter/VCenterComplex' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/VCenter/VCenterComplex:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<VCenter_VCenterComplex> ): void
  {
    data = data as VCenter_VCenterComplex;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.description = data.description;
    this.built_percentage = data.built_percentage;
    this.updated = data.updated;
    this.created = data.created;
    this.vcenter_host = data.vcenter_host !== null ? new Building_Structure( this._service, data.vcenter_host ) : undefined;
    this.vcenter_username = data.vcenter_username;
    this.vcenter_password = data.vcenter_password;
    this.vcenter_datacenter = data.vcenter_datacenter;
    this.vcenter_cluster = data.vcenter_cluster;
    if( data.members !== undefined )
      this.members = data.members.map( ( uri: string ) => { return new Building_Structure( this._service, uri ) } );
    this.state = data.state;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'vcenter_host': this.vcenter_host !== undefined ? this.vcenter_host.toURL() : undefined, 'vcenter_username': this.vcenter_username, 'vcenter_password': this.vcenter_password, 'vcenter_datacenter': this.vcenter_datacenter, 'vcenter_cluster': this.vcenter_cluster, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'vcenter_host': this.vcenter_host !== undefined ? this.vcenter_host.toURL() : undefined, 'vcenter_username': this.vcenter_username, 'vcenter_password': this.vcenter_password, 'vcenter_datacenter': this.vcenter_datacenter, 'vcenter_cluster': this.vcenter_cluster, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.VCenter_VCenterComplex_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.VCenter_VCenterComplex_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.VCenter_VCenterComplex_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.VCenter_VCenterComplex_delete( this.name );
  }


}

export namespace VCenter_VCenterComplex
{

}

// Model VCenterFoundation from VCenter at /api/v1/VCenter/VCenterFoundation
/*
VCenterFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, vcenter_complex, vcenter_uuid)
*/
export class VCenter_VCenterFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public vcenter_complex: VCenter_VCenterComplex | undefined = undefined;
  public vcenter_uuid: string | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<VCenter_VCenterFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as VCenter_VCenterFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/VCenter/VCenterFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/VCenter/VCenterFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<VCenter_VCenterFoundation> ): void
  {
    data = data as VCenter_VCenterFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.vcenter_complex = data.vcenter_complex !== null ? new VCenter_VCenterComplex( this._service, data.vcenter_complex ) : undefined;
    this.vcenter_uuid = data.vcenter_uuid;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'vcenter_complex': this.vcenter_complex !== undefined ? this.vcenter_complex.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'vcenter_complex': this.vcenter_complex !== undefined ? this.vcenter_complex.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.VCenter_VCenterFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.VCenter_VCenterFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.VCenter_VCenterFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.VCenter_VCenterFoundation_delete( this.locator );
  }


}

export namespace VCenter_VCenterFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model DockerComplex from Docker at /api/v1/Docker/DockerComplex
/*
DockerComplex(name, site, description, built_percentage, updated, created, complex_ptr)
*/
export class Docker_DockerComplex
{
  private _service: Contractor;

  public name: string = '';

  public site: Site_Site | undefined = undefined;
  public description: string | undefined = undefined;
  public built_percentage: number | undefined = 90;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public members: Building_Structure[] | undefined = [];
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Docker_DockerComplex> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Docker_DockerComplex;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Docker/DockerComplex' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Docker/DockerComplex:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Docker_DockerComplex> ): void
  {
    data = data as Docker_DockerComplex;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.description = data.description;
    this.built_percentage = data.built_percentage;
    this.updated = data.updated;
    this.created = data.created;
    if( data.members !== undefined )
      this.members = data.members.map( ( uri: string ) => { return new Building_Structure( this._service, uri ) } );
    this.state = data.state;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Docker_DockerComplex_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Docker_DockerComplex_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Docker_DockerComplex_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Docker_DockerComplex_delete( this.name );
  }


}

export namespace Docker_DockerComplex
{

}

// Model DockerFoundation from Docker at /api/v1/Docker/DockerFoundation
/*
DockerFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, docker_complex, docker_id)
*/
export class Docker_DockerFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public docker_complex: Docker_DockerComplex | undefined = undefined;
  public docker_id: string | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Docker_DockerFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Docker_DockerFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Docker/DockerFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/Docker/DockerFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<Docker_DockerFoundation> ): void
  {
    data = data as Docker_DockerFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.docker_complex = data.docker_complex !== null ? new Docker_DockerComplex( this._service, data.docker_complex ) : undefined;
    this.docker_id = data.docker_id;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'docker_complex': this.docker_complex !== undefined ? this.docker_complex.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'docker_complex': this.docker_complex !== undefined ? this.docker_complex.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Docker_DockerFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Docker_DockerFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Docker_DockerFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Docker_DockerFoundation_delete( this.locator );
  }


}

export namespace Docker_DockerFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model DockerPort from Docker at /api/v1/Docker/DockerPort
/*
DockerPort(id, complex, port, address_offset, foundation, foundation_index, updated, created)
*/
export class Docker_DockerPort
{
  private _service: Contractor;

  public id: number = 0;

  public complex: Docker_DockerComplex | undefined = undefined;
  public port: number | undefined = undefined;
  public address_offset: number | undefined = undefined;
  public foundation: Docker_DockerFoundation | undefined = undefined;
  public foundation_index: number | undefined = 0;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Docker_DockerPort> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Docker_DockerPort;
      this.id = source.id;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Docker/DockerPort' ) )
    {
      this.id = parseInt( source.split( ':' )[ 1 ] );
    }
    else if( typeof source === 'number' )
    {
      this.id = source;
    }
  }

  toString(): string
  {
    return this.id.toString();
  }

  toURL(): string
  {
    return '/api/v1/Docker/DockerPort:' + this.id.toString() + ':';
  }

  _update( data: ModelUpdate<Docker_DockerPort> ): void
  {
    data = data as Docker_DockerPort;
    this.complex = data.complex !== null ? new Docker_DockerComplex( this._service, data.complex ) : undefined;
    this.port = data.port;
    this.address_offset = data.address_offset;
    this.foundation = data.foundation !== null ? new Docker_DockerFoundation( this._service, data.foundation ) : undefined;
    this.foundation_index = data.foundation_index;
    this.updated = data.updated;
    this.created = data.created;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'complex': this.complex !== undefined ? this.complex.toURL() : undefined, 'port': this.port, 'address_offset': this.address_offset, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined, 'foundation_index': this.foundation_index,  };

    return { 'complex': this.complex !== undefined ? this.complex.toURL() : undefined, 'port': this.port, 'address_offset': this.address_offset, 'foundation': this.foundation !== undefined ? this.foundation.toURL() : undefined, 'foundation_index': this.foundation_index,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Docker_DockerPort_get( this.id );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Docker_DockerPort_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Docker_DockerPort_update( this.id, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Docker_DockerPort_delete( this.id );
  }


}

export namespace Docker_DockerPort
{

}

// Model IPMIFoundation from IPMI at /api/v1/IPMI/IPMIFoundation
/*
IPMIFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, ipmi_username, ipmi_password, ipmi_ip_address, ipmi_sol_port, plot)
*/
export class IPMI_IPMIFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public ipmi_username: string | undefined = undefined;
  public ipmi_password: string | undefined = undefined;
  public ipmi_ip_address: string | undefined = undefined;
  public ipmi_sol_port: 'console' | 'ttyS0' | 'ttyS1' | 'ttyS2' | 'ttyS3' | undefined = 'ttyS1';
  public plot: Survey_Plot | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<IPMI_IPMIFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as IPMI_IPMIFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/IPMI/IPMIFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/IPMI/IPMIFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<IPMI_IPMIFoundation> ): void
  {
    data = data as IPMI_IPMIFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.ipmi_username = data.ipmi_username;
    this.ipmi_password = data.ipmi_password;
    this.ipmi_ip_address = data.ipmi_ip_address;
    this.ipmi_sol_port = data.ipmi_sol_port;
    this.plot = data.plot !== null ? new Survey_Plot( this._service, data.plot ) : undefined;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'ipmi_username': this.ipmi_username, 'ipmi_password': this.ipmi_password, 'ipmi_ip_address': this.ipmi_ip_address, 'ipmi_sol_port': this.ipmi_sol_port, 'plot': this.plot !== undefined ? this.plot.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'ipmi_username': this.ipmi_username, 'ipmi_password': this.ipmi_password, 'ipmi_ip_address': this.ipmi_ip_address, 'ipmi_sol_port': this.ipmi_sol_port, 'plot': this.plot !== undefined ? this.plot.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.IPMI_IPMIFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.IPMI_IPMIFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.IPMI_IPMIFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.IPMI_IPMIFoundation_delete( this.locator );
  }


}

export namespace IPMI_IPMIFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model ManualComplex from Manual at /api/v1/Manual/ManualComplex
/*
ManualComplex(name, site, description, built_percentage, updated, created, complex_ptr)
*/
export class Manual_ManualComplex
{
  private _service: Contractor;

  public name: string = '';

  public site: Site_Site | undefined = undefined;
  public description: string | undefined = undefined;
  public built_percentage: number | undefined = 90;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public members: Building_Structure[] | undefined = [];
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Manual_ManualComplex> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Manual_ManualComplex;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Manual/ManualComplex' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Manual/ManualComplex:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Manual_ManualComplex> ): void
  {
    data = data as Manual_ManualComplex;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.description = data.description;
    this.built_percentage = data.built_percentage;
    this.updated = data.updated;
    this.created = data.created;
    if( data.members !== undefined )
      this.members = data.members.map( ( uri: string ) => { return new Building_Structure( this._service, uri ) } );
    this.state = data.state;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Manual_ManualComplex_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Manual_ManualComplex_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Manual_ManualComplex_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Manual_ManualComplex_delete( this.name );
  }


}

export namespace Manual_ManualComplex
{

}

// Model ManualFoundation from Manual at /api/v1/Manual/ManualFoundation
/*
ManualFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr)
*/
export class Manual_ManualFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Manual_ManualFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Manual_ManualFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Manual/ManualFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/Manual/ManualFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<Manual_ManualFoundation> ): void
  {
    data = data as Manual_ManualFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Manual_ManualFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Manual_ManualFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Manual_ManualFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Manual_ManualFoundation_delete( this.locator );
  }


}

export namespace Manual_ManualFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model ManualComplexedFoundation from Manual at /api/v1/Manual/ManualComplexedFoundation
/*
ManualComplexedFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, complex_host)
*/
export class Manual_ManualComplexedFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public complex_host: Manual_ManualComplex | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Manual_ManualComplexedFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Manual_ManualComplexedFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Manual/ManualComplexedFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/Manual/ManualComplexedFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<Manual_ManualComplexedFoundation> ): void
  {
    data = data as Manual_ManualComplexedFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.complex_host = data.complex_host !== null ? new Manual_ManualComplex( this._service, data.complex_host ) : undefined;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'complex_host': this.complex_host !== undefined ? this.complex_host.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'complex_host': this.complex_host !== undefined ? this.complex_host.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Manual_ManualComplexedFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Manual_ManualComplexedFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Manual_ManualComplexedFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Manual_ManualComplexedFoundation_delete( this.locator );
  }


}

export namespace Manual_ManualComplexedFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model ProxmoxComplex from Proxmox at /api/v1/Proxmox/ProxmoxComplex
/*
ProxmoxComplex(name, site, description, built_percentage, updated, created, complex_ptr, proxmox_host, proxmox_username, proxmox_password)
*/
export class Proxmox_ProxmoxComplex
{
  private _service: Contractor;

  public name: string = '';

  public site: Site_Site | undefined = undefined;
  public description: string | undefined = undefined;
  public built_percentage: number | undefined = 90;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public proxmox_host: Building_Structure | undefined = undefined;
  public proxmox_username: string | undefined = undefined;
  public proxmox_password: string | undefined = undefined;
  public members: Building_Structure[] | undefined = [];
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Proxmox_ProxmoxComplex> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Proxmox_ProxmoxComplex;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Proxmox/ProxmoxComplex' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Proxmox/ProxmoxComplex:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Proxmox_ProxmoxComplex> ): void
  {
    data = data as Proxmox_ProxmoxComplex;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.description = data.description;
    this.built_percentage = data.built_percentage;
    this.updated = data.updated;
    this.created = data.created;
    this.proxmox_host = data.proxmox_host !== null ? new Building_Structure( this._service, data.proxmox_host ) : undefined;
    this.proxmox_username = data.proxmox_username;
    this.proxmox_password = data.proxmox_password;
    if( data.members !== undefined )
      this.members = data.members.map( ( uri: string ) => { return new Building_Structure( this._service, uri ) } );
    this.state = data.state;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'proxmox_host': this.proxmox_host !== undefined ? this.proxmox_host.toURL() : undefined, 'proxmox_username': this.proxmox_username, 'proxmox_password': this.proxmox_password, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'proxmox_host': this.proxmox_host !== undefined ? this.proxmox_host.toURL() : undefined, 'proxmox_username': this.proxmox_username, 'proxmox_password': this.proxmox_password, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Proxmox_ProxmoxComplex_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Proxmox_ProxmoxComplex_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Proxmox_ProxmoxComplex_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Proxmox_ProxmoxComplex_delete( this.name );
  }


}

export namespace Proxmox_ProxmoxComplex
{

}

// Model ProxmoxFoundation from Proxmox at /api/v1/Proxmox/ProxmoxFoundation
/*
ProxmoxFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, proxmox_complex, proxmox_id)
*/
export class Proxmox_ProxmoxFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public proxmox_complex: Proxmox_ProxmoxComplex | undefined = undefined;
  public proxmox_id: number | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Proxmox_ProxmoxFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Proxmox_ProxmoxFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Proxmox/ProxmoxFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/Proxmox/ProxmoxFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<Proxmox_ProxmoxFoundation> ): void
  {
    data = data as Proxmox_ProxmoxFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.proxmox_complex = data.proxmox_complex !== null ? new Proxmox_ProxmoxComplex( this._service, data.proxmox_complex ) : undefined;
    this.proxmox_id = data.proxmox_id;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'proxmox_complex': this.proxmox_complex !== undefined ? this.proxmox_complex.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'proxmox_complex': this.proxmox_complex !== undefined ? this.proxmox_complex.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Proxmox_ProxmoxFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Proxmox_ProxmoxFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Proxmox_ProxmoxFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Proxmox_ProxmoxFoundation_delete( this.locator );
  }


}

export namespace Proxmox_ProxmoxFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model AzureComplex from Azure at /api/v1/Azure/AzureComplex
/*
AzureComplex(name, site, description, built_percentage, updated, created, complex_ptr, azure_subscription_id, azure_location, azure_resource_group, azure_client_id, azure_password, azure_tenant_id)
*/
export class Azure_AzureComplex
{
  private _service: Contractor;

  public name: string = '';

  public site: Site_Site | undefined = undefined;
  public description: string | undefined = undefined;
  public built_percentage: number | undefined = 90;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public azure_subscription_id: string | undefined = undefined;
  public azure_location: string | undefined = undefined;
  public azure_resource_group: string | undefined = undefined;
  public azure_client_id: string | undefined = undefined;
  public azure_password: string | undefined = undefined;
  public azure_tenant_id: string | undefined = undefined;
  public members: Building_Structure[] | undefined = [];
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Azure_AzureComplex> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Azure_AzureComplex;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Azure/AzureComplex' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Azure/AzureComplex:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Azure_AzureComplex> ): void
  {
    data = data as Azure_AzureComplex;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.description = data.description;
    this.built_percentage = data.built_percentage;
    this.updated = data.updated;
    this.created = data.created;
    this.azure_subscription_id = data.azure_subscription_id;
    this.azure_location = data.azure_location;
    this.azure_resource_group = data.azure_resource_group;
    this.azure_client_id = data.azure_client_id;
    this.azure_password = data.azure_password;
    this.azure_tenant_id = data.azure_tenant_id;
    if( data.members !== undefined )
      this.members = data.members.map( ( uri: string ) => { return new Building_Structure( this._service, uri ) } );
    this.state = data.state;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'azure_subscription_id': this.azure_subscription_id, 'azure_location': this.azure_location, 'azure_resource_group': this.azure_resource_group, 'azure_client_id': this.azure_client_id, 'azure_password': this.azure_password, 'azure_tenant_id': this.azure_tenant_id, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'azure_subscription_id': this.azure_subscription_id, 'azure_location': this.azure_location, 'azure_resource_group': this.azure_resource_group, 'azure_client_id': this.azure_client_id, 'azure_password': this.azure_password, 'azure_tenant_id': this.azure_tenant_id, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Azure_AzureComplex_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Azure_AzureComplex_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Azure_AzureComplex_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Azure_AzureComplex_delete( this.name );
  }


}

export namespace Azure_AzureComplex
{

}

// Model AzureFoundation from Azure at /api/v1/Azure/AzureFoundation
/*
AzureFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, azure_complex, azure_resource_name)
*/
export class Azure_AzureFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public azure_complex: Azure_AzureComplex | undefined = undefined;
  public azure_resource_name: string | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Azure_AzureFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Azure_AzureFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Azure/AzureFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/Azure/AzureFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<Azure_AzureFoundation> ): void
  {
    data = data as Azure_AzureFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.azure_complex = data.azure_complex !== null ? new Azure_AzureComplex( this._service, data.azure_complex ) : undefined;
    this.azure_resource_name = data.azure_resource_name;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'azure_complex': this.azure_complex !== undefined ? this.azure_complex.toURL() : undefined, 'azure_resource_name': this.azure_resource_name,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'azure_complex': this.azure_complex !== undefined ? this.azure_complex.toURL() : undefined, 'azure_resource_name': this.azure_resource_name,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Azure_AzureFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Azure_AzureFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Azure_AzureFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Azure_AzureFoundation_delete( this.locator );
  }


}

export namespace Azure_AzureFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model AMTFoundation from AMT at /api/v1/AMT/AMTFoundation
/*
AMTFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, amt_username, amt_password, amt_ip_address, plot)
*/
export class AMT_AMTFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public amt_username: string | undefined = 'admin';
  public amt_password: string | undefined = undefined;
  public amt_ip_address: string | undefined = undefined;
  public plot: Survey_Plot | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<AMT_AMTFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as AMT_AMTFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/AMT/AMTFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/AMT/AMTFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<AMT_AMTFoundation> ): void
  {
    data = data as AMT_AMTFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.amt_username = data.amt_username;
    this.amt_password = data.amt_password;
    this.amt_ip_address = data.amt_ip_address;
    this.plot = data.plot !== null ? new Survey_Plot( this._service, data.plot ) : undefined;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'amt_username': this.amt_username, 'amt_password': this.amt_password, 'amt_ip_address': this.amt_ip_address, 'plot': this.plot !== undefined ? this.plot.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'amt_username': this.amt_username, 'amt_password': this.amt_password, 'amt_ip_address': this.amt_ip_address, 'plot': this.plot !== undefined ? this.plot.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.AMT_AMTFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.AMT_AMTFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.AMT_AMTFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.AMT_AMTFoundation_delete( this.locator );
  }


}

export namespace AMT_AMTFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model TestComplex from Test at /api/v1/Test/TestComplex
/*
TestComplex(name, site, description, built_percentage, updated, created, complex_ptr)
*/
export class Test_TestComplex
{
  private _service: Contractor;

  public name: string = '';

  public site: Site_Site | undefined = undefined;
  public description: string | undefined = undefined;
  public built_percentage: number | undefined = 90;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public members: Building_Structure[] | undefined = [];
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Test_TestComplex> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Test_TestComplex;
      this.name = source.name;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Test/TestComplex' ) )
    {
      this.name = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.name = source;
    }
  }

  toString(): string
  {
    return this.name.toString();
  }

  toURL(): string
  {
    return '/api/v1/Test/TestComplex:' + this.name.toString() + ':';
  }

  _update( data: ModelUpdate<Test_TestComplex> ): void
  {
    data = data as Test_TestComplex;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.description = data.description;
    this.built_percentage = data.built_percentage;
    this.updated = data.updated;
    this.created = data.created;
    if( data.members !== undefined )
      this.members = data.members.map( ( uri: string ) => { return new Building_Structure( this._service, uri ) } );
    this.state = data.state;
    this.type = data.type;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'name': this.name, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'description': this.description, 'built_percentage': this.built_percentage, 'members': this.members !== undefined ? this.members.map( i => i.toURL() ) : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Test_TestComplex_get( this.name );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Test_TestComplex_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Test_TestComplex_update( this.name, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Test_TestComplex_delete( this.name );
  }


}

export namespace Test_TestComplex
{

}

// Model TestFoundation from Test at /api/v1/Test/TestFoundation
/*
TestFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, test_delay_variance, test_fail_likelihood)
*/
export class Test_TestFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public test_delay_variance: number | undefined = 30;
  public test_fail_likelihood: number | undefined = 0;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Test_TestFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Test_TestFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Test/TestFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/Test/TestFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<Test_TestFoundation> ): void
  {
    data = data as Test_TestFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.test_delay_variance = data.test_delay_variance;
    this.test_fail_likelihood = data.test_fail_likelihood;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'test_delay_variance': this.test_delay_variance, 'test_fail_likelihood': this.test_fail_likelihood,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'test_delay_variance': this.test_delay_variance, 'test_fail_likelihood': this.test_fail_likelihood,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Test_TestFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Test_TestFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Test_TestFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Test_TestFoundation_delete( this.locator );
  }


}

export namespace Test_TestFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model TestComplexedFoundation from Test at /api/v1/Test/TestComplexedFoundation
/*
TestComplexedFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, complex_host)
*/
export class Test_TestComplexedFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public complex_host: Test_TestComplex | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<Test_TestComplexedFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as Test_TestComplexedFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/Test/TestComplexedFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/Test/TestComplexedFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<Test_TestComplexedFoundation> ): void
  {
    data = data as Test_TestComplexedFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.complex_host = data.complex_host !== null ? new Test_TestComplex( this._service, data.complex_host ) : undefined;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'complex_host': this.complex_host !== undefined ? this.complex_host.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'complex_host': this.complex_host !== undefined ? this.complex_host.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.Test_TestComplexedFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.Test_TestComplexedFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.Test_TestComplexedFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.Test_TestComplexedFoundation_delete( this.locator );
  }


}

export namespace Test_TestComplexedFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model AWSEC2Foundation from AWS at /api/v1/AWS/AWSEC2Foundation
/*
AWSEC2Foundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, awsec2_instance_id)
*/
export class AWS_AWSEC2Foundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public awsec2_instance_id: string | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<AWS_AWSEC2Foundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as AWS_AWSEC2Foundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/AWS/AWSEC2Foundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/AWS/AWSEC2Foundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<AWS_AWSEC2Foundation> ): void
  {
    data = data as AWS_AWSEC2Foundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.awsec2_instance_id = data.awsec2_instance_id;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'awsec2_instance_id': this.awsec2_instance_id,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'awsec2_instance_id': this.awsec2_instance_id,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.AWS_AWSEC2Foundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.AWS_AWSEC2Foundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.AWS_AWSEC2Foundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.AWS_AWSEC2Foundation_delete( this.locator );
  }


}

export namespace AWS_AWSEC2Foundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}

// Model RedFishFoundation from RedFish at /api/v1/RedFish/RedFishFoundation
/*
RedFishFoundation(locator, site, blueprint, id_map, located_at, built_at, updated, created, foundation_ptr, redfish_username, redfish_password, redfish_ip_address, redfish_sol_port, plot)
*/
export class RedFish_RedFishFoundation
{
  private _service: Contractor;

  public locator: string = '';

  public site: Site_Site | undefined = undefined;
  public blueprint: BluePrint_FoundationBluePrint | undefined = undefined;
  public id_map: string | undefined = undefined;
  public located_at: Date | undefined = undefined;
  public built_at: Date | undefined = undefined;
  public updated: Date | undefined = undefined;
  public created: Date | undefined = undefined;
  public redfish_username: string | undefined = undefined;
  public redfish_password: string | undefined = undefined;
  public redfish_ip_address: string | undefined = undefined;
  public redfish_sol_port: 'console' | 'ttyS0' | 'ttyS1' | 'ttyS2' | 'ttyS3' | undefined = 'ttyS1';
  public plot: Survey_Plot | undefined = undefined;
  public state: string | undefined = undefined;
  public type: string | undefined = undefined;
  public class_list: string | undefined = undefined;

  constructor( service: Contractor, source?: ModelConstructorSource<RedFish_RedFishFoundation> )
  {
    this._service = service;
    if( typeof source === 'object' )
    {
      source = source as RedFish_RedFishFoundation;
      this.locator = source.locator;
      this._update( source );
    }
    else if( ( typeof source === 'string' ) && source.startsWith( '/api/v1/RedFish/RedFishFoundation' ) )
    {
      this.locator = source.split( ':' )[ 1 ];
    }
    else if( typeof source === 'string' )
    {
      this.locator = source;
    }
  }

  toString(): string
  {
    return this.locator.toString();
  }

  toURL(): string
  {
    return '/api/v1/RedFish/RedFishFoundation:' + this.locator.toString() + ':';
  }

  _update( data: ModelUpdate<RedFish_RedFishFoundation> ): void
  {
    data = data as RedFish_RedFishFoundation;
    this.site = data.site !== null ? new Site_Site( this._service, data.site ) : undefined;
    this.blueprint = data.blueprint !== null ? new BluePrint_FoundationBluePrint( this._service, data.blueprint ) : undefined;
    this.id_map = data.id_map;
    this.located_at = data.located_at;
    this.built_at = data.built_at;
    this.updated = data.updated;
    this.created = data.created;
    this.redfish_username = data.redfish_username;
    this.redfish_password = data.redfish_password;
    this.redfish_ip_address = data.redfish_ip_address;
    this.redfish_sol_port = data.redfish_sol_port;
    this.plot = data.plot !== null ? new Survey_Plot( this._service, data.plot ) : undefined;
    this.state = data.state;
    this.type = data.type;
    this.class_list = data.class_list;
  }

  _as_map( is_create: boolean ): Record<string, any>
  {
    if( is_create )
      return { 'locator': this.locator, 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'redfish_username': this.redfish_username, 'redfish_password': this.redfish_password, 'redfish_ip_address': this.redfish_ip_address, 'redfish_sol_port': this.redfish_sol_port, 'plot': this.plot !== undefined ? this.plot.toURL() : undefined,  };

    return { 'site': this.site !== undefined ? this.site.toURL() : undefined, 'blueprint': this.blueprint !== undefined ? this.blueprint.toURL() : undefined, 'id_map': this.id_map, 'redfish_username': this.redfish_username, 'redfish_password': this.redfish_password, 'redfish_ip_address': this.redfish_ip_address, 'redfish_sol_port': this.redfish_sol_port, 'plot': this.plot !== undefined ? this.plot.toURL() : undefined,  };
  }


  async _get(): Promise<void>
  {
    const record = await this._service.RedFish_RedFishFoundation_get( this.locator );

    this._update( record );
  }

  async _create(): Promise<void>
  {
    return this._service.RedFish_RedFishFoundation_create( this );
  }

  async _save(): Promise<void>
  {
    return this._service.RedFish_RedFishFoundation_update( this.locator, this );
  }

  async _delete(): Promise<void>
  {
    return this._service.RedFish_RedFishFoundation_delete( this.locator );
  }


}

export namespace RedFish_RedFishFoundation
{

  export class _ListFilter_site implements ListFilter
  {
    public site: Site_Site;

    constructor( site: Site_Site )
    {
      this.site = site;
    }

  };


}
